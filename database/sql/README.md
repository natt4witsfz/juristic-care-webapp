# SQL Workspace

The approved SQL implementation is in [`sql/`](../../sql/). New database changes begin as reviewed design and are captured as Supabase migrations. Do not maintain a second mutable schema copy here.
