# Seed Workspace

Feature-owned deterministic, non-production seed modules are designed here. The executable local seed entry point is [`supabase/seed.sql`](../../supabase/seed.sql). Never copy production personal, Evidence, credential, or secret data into seeds.
