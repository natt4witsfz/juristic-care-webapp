# Service Boundary

Services orchestrate authorized application use cases. They validate input, establish acting context, call domain/repository boundaries, emit audit/event outcomes atomically, and return purpose-specific results. No business feature service is included in the bootstrap.
