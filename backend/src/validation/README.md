# Validation Boundary

Validation schemas protect transport and integration boundaries. Structural validation does not replace domain rules, database constraints, RLS, or human Authority checks. Feature schemas are added with their owning feature and tests.
