const I18N = {
  th: {
    "meta.title": "Juristic Care — ระบบรับแจ้งและบริหารงานนิติบุคคล",
    "auth.heroTitle": "ทุกปัญหาในบ้าน<br>มีคนรับช่วงต่อ",
    "auth.heroText": "แจ้ง ติดตาม และจัดการงานของนิติบุคคล<br>ในที่เดียว โปร่งใส และไม่ตกหล่น",
    "auth.eyebrow": "Awesome Communicate Service",
    "auth.acceptanceRate": "งานได้รับการตอบรับ",
    "auth.totalIssues": "จำนวนปัญหาทั้งหมด",
    "auth.solvedIssues": "แก้ไขแล้ว",
    "auth.ready": "ระบบพร้อมให้บริการ",
    "auth.loginTitle": "ยินดีต้อนรับกลับ",
    "auth.loginSubtitle": "เข้าสู่ระบบเพื่อดูสถานะงานของห้องคุณ",
    "auth.setupTitle": "ตั้งค่าบัญชีครั้งแรก",
    "auth.setupSubtitle": "กรอกเลขห้องและตั้งรหัสผ่านอย่างน้อย 4 ตัว",
    "auth.roomLabel": "เลขห้อง",
    "auth.roomPlaceholder": "เช่น A-1204",
    "auth.passwordLabel": "รหัสผ่าน",
    "auth.passwordPlaceholder": "กรอกรหัสผ่าน",
    "auth.togglePassword": "แสดงหรือซ่อนรหัสผ่าน",
    "auth.loginButton": "เข้าสู่ระบบ",
    "auth.createAccount": "สร้างบัญชี",
    "auth.setupLink": "เข้าใช้งานครั้งแรก / ตั้งรหัสผ่าน",
    "auth.backToLogin": "กลับไปหน้าเข้าสู่ระบบ",
    "auth.privacy": "ข้อมูลของคุณถูกใช้เพื่อการบริหารอาคารเท่านั้น",
    "nav.mainMenu": "เมนูหลัก",
    "nav.mobileMenu": "เมนูสำหรับโทรศัพท์",
    "nav.dashboard": "ภาพรวม",
    "nav.commonWork": "งานส่วนกลาง",
    "nav.commonWorkShort": "ส่วนกลาง",
    "nav.finance": "งบการเงิน",
    "nav.financeShort": "งบ",
    "nav.calendar": "ปฏิทิน",
    "nav.calendarShort": "ปฏิทิน",
    "nav.routineDashboard": "Dashboard งานประจำ",
    "nav.routineToday": "งานวันนี้",
    "nav.routineVerify": "รอตรวจรับ",
    "nav.routineTemplates": "แม่แบบงานประจำ",
    "nav.routineCalendar": "ปฏิทินงานประจำ",
    "nav.routineReports": "รายงานงานประจำ",
    "nav.routineSettings": "ตั้งค่า Daily PM",
    "nav.chat": "Chat with Juristic",
    "nav.chatShort": "Chat",
    "nav.pool": "Pool งานกลาง",
    "nav.poolShort": "Pool",
    "nav.juristicTeam": "งานของทีมนิติ",
    "nav.myJobs": "งานทีมช่าง",
    "nav.myJobsShort": "งานช่าง",
    "nav.housekeepingJobs": "งานแม่บ้าน",
    "nav.housekeepingJobsShort": "แม่บ้าน",
    "nav.roomJobs": "ห้องของฉัน",
    "nav.roomJobsShort": "ห้องฉัน",
    "nav.team": "ทีมงาน",
    "nav.teamShort": "ทีม",
    "nav.residents": "ลูกบ้าน",
    "nav.residentsShort": "ลูกบ้าน",
    "nav.staffLogs": "Staff Log",
    "nav.staffLogsShort": "Staff",
    "nav.logs": "Admin Log",
    "nav.logsShort": "Log",
    "nav.googleFormsSub": "ช่องทางรับแจ้งจากลูกบ้าน",
    "nav.logout": "ออกจากระบบ",
    "common.openMenu": "เปิดเมนู",
    "common.notifications": "การแจ้งเตือน",
    "common.createJob": "สร้างงานใหม่",
    "common.create": "สร้างงาน",
    "common.cancel": "ยกเลิก",
    "common.close": "ปิด",
    "common.viewAll": "ดูทั้งหมด",
    "common.unassigned": "ยังไม่มอบหมาย",
    "common.googleForms": "Google Forms",
    "common.noDescription": "ไม่มีรายละเอียดเพิ่มเติม",
    "dashboard.todayWorkText": "วันนี้มี <strong id=\"todayWork\">0 งาน</strong> ที่ทีมนิติและคณะกรรมการกำลังดูแล",
    "dashboard.greeting": "สวัสดี, {name} 👋",
    "dashboard.today": "วันนี้",
    "dashboard.sevenDays": "ย้อนหลัง 7 วัน",
    "dashboard.thirtyDays": "ย้อนหลัง 30 วัน",
    "dashboard.plusTwoToday": "+2 วันนี้",
    "dashboard.inProgressLong": "รับเรื่องแล้ว",
    "dashboard.plusTwelveMonth": "+12% เดือนนี้",
    "dashboard.completed": "เสร็จสิ้นแล้ว",
    "dashboard.fromReviews": "จาก 28 รีวิว",
    "dashboard.satisfaction": "คะแนนความพึงพอใจ",
    "dashboard.careJobs": "งานที่ต้องดูแล",
    "dashboard.latestJobs": "รายการล่าสุดที่ต้องติดตาม",
    "dashboard.recentActivity": "ความเคลื่อนไหวล่าสุด",
    "dashboard.teamUpdates": "อัปเดตจากทีมงาน",
    "dashboard.categoryOverview": "ภาพรวมตามประเภทของเหตุที่เกิดขึ้น",
    "dashboard.monthlyJobs": "จำนวนงานที่รับแจ้งตามช่วงวันที่ที่เลือก",
    "status.open": "เปิดงาน",
    "status.received": "รับเรื่อง",
    "status.inspecting": "รอตรวจสอบ",
    "status.pending_inspection": "รอตรวจสอบ",
    "status.waiting_repair": "ตรวจสอบแล้วรอแก้ไข/อะไหล่",
    "status.inspected_waiting_repair": "ตรวจสอบแล้วรอแก้ไข/อะไหล่",
    "status.completed": "แก้ไขเรียบร้อยสมบูรณ์แล้ว",
    "status.follow_up": "แก้ไขแล้ว รอติดตาม",
    "status.repaired_follow_up": "แก้ไขแล้ว รอติดตาม",
    "status.temporary_waiting": "แก้ไขเบื้องต้น รออะไหล่/ผู้รับเหมา",
    "status.temporary_waiting_parts": "แก้ไขเบื้องต้น รออะไหล่/ผู้รับเหมา",
    "status.waiting_owner_or_admin_verification": "ช่างแจ้งเสร็จแล้ว รอตรวจสอบ",
    "status.rejected": "ปฏิเสธงาน",
    "priority.normal": "ปกติ",
    "priority.high": "เร่งด่วน",
    "priority.urgent": "ฉุกเฉิน",
    "list.searchPlaceholder": "ค้นหาจากหัวข้อ เลขห้อง หรือเลขที่งาน...",
    "list.statusFilter": "กรองสถานะ",
    "list.categoryFilter": "กรองประเภท",
    "list.allStatuses": "ทุกสถานะ",
    "list.allCategories": "ทุกประเภท",
    "list.resultCount": "พบ <strong id=\"resultCount\">0</strong> รายการ",
    "team.totalMembers": "สมาชิกทั้งหมด",
    "team.available": "พร้อมรับงาน",
    "team.working": "กำลังปฏิบัติงาน",
    "team.away": "ไม่อยู่",
    "team.userList": "รายชื่อผู้ใช้งาน",
    "team.userListSub": "ผู้ดูแล เจ้าหน้าที่ และลูกบ้านในระบบ",
    "modal.jobDetail": "รายละเอียดงาน",
    "modal.location": "พื้นที่ / เลขห้อง",
    "modal.category": "ประเภท",
    "modal.reporter": "ผู้แจ้ง",
    "modal.reportedAt": "วันที่รับแจ้ง",
    "modal.priority": "ความเร่งด่วน",
    "modal.assignee": "ผู้รับผิดชอบ",
    "modal.updateJob": "อัปเดตงาน",
    "modal.saveStatus": "บันทึกสถานะ",
    "create.eyebrow": "NEW WORK ORDER",
    "create.titleLabel": "หัวข้องาน",
    "create.titlePlaceholder": "ระบุปัญหาหรืองานที่ต้องดำเนินการ",
    "create.roomLabel": "เลขห้อง / พื้นที่",
    "create.roomPlaceholder": "เช่น A-1204 หรือ Lobby",
    "create.categoryLabel": "ประเภท",
    "create.priorityLabel": "ระดับความเร่งด่วน",
    "create.assigneeLabel": "มอบหมายให้",
    "create.descriptionLabel": "รายละเอียด",
    "create.descriptionPlaceholder": "อธิบายรายละเอียดเพิ่มเติม",
    "create.toPool": "เข้า Pool กลาง",
    "empty.dashboardJobs": "ยังไม่มีงานที่ต้องดูแล",
    "empty.noResults": "ไม่พบงานตามเงื่อนไข",
    "empty.sub": "รายการใหม่จะแสดงที่นี่โดยอัตโนมัติ",
    "toast.passwordSet": "ตั้งรหัสผ่านเรียบร้อยแล้ว",
    "toast.loginFailed": "เลขห้องหรือรหัสผ่านไม่ถูกต้อง (ทดลอง ADMIN / STAFF-01 / STAFF-02 / A-0201 ใช้รหัส 1234)",
    "toast.claimed": "รับงาน {id} เรียบร้อยแล้ว",
    "toast.statusUpdated": "อัปเดตสถานะงานแล้ว",
    "toast.created": "สร้างงานใหม่เรียบร้อยแล้ว",
    "toast.assigned": "มอบหมายงานแล้ว",
    "toast.accepted": "รับมอบหมายงานแล้ว",
    "page.dashboard.title": "ภาพรวม",
    "page.dashboard.subtitle": "ภาพรวมงานส่วนกลางและงานที่ลูกบ้านเลือกเปิดเผย",
    "page.commonWork.title": "งานส่วนกลาง",
    "page.commonWork.subtitle": "งานพื้นที่ส่วนกลางและงานที่เปิดให้เห็นในภาพรวมของโครงการ",
    "page.finance.title": "งบการเงิน",
    "page.finance.subtitle": "สรุปรายรับ รายจ่าย ค้างชำระ และเงินสำรองของนิติบุคคล",
    "page.calendar.title": "ปฏิทินนิติบุคคล",
    "page.calendar.subtitle": "นัดหมาย งานติดตาม และกำหนดการที่เตรียมเชื่อมต่อ Google Calendar",
    "page.pool.title": "Pool งานกลาง",
    "page.pool.subtitle": "งานดิบจาก Google Form ที่ยังไม่แบ่งประเภทหรือผู้รับผิดชอบ",
    "page.juristicTeam.title": "งานของทีมนิติ",
    "page.juristicTeam.subtitle": "งานที่ทีมนิติสร้าง รับเรื่อง หรือรอการจัดการภายใน",
    "page.myjobs.title": "งานทีมช่าง",
    "page.myjobs.subtitle": "งานที่ได้รับมอบหมายให้ทีมช่างดำเนินการ",
    "page.housekeepingJobs.title": "งานแม่บ้าน",
    "page.housekeepingJobs.subtitle": "งานที่ได้รับมอบหมายให้ทีมแม่บ้านดำเนินการ",
    "page.roomjobs.title": "ห้องของฉัน",
    "page.roomjobs.subtitle": "งานแจ้งภายในห้องชุดของคุณ",
    "page.team.title": "ทีมงาน",
    "page.team.subtitle": "ผู้ใช้งาน สิทธิ์มอบหมาย และทีมช่าง",
    "page.residents.title": "ลูกบ้าน",
    "page.residents.subtitle": "ทะเบียนห้อง ผู้อยู่อาศัย เบอร์ติดต่อ รถ และค่าส่วนกลาง",
    "page.staffLogs.title": "Staff Log",
    "page.staffLogs.subtitle": "บันทึกกิจกรรมของทีมงานทั้งหมด",
    "page.chat.title": "Chat with Juristic",
    "page.chat.subtitle": "ช่องทางแชทส่วนกลางและแชทส่วนตัวระหว่างลูกบ้าน นิติบุคคล และคณะกรรมการ",
    "page.routineDashboard.title": "Dashboard งานประจำ",
    "page.routineDashboard.subtitle": "ภาพรวม Routine Daily Task / Daily PM แยกจากงานแจ้งซ่อมทั่วไป",
    "page.routineToday.title": "งานวันนี้",
    "page.routineToday.subtitle": "งานประจำที่ต้องทำวันนี้ พร้อม Checklist และหลักฐาน",
    "page.routineVerify.title": "รอตรวจรับ",
    "page.routineVerify.subtitle": "งานประจำวันที่ส่งแล้วและรอผู้มีสิทธิ์ตรวจรับ",
    "page.routineTemplates.title": "แม่แบบงานประจำ",
    "page.routineTemplates.subtitle": "จัดการ Template สำหรับสร้างงานซ้ำอัตโนมัติ",
    "page.routineCalendar.title": "ปฏิทินงานประจำ",
    "page.routineCalendar.subtitle": "ดูงานประจำรายวัน รายสัปดาห์ และรายเดือน",
    "page.routineReports.title": "รายงานงานประจำ",
    "page.routineReports.subtitle": "รายงานความครบถ้วน ความตรงเวลา งานช้า และงานถูกตีกลับ",
    "page.routineSettings.title": "ตั้งค่า Daily PM",
    "page.routineSettings.subtitle": "ตั้งค่าหมวดงาน พื้นที่ ผู้ตรวจรับ และค่าเริ่มต้นของหลักฐาน",
    "page.logs.title": "Admin Log",
    "page.logs.subtitle": "บันทึกการเข้าใช้งานและกิจกรรมสำคัญของ Admin",
    "logs.title": "Admin Log",
    "logs.subtitle": "ดูได้เฉพาะผู้ดูแลระบบ และไม่มีปุ่มลบผ่านหน้าระบบ",
    "role.admin": "ผู้ดูแลระบบ",
    "role.coAdmin": "Co-Admin",
    "role.assignmentOfficer": "เจ้าหน้าที่มอบหมายงาน",
    "role.technician": "ทีมช่าง",
    "role.resident": "ลูกบ้าน"
  },
  en: {
    "meta.title": "Juristic Care — Issue Reporting and Work Management",
    "auth.heroTitle": "Every home issue<br>gets a clear owner",
    "auth.heroText": "Report, track, and manage juristic office work<br>in one transparent place.",
    "auth.eyebrow": "Awesome Communicate Service",
    "auth.acceptanceRate": "issues acknowledged",
    "auth.totalIssues": "Total issues",
    "auth.solvedIssues": "Solved",
    "auth.ready": "System ready",
    "auth.loginTitle": "Welcome back",
    "auth.loginSubtitle": "Sign in to track your room's work status",
    "auth.setupTitle": "Set up your account",
    "auth.setupSubtitle": "Enter your room number and create a password of at least 4 characters",
    "auth.roomLabel": "Room number",
    "auth.roomPlaceholder": "e.g. A-1204",
    "auth.passwordLabel": "Password",
    "auth.passwordPlaceholder": "Enter password",
    "auth.togglePassword": "Show or hide password",
    "auth.loginButton": "Sign in",
    "auth.createAccount": "Create account",
    "auth.setupLink": "First-time access / set password",
    "auth.backToLogin": "Back to sign in",
    "auth.privacy": "Your information is used only for building management.",
    "nav.mainMenu": "Main menu",
    "nav.mobileMenu": "Mobile menu",
    "nav.dashboard": "Overview",
    "nav.commonWork": "Common Work",
    "nav.commonWorkShort": "Common",
    "nav.finance": "Finance",
    "nav.financeShort": "Finance",
    "nav.calendar": "Calendar",
    "nav.calendarShort": "Calendar",
    "nav.routineDashboard": "Routine Dashboard",
    "nav.routineToday": "Today Tasks",
    "nav.routineVerify": "Verification",
    "nav.routineTemplates": "Routine Templates",
    "nav.routineCalendar": "Routine Calendar",
    "nav.routineReports": "Routine Reports",
    "nav.routineSettings": "Daily PM Settings",
    "nav.chat": "Chat with Juristic",
    "nav.chatShort": "Chat",
    "nav.pool": "Central Pool",
    "nav.poolShort": "Pool",
    "nav.juristicTeam": "Juristic Team Jobs",
    "nav.myJobs": "Technician Jobs",
    "nav.myJobsShort": "Jobs",
    "nav.housekeepingJobs": "Housekeeping Jobs",
    "nav.housekeepingJobsShort": "Housekeeping",
    "nav.roomJobs": "My Room",
    "nav.roomJobsShort": "Room",
    "nav.team": "Team",
    "nav.teamShort": "Team",
    "nav.residents": "Residents",
    "nav.residentsShort": "Resident",
    "nav.staffLogs": "Staff Log",
    "nav.staffLogsShort": "Staff",
    "nav.logs": "Admin Log",
    "nav.logsShort": "Log",
    "nav.googleFormsSub": "Resident issue intake",
    "nav.logout": "Log out",
    "common.openMenu": "Open menu",
    "common.notifications": "Notifications",
    "common.createJob": "Create Job",
    "common.create": "Create",
    "common.cancel": "Cancel",
    "common.close": "Close",
    "common.viewAll": "View all",
    "common.unassigned": "Unassigned",
    "common.googleForms": "Google Forms",
    "common.noDescription": "No additional details",
    "dashboard.todayWorkText": "Today there are <strong id=\"todayWork\">0 jobs</strong> handled by the juristic team and committee",
    "dashboard.greeting": "Hello, {name} 👋",
    "dashboard.today": "Today",
    "dashboard.sevenDays": "Last 7 days",
    "dashboard.thirtyDays": "Last 30 days",
    "dashboard.plusTwoToday": "+2 today",
    "dashboard.inProgressLong": "Received",
    "dashboard.plusTwelveMonth": "+12% this month",
    "dashboard.completed": "Completed",
    "dashboard.fromReviews": "from 28 reviews",
    "dashboard.satisfaction": "Satisfaction score",
    "dashboard.careJobs": "Jobs to handle",
    "dashboard.latestJobs": "Latest items to follow up",
    "dashboard.recentActivity": "Recent activity",
    "dashboard.teamUpdates": "Updates from the team",
    "dashboard.categoryOverview": "Overview by incident type",
    "dashboard.monthlyJobs": "Jobs received in the selected date range",
    "status.open": "Open",
    "status.received": "Received",
    "status.inspecting": "Pending inspection",
    "status.pending_inspection": "Pending inspection",
    "status.waiting_repair": "Inspected, waiting for repair/parts",
    "status.inspected_waiting_repair": "Inspected, waiting for repair/parts",
    "status.completed": "Fully resolved",
    "status.follow_up": "Repaired, follow-up pending",
    "status.repaired_follow_up": "Repaired, follow-up pending",
    "status.temporary_waiting": "Temporary fix, waiting for parts/contractor",
    "status.temporary_waiting_parts": "Temporary fix, waiting for parts/contractor",
    "status.waiting_owner_or_admin_verification": "Technician completed, pending verification",
    "status.rejected": "Rejected",
    "priority.normal": "Normal",
    "priority.high": "High",
    "priority.urgent": "Urgent",
    "list.searchPlaceholder": "Search by title, room, or ticket number...",
    "list.statusFilter": "Filter status",
    "list.categoryFilter": "Filter category",
    "list.allStatuses": "All statuses",
    "list.allCategories": "All categories",
    "list.resultCount": "Found <strong id=\"resultCount\">0</strong> items",
    "team.totalMembers": "Total members",
    "team.available": "Available",
    "team.working": "Working",
    "team.away": "Away",
    "team.userList": "User list",
    "team.userListSub": "Admins, assignment officers, technicians, and residents",
    "modal.jobDetail": "Job Details",
    "modal.location": "Area / Room",
    "modal.category": "Category",
    "modal.reporter": "Reporter",
    "modal.reportedAt": "Reported at",
    "modal.priority": "Priority",
    "modal.assignee": "Assignee",
    "modal.updateJob": "Update job",
    "modal.saveStatus": "Save status",
    "create.eyebrow": "NEW WORK ORDER",
    "create.titleLabel": "Job title",
    "create.titlePlaceholder": "Describe the issue or work to be done",
    "create.roomLabel": "Room / Area",
    "create.roomPlaceholder": "e.g. A-1204 or Lobby",
    "create.categoryLabel": "Category",
    "create.priorityLabel": "Priority",
    "create.assigneeLabel": "Assign to",
    "create.descriptionLabel": "Details",
    "create.descriptionPlaceholder": "Add more details",
    "create.toPool": "Send to Central Pool",
    "empty.dashboardJobs": "No jobs to handle yet",
    "empty.noResults": "No jobs match your filters",
    "empty.sub": "New items will appear here automatically",
    "toast.passwordSet": "Password has been set",
    "toast.loginFailed": "Room number or password is incorrect (try ADMIN / STAFF-01 / STAFF-02 / A-0201 with 1234)",
    "toast.claimed": "Job {id} has been claimed",
    "toast.statusUpdated": "Job status updated",
    "toast.created": "New job created",
    "toast.assigned": "Job assigned.",
    "toast.accepted": "Assignment accepted",
    "page.dashboard.title": "Overview",
    "page.dashboard.subtitle": "Shared common-area jobs and resident-visible reports",
    "page.commonWork.title": "Common Work",
    "page.commonWork.subtitle": "Common-area jobs and publicly visible project work",
    "page.finance.title": "Finance",
    "page.finance.subtitle": "Income, expenses, unpaid fees, and reserve fund overview",
    "page.calendar.title": "Juristic Calendar",
    "page.calendar.subtitle": "Appointments, follow-ups, and Google Calendar-ready schedule",
    "page.pool.title": "Central Pool",
    "page.pool.subtitle": "Raw Google Form jobs not yet categorized or assigned",
    "page.juristicTeam.title": "Juristic Team Jobs",
    "page.juristicTeam.subtitle": "Jobs created, received, or managed by the juristic team",
    "page.myjobs.title": "Technician Jobs",
    "page.myjobs.subtitle": "Jobs assigned to technician teams",
    "page.housekeepingJobs.title": "Housekeeping Jobs",
    "page.housekeepingJobs.subtitle": "Jobs assigned to housekeeping teams",
    "page.roomjobs.title": "My Room",
    "page.roomjobs.subtitle": "Reports inside your unit",
    "page.team.title": "Team",
    "page.team.subtitle": "Users, assignment authority, and technician teams",
    "page.residents.title": "Residents",
    "page.residents.subtitle": "Room registry, occupants, contacts, cars, and common fee status",
    "page.staffLogs.title": "Staff Log",
    "page.staffLogs.subtitle": "Activity log for all staff members",
    "page.chat.title": "Chat with Juristic",
    "page.chat.subtitle": "Common and private chat channels for residents, juristic staff, and committee members",
    "page.routineDashboard.title": "Routine Dashboard",
    "page.routineDashboard.subtitle": "Overview for Routine Daily Task / Daily PM, separated from repair tickets",
    "page.routineToday.title": "Today Tasks",
    "page.routineToday.subtitle": "Routine tasks due today with checklist and evidence",
    "page.routineVerify.title": "Verification",
    "page.routineVerify.subtitle": "Submitted routine tasks waiting for authorized verification",
    "page.routineTemplates.title": "Routine Templates",
    "page.routineTemplates.subtitle": "Manage recurring PM templates",
    "page.routineCalendar.title": "Routine Calendar",
    "page.routineCalendar.subtitle": "View daily, weekly, and monthly routine tasks",
    "page.routineReports.title": "Routine Reports",
    "page.routineReports.subtitle": "Completion, on-time, late, missed, and rejected reports",
    "page.routineSettings.title": "Daily PM Settings",
    "page.routineSettings.subtitle": "Configure categories, areas, verifiers, and evidence defaults",
    "page.logs.title": "Admin Log",
    "page.logs.subtitle": "Admin sign-ins and important activities",
    "logs.title": "Admin Log",
    "logs.subtitle": "Visible only to Admin and not deletable from the UI",
    "role.admin": "Administrator",
    "role.coAdmin": "Co-Admin",
    "role.assignmentOfficer": "Assignment Officer",
    "role.technician": "Technician Team",
    "role.resident": "Resident"
  }
};

Object.assign(I18N.th, {
  "nav.myJobs": "งานของฉัน",
  "nav.myJobsShort": "งานฉัน",
  "nav.technicianJobs": "งานช่างอาคาร",
  "nav.technicianJobsShort": "ช่างอาคาร",
  "page.myjobs.title": "งานของฉัน",
  "page.myjobs.subtitle": "งานที่มอบหมายเข้าบัญชีของผู้ใช้งานนี้",
  "page.technicianJobs.title": "งานช่างอาคาร",
  "page.technicianJobs.subtitle": "งานที่ได้รับมอบหมายให้แผนกช่างอาคาร",
  "create.mainCategoryLabel": "ประเภทหลัก",
  "dashboard.announcements": "ประกาศ",
  "dashboard.announcementsSub": "เอกสารและรูปภาพสำหรับคณะกรรมการและพนักงาน",
  "category.main.common": "งานส่วนกลาง",
  "category.main.resident": "งานลูกบ้าน"
});
Object.assign(I18N.en, {
  "nav.myJobs": "My Jobs",
  "nav.myJobsShort": "My Jobs",
  "nav.technicianJobs": "Building Technician Jobs",
  "nav.technicianJobsShort": "Technician",
  "page.myjobs.title": "My Jobs",
  "page.myjobs.subtitle": "Jobs assigned to this signed-in account",
  "page.technicianJobs.title": "Building Technician Jobs",
  "page.technicianJobs.subtitle": "Jobs assigned to the building technician department",
  "create.mainCategoryLabel": "Main category",
  "dashboard.announcements": "Announcements",
  "dashboard.announcementsSub": "Documents and images for committee and staff",
  "category.main.common": "Common Work",
  "category.main.resident": "Resident Work"
});
Object.assign(I18N.th, {
  "nav.backhouse": "ประกาศ",
  "nav.backhouseShort": "ประกาศ",
  "nav.organization": "Organization Chart",
  "nav.organizationShort": "Org Chart",
  "page.backhouse.title": "ประกาศ",
  "page.backhouse.subtitle": "จัดการประกาศ เอกสาร PDF และรูปภาพที่แสดงในหน้าภาพรวม",
  "page.organization.title": "Organization Chart",
  "page.organization.subtitle": "จัดตำแหน่งคณะกรรมการและทีมงานนิติบุคคลด้วยการลากโปรไฟล์เข้า Slot",
  "status.completed": "แก้ไขเรียบร้อยสมบูรณ์แล้ว"
});
Object.assign(I18N.en, {
  "nav.backhouse": "Announcements",
  "nav.backhouseShort": "Announcements",
  "nav.organization": "Organization Chart",
  "nav.organizationShort": "Org Chart",
  "page.backhouse.title": "Announcements",
  "page.backhouse.subtitle": "Manage announcement PDFs and images shown on the dashboard",
  "page.organization.title": "Organization Chart",
  "page.organization.subtitle": "Drag committee and juristic team profiles into organization slots",
  "status.completed": "Fully resolved"
});
Object.assign(I18N.th, {
  "nav.residentWork": "งานของลูกบ้าน",
  "nav.residentWorkShort": "ลูกบ้าน",
  "page.residentWork.title": "งานของลูกบ้าน",
  "page.residentWork.subtitle": "รวมงานที่แจ้งจากห้องชุดของลูกบ้าน เห็นได้เฉพาะคณะกรรมการและเจ้าหน้าที่นิติบุคคล"
});
Object.assign(I18N.en, {
  "nav.residentWork": "Resident Jobs",
  "nav.residentWorkShort": "Resident",
  "page.residentWork.title": "Resident Jobs",
  "page.residentWork.subtitle": "Resident unit jobs, visible only to committee and juristic staff"
});
Object.assign(I18N.th, {
  "board.myJobs.title": "กระดานงานของฉัน",
  "board.myJobs.subtitle": "มุมมองแบบ Trello แบ่งช่องตามกรองสถานะของงานที่มอบหมายเข้าบัญชีนี้",
  "board.technician.title": "กระดานงานของช่าง",
  "board.technician.subtitle": "มุมมองแบบ Trello แบ่งช่องตามกรองสถานะของงานช่างอาคาร",
  "board.housekeeping.title": "กระดานงานของแม่บ้าน",
  "board.housekeeping.subtitle": "มุมมองแบบ Trello แบ่งช่องตามกรองสถานะของงานแม่บ้าน",
  "board.juristic.title": "กระดานงานของทีมนิติ",
  "board.juristic.subtitle": "มุมมองแบบ Trello แบ่งช่องตามกรองสถานะของงานที่ทีมนิติดูแล",
  "board.resident.title": "กระดานงานของลูกบ้าน",
  "board.resident.subtitle": "มุมมองแบบ Trello แบ่งช่องตามกรองสถานะของงานลูกบ้าน",
  "board.common.title": "กระดานงานส่วนกลาง",
  "board.common.subtitle": "มุมมองแบบ Trello แบ่งช่องตามกรองสถานะของงานส่วนกลาง",
  "list.viewSwitch": "สลับมุมมอง"
});
Object.assign(I18N.en, {
  "board.myJobs.title": "My Jobs Board",
  "board.myJobs.subtitle": "Trello-style board grouped by status for jobs assigned to this account",
  "board.technician.title": "Technician Jobs Board",
  "board.technician.subtitle": "Trello-style board grouped by status for technician jobs",
  "board.housekeeping.title": "Housekeeping Jobs Board",
  "board.housekeeping.subtitle": "Trello-style board grouped by status for housekeeping jobs",
  "board.juristic.title": "Juristic Team Jobs Board",
  "board.juristic.subtitle": "Trello-style board grouped by status for juristic team jobs",
  "board.resident.title": "Resident Jobs Board",
  "board.resident.subtitle": "Trello-style board grouped by status for resident jobs",
  "board.common.title": "Common Work Board",
  "board.common.subtitle": "Trello-style board grouped by common work status",
  "list.viewSwitch": "Switch view"
});
Object.assign(I18N.th, {
  "nav.permissions": "สิทธิ์การใช้งาน",
  "nav.permissionsShort": "สิทธิ์",
  "page.permissions.title": "สิทธิ์การใช้งาน",
  "page.permissions.subtitle": "กำหนดสิทธิ์เข้า Sidebar และอำนาจการทำงานรายบุคคลโดยไม่ต้องแก้โค้ด"
});
Object.assign(I18N.en, {
  "nav.permissions": "Permissions",
  "nav.permissionsShort": "Permissions",
  "page.permissions.title": "Permissions",
  "page.permissions.subtitle": "Manage sidebar access and action authority per user without editing code"
});

const legacyStatusMap = {
  waiting: "open",
  in_progress: "received",
  inspecting: "pending_inspection",
  waiting_repair: "inspected_waiting_repair",
  done: "completed",
  follow_up: "repaired_follow_up",
  temporary_waiting: "temporary_waiting_parts"
};
const statuses = ["received", "pending_inspection", "inspected_waiting_repair", "completed", "repaired_follow_up", "temporary_waiting_parts", "rejected"];
const statusMeta = {
  open: { th: "เปิดงาน / ยังไม่มอบหมาย", en: "Open / Unassigned" },
  received: { th: "รับเรื่อง", en: "Received" },
  pending_inspection: { th: "รอตรวจสอบ", en: "Pending inspection" },
  inspected_waiting_repair: { th: "ตรวจสอบแล้วรอแก้ไข/อะไหล่", en: "Inspected, waiting for repair/parts" },
  completed: { th: "แก้ไขเรียบร้อยสมบูรณ์แล้ว", en: "Completed" },
  repaired_follow_up: { th: "แก้ไขแล้ว รอติดตาม", en: "Repaired, follow-up pending" },
  temporary_waiting_parts: { th: "แก้ไขเบื้องต้น รออะไหล่/ผู้รับเหมา", en: "Temporary fix, waiting parts/contractor" },
  rejected: { th: "ปฏิเสธงาน", en: "Rejected" }
};
const subStatusMeta = {
  waiting_owner_or_admin_verification: { th: "ช่างแจ้งเสร็จแล้ว รอตรวจสอบ", en: "Technician completed, pending verification" }
};
const statusesNeedNextUpdate = new Set(["pending_inspection", "inspected_waiting_repair", "repaired_follow_up", "temporary_waiting_parts"]);
const dashboardInProgressStatuses = new Set(["received", "pending_inspection", "inspected_waiting_repair", "repaired_follow_up", "temporary_waiting_parts"]);
const mainCategoryProgressStatuses = new Set(["received", "pending_inspection", "inspected_waiting_repair", "repaired_follow_up", "temporary_waiting_parts"]);
const categoryMeta = {
  plumbing: { th: "ประปา", en: "Plumbing", icon: "💧", color: "#397ccb", soft: "#eaf3ff" },
  electrical: { th: "ไฟฟ้า", en: "Electrical", icon: "⚡", color: "#d9991c", soft: "#fff4d9" },
  cleaning: { th: "ความสะอาด", en: "Cleaning", icon: "✦", color: "#178d6d", soft: "#e6f5ef" },
  building: { th: "อาคาร", en: "Building", icon: "▥", color: "#7958c4", soft: "#f1edff" },
  elevator: { th: "ลิฟต์", en: "Elevator", icon: "↕", color: "#d25b5b", soft: "#ffeded" },
  security: { th: "ความปลอดภัย", en: "Security", icon: "◇", color: "#475d86", soft: "#edf1f8" },
  garden: { th: "สวน", en: "Garden", icon: "♧", color: "#4d9a51", soft: "#edf7ed" }
};
const departments = [
  { key: "committee", th: "คณะกรรมการ", en: "Committee" },
  { key: "juristic", th: "เจ้าหน้าที่นิติบุคคล", en: "Juristic Office" },
  { key: "technician", th: "เจ้าหน้าที่ช่างอาคาร", en: "Building Technician" },
  { key: "housekeeping", th: "แม่บ้าน", en: "Housekeeping" },
  { key: "security_staff", th: "พนักงานรักษาความปลอดภัย", en: "Security" },
  { key: "gardener", th: "คนสวน", en: "Gardener" },
  { key: "contractor", th: "ผู้รับเหมา", en: "Contractor" }
];
Object.assign(categoryMeta, {
  furniture: { th: "เฟอร์นิเจอร์", en: "Furniture", icon: "▤", color: "#9a6b46", soft: "#f7eee6" },
  internet: { th: "อินเตอร์เน็ต", en: "Internet", icon: "◌", color: "#2580a8", soft: "#e7f5fb" }
});
categoryMeta.building.th = "โครงสร้างอาคาร";
categoryMeta.building.en = "Building Structure";
categoryMeta.security.th = "ระบบความปลอดภัย";
categoryMeta.security.en = "Security System";
const categoryOrder = ["plumbing", "electrical", "cleaning", "building", "elevator", "garden", "furniture", "security", "internet"];
Object.assign(categoryMeta.plumbing, { color: "#0077E6", soft: "#E4F1FF" });
Object.assign(categoryMeta.electrical, { color: "#D88400", soft: "#FFF0C2" });
Object.assign(categoryMeta.cleaning, { color: "#008A68", soft: "#DFF7EF" });
Object.assign(categoryMeta.building, { color: "#6D35D9", soft: "#EEE5FF" });
Object.assign(categoryMeta.elevator, { color: "#D63B5C", soft: "#FFE3EA" });
Object.assign(categoryMeta.garden, { color: "#2F8A1F", soft: "#E4F8DF" });
Object.assign(categoryMeta.furniture, { color: "#A84F12", soft: "#FFE9D8" });
Object.assign(categoryMeta.security, { color: "#1F4C8F", soft: "#E4ECFF" });
Object.assign(categoryMeta.internet, { color: "#007C9E", soft: "#DDF6FF" });
const mainCategoryMeta = {
  common: { th: "งานส่วนกลาง", en: "Common Work" },
  resident: { th: "งานลูกบ้าน", en: "Resident Work" }
};
const mainCategoryLabel = (key) => mainCategoryMeta[key]?.[currentLang] || mainCategoryMeta[key]?.th || key || "-";
const photo = (label, color) => `data:image/svg+xml;utf8,${encodeURIComponent(`<svg xmlns="http://www.w3.org/2000/svg" width="320" height="220"><rect width="100%" height="100%" rx="18" fill="${color}"/><text x="50%" y="50%" dominant-baseline="middle" text-anchor="middle" font-family="Arial" font-size="28" fill="white">${label}</text></svg>`)}`;

const seedAnnouncements = [
  { id: "ann-01", title: { th: "ประกาศปิดน้ำเพื่อซ่อมปั๊มอาคาร A", en: "Water shutdown for Building A pump repair" }, type: "document", date: "2026-06-26", audience: { th: "คณะกรรมการและพนักงาน", en: "Committee and staff" }, summary: { th: "แนบเอกสารแจ้งกำหนดการและพื้นที่ที่ได้รับผลกระทบ", en: "Attached schedule and affected areas." }, pages: [photo("Water Notice 1", "#397ccb"), photo("Water Notice 2", "#2f80c0")] },
  { id: "ann-02", title: { th: "รูปแบบจุดวางพัสดุชั่วคราว", en: "Temporary parcel station layout" }, type: "image", date: "2026-06-25", audience: { th: "พนักงานนิติและ รปภ.", en: "Juristic and security staff" }, summary: { th: "คลิกเพื่อดูรูปขยายและเลื่อนไปหน้าถัดไป", en: "Open to zoom and browse pages." }, pages: [photo("Parcel Layout", "#178d6d"), photo("Queue Flow", "#4d9a51"), photo("Signage", "#d9991c")] },
  { id: "ann-03", title: { th: "เอกสารประชุมคณะกรรมการรอบเดือน", en: "Monthly committee meeting pack" }, type: "document", date: "2026-06-24", audience: { th: "คณะกรรมการ", en: "Committee" }, summary: { th: "ไฟล์สรุปวาระ งานคงค้าง และงบประมาณเบื้องต้น", en: "Agenda, backlog, and budget snapshot." }, pages: [photo("Agenda", "#475d86"), photo("Backlog", "#7958c4")] }
];
let announcements = JSON.parse(localStorage.getItem("juristicBackhouseAnnouncements") || "null") || seedAnnouncements;
announcements = announcements.map(item => ({
  ...item,
  summary: typeof item.summary === "object" ? {
    ...item.summary,
    th: String(item.summary.th || "").replaceAll("Backhouse", "ประกาศ"),
    en: String(item.summary.en || "").replaceAll("Backhouse", "Announcements")
  } : item.summary
}));
let activeAnnouncementIndex = 0;
let activeAnnouncementPage = 0;
let activeAnnouncementDownloadUrl = "";

const organizationSlots = [
  { id: "committee-1", row: 1, title: "คณะกรรมการ", dept: "committee" },
  { id: "committee-2", row: 1, title: "คณะกรรมการ", dept: "committee" },
  { id: "committee-3", row: 1, title: "คณะกรรมการ", dept: "committee" },
  { id: "building-manager", row: 2, title: "ผู้จัดการอาคาร", dept: "juristic" },
  { id: "assistant-manager", row: 3, title: "ผู้ช่วยผู้จัดการอาคาร", dept: "juristic" },
  { id: "chief-technician", row: 3, title: "หัวหน้าช่างอาคาร", dept: "technician" },
  ...Array.from({ length: 4 }, (_, index) => ({ id: `juristic-staff-${index + 1}`, row: 4, title: `พนักงานนิติบุคคล ${index + 1}`, dept: "juristic" })),
  ...Array.from({ length: 4 }, (_, index) => ({ id: `technician-staff-${index + 1}`, row: 4, title: `ช่างอาคาร ${index + 1}`, dept: "technician" }))
];
const organizationLegacySlotMap = {
  "building-manager": "juristic-1",
  "assistant-manager": "juristic-2",
  "chief-technician": "technician-1",
  ...Object.fromEntries(Array.from({ length: 20 }, (_, index) => [`juristic-staff-${index + 1}`, `juristic-${index + 3}`])),
  ...Object.fromEntries(Array.from({ length: 20 }, (_, index) => [`technician-staff-${index + 1}`, `technician-${index + 2}`]))
};
function normalizeOrganizationAssignments(assignments = {}) {
  return Object.entries(assignments || {}).reduce((normalized, [slotId, userId]) => {
    if (!userId) return normalized;
    const normalizedSlotId = organizationLegacySlotMap[slotId] || slotId;
    if (!normalized[normalizedSlotId]) normalized[normalizedSlotId] = userId;
    return normalized;
  }, {});
}
let organizationAssignments = normalizeOrganizationAssignments(JSON.parse(localStorage.getItem("juristicOrganizationAssignments") || "null") || {});
let organizationDraftAssignments = { ...organizationAssignments };
let organizationDirty = false;

const users = [
  { id: "u01", name: "นภา บริหาร", enName: "Napa Admin", room: "ADMIN", role: "admin", roleKey: "role.admin", password: "1234", canAssign: true },
  { id: "u02", name: "สมชาย หัวหน้าช่าง", enName: "Somchai Chief", room: "STAFF-01", role: "staff", roleKey: "role.assignmentOfficer", password: "1234", canAssign: true },
  { id: "u03", name: "วิชัย ทีมประปา", enName: "Wichai Plumbing", room: "STAFF-02", role: "staff", roleKey: "role.technician", password: "1234", team: "plumbing" },
  { id: "u04", name: "สุเมธ ทีมไฟฟ้า", enName: "Sumet Electrical", room: "STAFF-03", role: "staff", roleKey: "role.technician", password: "1234", team: "electrical" },
  { id: "u05", name: "มาลี แม่บ้าน", enName: "Malee Cleaning", room: "STAFF-04", role: "staff", roleKey: "role.technician", password: "1234", team: "cleaning" },
  { id: "u06", name: "ธนา รปภ.", enName: "Thana Security", room: "STAFF-05", role: "staff", roleKey: "role.technician", password: "1234", team: "security" },
  { id: "u07", name: "ปกรณ์ ช่างสวน", enName: "Pakorn Garden", room: "STAFF-06", role: "staff", roleKey: "role.technician", password: "1234", team: "garden" },
  ...Array.from({ length: 23 }, (_, i) => ({
    id: `u${String(i + 8).padStart(2, "0")}`,
    name: ["กิตติ วัฒนะ", "พิมพ์ชนก มณี", "อรทัย ใจดี", "ณัฐพล รุ่งเรือง", "วราภรณ์ สุขใจ", "ธีรภัทร คงมั่น", "ชุติมา พรชัย", "ปวีณา แก้วใส", "กรณ์ธนา พูลผล", "อัญชลี มีสุข", "ภาคิน ทองดี", "ศิริพร รัตนะ", "ชนินทร์ สุขสันต์", "มนัสวี ตั้งใจ", "ธัญชนก พรหมมา", "รณชัย แสงดี", "พัชรี อุดม", "เขมิกา ชื่นใจ", "วุฒิชัย ใจกล้า", "นริศรา ศรีสุข", "ไพลิน กิตติ", "ธนภัทร เกษม", "สุภาวดี งามดี"][i],
    enName: `Resident ${String(i + 1).padStart(2, "0")}`,
    room: `${i % 2 ? "B" : "A"}-${String(Math.floor(i / 2) + 2).padStart(2, "0")}${String((i % 8) + 1).padStart(2, "0")}`,
    role: "resident", roleKey: "role.resident", password: "1234"
  }))
];

const seedJobs = [
  { id: "GF-260625-001", source: "Google Form", raw: true, title: { th: "มีน้ำหยดจากฝ้าในห้องน้ำ", en: "Water dripping from bathroom ceiling" }, room: "A-0201", areaType: "unit", sharePublic: false, category: null, priority: "high", status: "open", reporter: "u08", date: "2026-06-25", time: "09:18", description: { th: "ลูกบ้านแนบรูปจาก Google Form ยังไม่ได้คัดแยกประเภทงาน", en: "Resident submitted photos via Google Form; not yet categorized." }, attachments: [photo("Google Form Photo", "#397ccb")] },
  { id: "GF-260625-002", source: "Google Form", raw: true, title: { th: "ไฟหน้าลิฟต์ชั้น 8 ดับ", en: "Elevator-front light on floor 8 is out" }, room: { th: "ส่วนกลาง · ชั้น 8", en: "Common Area · Floor 8" }, areaType: "common", sharePublic: true, category: null, priority: "normal", status: "open", reporter: "u12", date: "2026-06-25", time: "10:02", description: { th: "งานดิบจาก Google Form ยังไม่ระบุหมวดหมู่", en: "Raw Google Form item with no category yet." }, attachments: [photo("Corridor", "#d9991c")] },
  { id: "JC-260625-003", source: "Assigned", raw: false, title: { th: "น้ำรั่วใต้ซิงค์ล้างจาน", en: "Water leak under kitchen sink" }, room: "A-1204", areaType: "unit", sharePublic: false, category: "plumbing", priority: "urgent", status: "received", assignee: "u03", reporter: "u09", date: "2026-06-25", time: "11:20", description: { th: "มอบหมายงานให้ช่างดำเนินการแล้ว", en: "Assigned to technician." }, attachments: [photo("Leak", "#2f80c0")] },
  { id: "JC-260625-004", source: "Assigned", raw: false, title: { th: "กล้องวงจรปิดมุมจอดรถไม่แสดงผล", en: "Parking corner CCTV is offline" }, room: { th: "ส่วนกลาง · Parking B1", en: "Common Area · Parking B1" }, areaType: "common", sharePublic: true, category: "security", priority: "high", status: "follow_up", assignee: "u06", reporter: "u01", date: "2026-06-24", time: "15:45", nextUpdateDate: "2026-06-27", description: { th: "แก้ไขเบื้องต้นแล้ว รอติดตามภาพอีกครั้ง", en: "Initial repair completed; pending follow-up." }, attachments: [photo("Before", "#475d86"), photo("After", "#178d6d")] }
];

const routineDepartments = [
  { key: "technician", th: "ช่างอาคาร", en: "Technician" },
  { key: "housekeeping", th: "แม่บ้าน", en: "Housekeeping" },
  { key: "security", th: "รปภ.", en: "Security" },
  { key: "juristic", th: "นิติบุคคล", en: "Juristic" }
];
const routineStatusMeta = {
  scheduled: { th: "ยังไม่ถึงเวลา", en: "Scheduled", tone: "gray" },
  ready: { th: "พร้อมทำงาน", en: "Ready", tone: "blue" },
  in_progress: { th: "กำลังทำ", en: "In progress", tone: "blue" },
  submitted: { th: "ส่งงานแล้ว", en: "Submitted", tone: "amber" },
  pending_verification: { th: "รอตรวจรับ", en: "Pending verification", tone: "amber" },
  approved: { th: "อนุมัติแล้ว", en: "Approved", tone: "green" },
  completed: { th: "เสร็จสมบูรณ์", en: "Completed", tone: "green" },
  rejected: { th: "ตีกลับ", en: "Rejected", tone: "orange" },
  rework: { th: "ต้องแก้ไข", en: "Rework", tone: "orange" },
  missed: { th: "เลยเวลา", en: "Missed", tone: "red" },
  late_submitted: { th: "ส่งช้า", en: "Late submitted", tone: "red" },
  cancelled: { th: "ยกเลิก", en: "Cancelled", tone: "gray" }
};
const seedRoutineTemplates = [
  {
    id: "rtemp-electric-room", title: "ตรวจห้องไฟประจำวัน", description: "ตรวจสภาพห้องไฟ ตู้ MDB กลิ่นไหม้ เสียงผิดปกติ และค่ากระแส/แรงดัน", department: "technician", area_name: "ห้องไฟ ชั้น 1", building: "A", floor: "1", frequency: "daily", days_of_week: [], start_time: "08:00", end_time: "09:00", grace_minutes: 10, assignee_type: "user", assignee_id: "u04", assignee_team_id: "technician", verifier_role: "juristic", verifier_id: "u02", requires_photo: true, min_photos: 2, max_photos: 3, requires_video: true, min_videos: 1, requires_meter_reading: true, is_active: true, created_by: "u01", created_at: "2026-06-29T08:00:00", updated_at: "2026-06-29T08:00:00",
    checklist: [
      { id: "rc-electric-video", label: "ถ่ายวิดีโอภาพรวมห้องไฟ", input_type: "video", is_required: true, sort_order: 1, help_text: "" },
      { id: "rc-electric-clean", label: "ตรวจความสะอาดและสิ่งกีดขวาง", input_type: "checkbox", is_required: true, sort_order: 2, help_text: "" },
      { id: "rc-electric-smell", label: "ตรวจกลิ่นไหม้ / เสียงผิดปกติ", input_type: "abnormal_note", is_required: true, sort_order: 3, help_text: "หากพบผิดปกติให้ระบุรายละเอียด" },
      { id: "rc-electric-temp", label: "อุณหภูมิห้อง", input_type: "meter_reading", is_required: true, sort_order: 4, help_text: "°C" },
      { id: "rc-electric-v1", label: "Voltage L1 / L2 / L3", input_type: "text", is_required: true, sort_order: 5, help_text: "เช่น 220/221/219" },
      { id: "rc-electric-a1", label: "Ampere L1 / L2 / L3", input_type: "text", is_required: true, sort_order: 6, help_text: "เช่น 15/14/15" },
      { id: "rc-electric-em", label: "ตรวจไฟ Emergency Light", input_type: "checkbox", is_required: true, sort_order: 7, help_text: "" }
    ]
  },
  {
    id: "rtemp-trash-room", title: "ทำความสะอาดห้องขยะช่วงเช้า", description: "ทำความสะอาดห้องขยะ เก็บขยะตกค้าง ล้างพื้น และฉีดน้ำยาดับกลิ่น", department: "housekeeping", area_name: "ห้องขยะ ชั้น 1", building: "A", floor: "1", frequency: "daily", days_of_week: [], start_time: "07:00", end_time: "08:00", grace_minutes: 10, assignee_type: "user", assignee_id: "u05", assignee_team_id: "housekeeping", verifier_role: "juristic", verifier_id: "u02", requires_photo: true, min_photos: 3, max_photos: 5, requires_video: true, min_videos: 1, requires_meter_reading: false, is_active: true, created_by: "u01", created_at: "2026-06-29T08:00:00", updated_at: "2026-06-29T08:00:00",
    checklist: [
      { id: "rc-trash-before", label: "ถ่ายรูปก่อนทำความสะอาด", input_type: "photo", is_required: true, sort_order: 1, help_text: "" },
      { id: "rc-trash-collect", label: "เก็บขยะตกค้าง", input_type: "checkbox", is_required: true, sort_order: 2, help_text: "" },
      { id: "rc-trash-wash-front", label: "ล้างพื้นหน้าห้องขยะ", input_type: "checkbox", is_required: true, sort_order: 3, help_text: "" },
      { id: "rc-trash-wash-inside", label: "ล้างพื้นในห้องขยะ", input_type: "checkbox", is_required: true, sort_order: 4, help_text: "" },
      { id: "rc-trash-door", label: "เช็ดประตู / มือจับ", input_type: "checkbox", is_required: true, sort_order: 5, help_text: "" },
      { id: "rc-trash-odor", label: "ฉีดน้ำยาดับกลิ่น", input_type: "checkbox", is_required: true, sort_order: 6, help_text: "" },
      { id: "rc-trash-note", label: "ระบุความผิดปกติหากพบ", input_type: "abnormal_note", is_required: false, sort_order: 7, help_text: "" },
      { id: "rc-trash-after-video", label: "ถ่ายวิดีโอหลังล้างพื้น", input_type: "video", is_required: true, sort_order: 8, help_text: "" }
    ]
  },
  {
    id: "rtemp-pump-room", title: "ตรวจห้องปั๊มน้ำ", description: "ตรวจปั๊มน้ำ แรงดัน เสียงผิดปกติ และสภาพพื้นที่", department: "technician", area_name: "ห้องปั๊มน้ำ B1", building: "B", floor: "B1", frequency: "daily", days_of_week: [], start_time: "09:00", end_time: "10:00", grace_minutes: 10, assignee_type: "user", assignee_id: "u03", assignee_team_id: "technician", verifier_role: "juristic", verifier_id: "u02", requires_photo: true, min_photos: 2, max_photos: 3, requires_video: false, min_videos: 0, requires_meter_reading: true, is_active: true, created_by: "u01", created_at: "2026-06-29T08:00:00", updated_at: "2026-06-29T08:00:00",
    checklist: [
      { id: "rc-pump-pressure", label: "บันทึกค่าแรงดันน้ำ", input_type: "meter_reading", is_required: true, sort_order: 1, help_text: "bar" },
      { id: "rc-pump-sound", label: "ตรวจเสียงผิดปกติ", input_type: "abnormal_note", is_required: true, sort_order: 2, help_text: "" },
      { id: "rc-pump-leak", label: "ตรวจรอยรั่ว", input_type: "checkbox", is_required: true, sort_order: 3, help_text: "" }
    ]
  },
  {
    id: "rtemp-fitness-close", title: "ตรวจฟิตเนสหลังปิดใช้งาน", description: "ตรวจความเรียบร้อย ของตกค้าง ไฟ และเครื่องออกกำลังกายหลังปิด", department: "juristic", area_name: "ห้องฟิตเนส ชั้น 2", building: "A", floor: "2", frequency: "daily", days_of_week: [], start_time: "21:30", end_time: "22:00", grace_minutes: 15, assignee_type: "user", assignee_id: "u02", assignee_team_id: "juristic", verifier_role: "juristic", verifier_id: "u01", requires_photo: true, min_photos: 2, max_photos: 3, requires_video: false, min_videos: 0, requires_meter_reading: false, is_active: true, created_by: "u01", created_at: "2026-06-29T08:00:00", updated_at: "2026-06-29T08:00:00",
    checklist: [
      { id: "rc-fit-clean", label: "ตรวจความเรียบร้อยโดยรวม", input_type: "checkbox", is_required: true, sort_order: 1, help_text: "" },
      { id: "rc-fit-lost", label: "ตรวจของตกค้าง", input_type: "abnormal_note", is_required: true, sort_order: 2, help_text: "หากพบให้ระบุ" },
      { id: "rc-fit-light", label: "ปิดไฟและอุปกรณ์ที่ไม่จำเป็น", input_type: "checkbox", is_required: true, sort_order: 3, help_text: "" }
    ]
  }
];

const seedResidentRooms = [
  {
    id: "room-a0201", roomNo1: "A-0201", roomNo2: "A0201", building: "A", commonFeeStatus: "paid",
    owner: { firstName: "กิตติ", lastName: "วัฒนะ", nickName: "กิต", phone: "081-234-5678", status: "owner", isPrimary: true },
    occupants: [{ firstName: "พิมพ์ชนก", lastName: "มณี", nickName: "พิม", phone: "089-222-1100", status: "tenant", isPrimary: false }],
    cars: [{ prefix: "กข", number: "1234", province: "กรุงเทพมหานคร", brand: "Toyota", model: "Yaris", color: "ขาว" }]
  },
  {
    id: "room-a1204", roomNo1: "A-1204", roomNo2: "A1204", building: "A", commonFeeStatus: "unpaid",
    owner: { firstName: "ณัฐพล", lastName: "รุ่งเรือง", nickName: "นัท", phone: "082-678-9012", status: "owner", isPrimary: true },
    occupants: [],
    cars: [{ prefix: "2กต", number: "8808", province: "นนทบุรี", brand: "Honda", model: "Civic", color: "ดำ" }, { prefix: "ขจ", number: "45", province: "กรุงเทพมหานคร", brand: "MG", model: "ZS", color: "เทา" }]
  },
  {
    id: "room-b0302", roomNo1: "B-0302", roomNo2: "B0302", building: "B", commonFeeStatus: "paid",
    owner: { firstName: "วราภรณ์", lastName: "สุขใจ", nickName: "วรา", phone: "086-554-1009", status: "owner", isPrimary: true },
    occupants: [{ firstName: "ธีรภัทร", lastName: "คงมั่น", nickName: "ธี", phone: "094-110-2345", status: "resident", isPrimary: false }],
    cars: []
  }
];

let jobs = JSON.parse(localStorage.getItem("juristicJobsV2") || "null") || seedJobs;
if (!jobs.some(job => job.source === "Google Form" || job.raw)) {
  jobs = seedJobs;
  localStorage.setItem("juristicJobsV2", JSON.stringify(jobs));
}
let routineTemplates = JSON.parse(localStorage.getItem("juristicRoutineTemplates") || "null") || seedRoutineTemplates;
let routineTasks = JSON.parse(localStorage.getItem("juristicRoutineTasks") || "null") || [];
let routineTaskResults = JSON.parse(localStorage.getItem("juristicRoutineTaskResults") || "null") || [];
let routineTaskAttachments = JSON.parse(localStorage.getItem("juristicRoutineTaskAttachments") || "null") || [];
let routineTaskLogs = JSON.parse(localStorage.getItem("juristicRoutineTaskLogs") || "null") || [];
let adminLogs = JSON.parse(localStorage.getItem("juristicAdminLogs") || "null") || [];
let staffLogs = JSON.parse(localStorage.getItem("juristicStaffLogs") || "null") || [];
let residentRooms = JSON.parse(localStorage.getItem("juristicResidentRooms") || "null") || seedResidentRooms;
let teamOverrides = JSON.parse(localStorage.getItem("juristicTeamOverrides") || "null") || {};
const seedChatThreads = [
  {
    id: "chat-common",
    type: "common",
    title: { th: "ส่วนกลาง", en: "Common Area" },
    roomNo: "",
    residentId: "",
    updatedAt: "2026-06-30T08:15:00",
    messages: [
      { id: "msg-common-1", by: "u01", at: "2026-06-30T08:00:00", text: "วันนี้จะมีทีมช่างตรวจระบบไฟทางเดินชั้น 8 เวลา 10:00 น." },
      { id: "msg-common-2", by: "u12", at: "2026-06-30T08:15:00", text: "รับทราบค่ะ ถ้ามีเสียงรบกวนจะได้แจ้งลูกบ้านในชั้นเดียวกัน" }
    ]
  },
  {
    id: "chat-room-a0201",
    type: "resident_private",
    title: { th: "A-0201 · กิตติ วัฒนะ", en: "A-0201 · Kitti Wattana" },
    roomNo: "A-0201",
    residentId: "u08",
    updatedAt: "2026-06-29T18:40:00",
    messages: [
      { id: "msg-a0201-1", by: "u08", at: "2026-06-29T18:20:00", text: "รบกวนสอบถามความคืบหน้างานน้ำหยดจากฝ้าห้องน้ำครับ" },
      { id: "msg-a0201-2", by: "u02", at: "2026-06-29T18:40:00", text: "รับทราบครับ พรุ่งนี้ทีมช่างจะเข้าตรวจสอบช่วงเช้า" }
    ]
  },
  {
    id: "chat-room-a1204",
    type: "resident_private",
    title: { th: "A-1204 · พิมพ์ชนก มณี", en: "A-1204 · Pimchanok Manee" },
    roomNo: "A-1204",
    residentId: "u09",
    updatedAt: "2026-06-28T11:05:00",
    messages: [
      { id: "msg-a1204-1", by: "u09", at: "2026-06-28T10:55:00", text: "ถ้าช่างเข้าห้อง รบกวนโทรแจ้งก่อน 15 นาทีค่ะ" },
      { id: "msg-a1204-2", by: "u03", at: "2026-06-28T11:05:00", text: "ได้ครับ จะให้ช่างโทรแจ้งก่อนเข้าดำเนินการ" }
    ]
  },
  {
    id: "chat-room-b0302",
    type: "resident_private",
    title: { th: "B-0302 · วราภรณ์ สุขใจ", en: "B-0302 · Waraporn Sukjai" },
    roomNo: "B-0302",
    residentId: "u12",
    updatedAt: "2026-06-27T13:30:00",
    messages: [
      { id: "msg-b0302-1", by: "u12", at: "2026-06-27T13:30:00", text: "สอบถามเรื่องใบเสร็จค่าส่วนกลางประจำปีค่ะ" }
    ]
  }
];
let chatThreads = JSON.parse(localStorage.getItem("juristicChatThreads") || "null") || seedChatThreads;
if (!chatThreads.some(thread => thread.type === "common")) {
  chatThreads = [...seedChatThreads, ...chatThreads];
  localStorage.setItem("juristicChatThreads", JSON.stringify(chatThreads));
}
let customUsers = JSON.parse(localStorage.getItem("juristicCustomUsers") || "null") || [];
let deletedUserIds = JSON.parse(localStorage.getItem("juristicDeletedUsers") || "null") || [];
users.push(...customUsers.filter(custom => !users.some(user => user.id === custom.id)));
let currentUser = null;
let currentView = "dashboard";
let setupMode = false;
let deviceMode = "desktop";
let currentLang = localStorage.getItem("juristicLang") || "th";
let teamSearch = "";
let teamDepartmentFilter = "all";
let residentSearch = "";
let residentBuildingFilter = "all";
let adminLogDateFilter = "";
let adminLogRoleFilter = "all";
let viewModes = JSON.parse(localStorage.getItem("juristicViewModes") || "null") || {};
const remoteConfig = window.JURISTIC_CONFIG || {};
let remoteSyncReady = !!(remoteConfig.REMOTE_SYNC && remoteConfig.APPS_SCRIPT_URL);
let remoteSyncBusy = false;
let dashboardRange = localStorage.getItem("juristicDashboardRange") || "today";
let commonWorkRange = localStorage.getItem("juristicCommonWorkRange") || "30";
if (commonWorkRange === "60") commonWorkRange = "all";
let permissionSearch = "";
let permissionDepartmentFilter = "all";
let bulkPermissionGroup = "residents";
let bulkPermissionPreset = "residentStandard";
let bulkPermissionMode = "replace";
let googleCalendarConnected = localStorage.getItem("juristicGoogleCalendarConnected") === "true";
let selectedCalendarEventId = localStorage.getItem("juristicSelectedCalendarEvent") || "cal-01";
let selectedCalendarDay = Number(localStorage.getItem("juristicSelectedCalendarDay") || "27");
let selectedCalendarMonth = Number(localStorage.getItem("juristicSelectedCalendarMonth") || "5");
let selectedCalendarYear = Number(localStorage.getItem("juristicSelectedCalendarYear") || "2026");
let chatScope = "common";
let chatSearch = "";
let chatRoomFilter = "all";
let activeChatRoomId = "";
let collapsedSidebarGroups = JSON.parse(localStorage.getItem("juristicCollapsedSidebarGroups") || "{}");
let routineDashboardDate = localStorage.getItem("juristicRoutineDashboardDate") || todayIso();
let routineDepartmentFilter = "all";
let routineStatusFilter = "all";
let activeRoutineTaskId = "";
let calendarPickerMode = "";
let backhouseDirty = false;

const calendarEvents = [
  { id: "cal-01", day: 10, date: "2026-06-10", time: "09:30", title: { th: "ตรวจระบบปั๊มน้ำอาคาร A", en: "Building A pump inspection" }, location: { th: "ห้องปั๊มน้ำ ชั้น B1", en: "Pump room B1" }, owner: { th: "ทีมช่างอาคาร", en: "Technician team" }, detail: { th: "ตรวจแรงดันน้ำและวาล์วหลัก พร้อมแนบผลตรวจในระบบหลังจบงาน", en: "Inspect water pressure and main valves, then attach the report." } },
  { id: "cal-02", day: 16, date: "2026-06-16", time: "14:00", title: { th: "ประชุมคณะกรรมการประจำเดือน", en: "Monthly committee meeting" }, location: { th: "ห้องประชุมชั้น 1", en: "Meeting room, 1st floor" }, owner: { th: "คณะกรรมการ", en: "Committee" }, detail: { th: "วาระ: งบประมาณ งานคงค้าง และแผนซ่อมบำรุงเดือนถัดไป", en: "Agenda: budget, backlog, and next month's maintenance plan." } },
  { id: "cal-03", day: 27, date: "2026-06-27", time: "10:30", title: { th: "ติดตามกล้องวงจรปิด B1", en: "Follow up B1 CCTV" }, location: { th: "ลานจอดรถ B1", en: "Parking B1" }, owner: { th: "ฝ่ายรักษาความปลอดภัย", en: "Security team" }, detail: { th: "ตรวจภาพหลังแก้ไขและบันทึกผลติดตามในใบงาน JC-260625-004", en: "Check post-repair footage and update job JC-260625-004." } },
  { id: "cal-04", day: 30, date: "2026-06-30", time: "16:00", title: { th: "ตัดรอบงบการเงินประจำเดือน", en: "Monthly finance cutoff" }, location: { th: "ฝ่ายบัญชี", en: "Accounting" }, owner: { th: "พนักงานนิติบุคคล", en: "Juristic staff" }, detail: { th: "สรุปรายรับ รายจ่าย และเอกสารแนบสำหรับ Export ให้ลูกบ้านดูได้", en: "Summarize income, expenses, and attachments for resident export." } },
  { id: "cal-05", day: 28, date: "2026-06-28", time: "09:00", title: { th: "ตรวจความสะอาดพื้นที่ส่วนกลาง", en: "Common-area cleaning audit" }, location: { th: "Lobby และชั้นส่วนกลาง", en: "Lobby and shared floors" }, owner: { th: "ทีมแม่บ้าน", en: "Housekeeping" }, detail: { th: "ตรวจจุดทำความสะอาดประจำสัปดาห์และแนบรูปหลังตรวจ", en: "Weekly cleaning checkpoint with photos after inspection." } },
  { id: "cal-06", day: 29, date: "2026-06-29", time: "11:00", title: { th: "ทดสอบระบบแจ้งเหตุเพลิงไหม้", en: "Fire alarm system test" }, location: { th: "ห้องควบคุมอาคาร", en: "Building control room" }, owner: { th: "ช่างอาคาร", en: "Building technician" }, detail: { th: "ทดสอบสัญญาณแจ้งเหตุและแจ้งเตือนล่วงหน้ากับลูกบ้าน", en: "Test alarms and resident notification flow." } },
  { id: "cal-07", day: 30, date: "2026-06-30", time: "10:00", title: { th: "ส่งรายงานผู้รับเหมาลิฟต์", en: "Elevator contractor report" }, location: { th: "สำนักงานนิติบุคคล", en: "Juristic office" }, owner: { th: "พนักงานนิติบุคคล", en: "Juristic staff" }, detail: { th: "รับรายงาน PM ลิฟต์และบันทึกเอกสารไว้ในประกาศ", en: "Collect elevator PM report and archive it in Announcements." } }
];

const $ = (s) => document.querySelector(s);
const $$ = (s) => [...document.querySelectorAll(s)];
const t = (key, vars = {}) => Object.entries(vars).reduce((text, [k, v]) => text.replaceAll(`{${k}}`, v), I18N[currentLang]?.[key] || I18N.th[key] || key);
const getUser = (id) => users.find(u => u.id === id);
const getText = (value) => typeof value === "object" && value !== null ? (value[currentLang] || value.th || value.en || "") : value;
const getUserName = (user) => currentLang === "en" ? (user?.enName || user?.name || "") : (user?.name || "");
const initials = (name) => name.split(" ").map(x => x[0]).join("").slice(0, 2).toUpperCase();
const roleLabel = (user) => t(user.roleKey || `role.${user.role}`);
const departmentLabel = (key) => departments.find(dept => dept.key === key)?.[currentLang] || key || "-";
const activeUsers = () => users.filter(user => !deletedUserIds.includes(user.id));
const permissionSidebarItems = [
  { key: "dashboard", view: "dashboard", icon: "▦", label: { th: "ภาพรวม", en: "Overview" }, defaultAccess: user => user?.role !== "resident" || true },
  { key: "commonWork", view: "commonWork", icon: "▧", label: { th: "งานส่วนกลาง", en: "Common Work" }, defaultAccess: user => user?.role !== "resident" },
  { key: "residentWork", view: "residentWork", icon: "▨", label: { th: "งานของลูกบ้าน", en: "Resident Jobs" }, defaultAccess: user => user?.role !== "resident" && ["committee", "juristic"].includes(inferDepartment(user || {})) },
  { key: "finance", view: "finance", icon: "฿", label: { th: "งบการเงิน", en: "Finance" }, defaultAccess: user => user?.role !== "resident" },
  { key: "calendar", view: "calendar", icon: "◴", label: { th: "ปฏิทิน", en: "Calendar" }, defaultAccess: user => user?.role !== "resident" },
  { key: "chat", view: "chat", icon: "✉", label: { th: "Chat with Juristic", en: "Chat with Juristic" }, defaultAccess: user => !!user },
  { key: "routineDashboard", view: "routineDashboard", icon: "▣", label: { th: "Dashboard งานประจำ", en: "Routine Dashboard" }, defaultAccess: user => user?.role === "admin" || user?.isCoAdmin || ["juristic", "committee"].includes(inferDepartment(user || {})) },
  { key: "routineToday", view: "routineToday", icon: "☑", label: { th: "งานวันนี้", en: "Today Tasks" }, defaultAccess: user => user?.role !== "resident" },
  { key: "routineVerify", view: "routineVerify", icon: "◇", label: { th: "รอตรวจรับ", en: "Verification" }, defaultAccess: user => user?.role === "admin" || user?.isCoAdmin || user?.role === "staff" || inferDepartment(user || {}) === "committee" },
  { key: "routineTemplates", view: "routineTemplates", icon: "▤", label: { th: "แม่แบบงานประจำ", en: "Routine Templates" }, defaultAccess: user => user?.role === "admin" || user?.isCoAdmin || inferDepartment(user || {}) === "juristic" },
  { key: "routineCalendar", view: "routineCalendar", icon: "◷", label: { th: "ปฏิทินงานประจำ", en: "Routine Calendar" }, defaultAccess: user => user?.role === "admin" || user?.isCoAdmin || user?.role === "staff" || inferDepartment(user || {}) === "committee" },
  { key: "routineReports", view: "routineReports", icon: "▥", label: { th: "รายงานงานประจำ", en: "Routine Reports" }, defaultAccess: user => user?.role === "admin" || user?.isCoAdmin || ["juristic", "committee", "technician", "housekeeping", "security_staff"].includes(inferDepartment(user || {})) },
  { key: "routineSettings", view: "routineSettings", icon: "⚙", label: { th: "ตั้งค่า Daily PM", en: "Daily PM Settings" }, defaultAccess: user => user?.role === "admin" || user?.isCoAdmin || inferDepartment(user || {}) === "juristic" },
  { key: "pool", view: "pool", icon: "●", label: { th: "Pool งานกลาง", en: "Central Pool" }, defaultAccess: user => user?.role !== "resident" },
  { key: "juristicTeam", view: "juristicTeam", icon: "◫", label: { th: "งานของทีมนิติ", en: "Juristic Team Jobs" }, defaultAccess: user => user?.role !== "resident" },
  { key: "myjobs", view: "myjobs", icon: "✓", label: { th: "งานของฉัน", en: "My Jobs" }, defaultAccess: user => user?.role !== "resident" },
  { key: "technicianJobs", view: "technicianJobs", icon: "⚒", label: { th: "งานช่างอาคาร", en: "Technician Jobs" }, defaultAccess: user => user?.role !== "resident" },
  { key: "housekeepingJobs", view: "housekeepingJobs", icon: "✦", label: { th: "งานแม่บ้าน", en: "Housekeeping Jobs" }, defaultAccess: user => user?.role !== "resident" },
  { key: "roomjobs", view: "roomjobs", icon: "⌂", label: { th: "ห้องของฉัน", en: "My Room" }, defaultAccess: user => user?.role === "resident" },
  { key: "team", view: "team", icon: "♙", label: { th: "ทีมงาน", en: "Team" }, defaultAccess: user => user?.role === "admin" || user?.isCoAdmin },
  { key: "residents", view: "residents", icon: "▤", label: { th: "ลูกบ้าน", en: "Residents" }, defaultAccess: user => user?.role === "admin" || user?.isCoAdmin },
  { key: "organization", view: "organization", icon: "◎", label: { th: "Organization Chart", en: "Organization Chart" }, defaultAccess: user => user?.role === "admin" || user?.isCoAdmin },
  { key: "backhouse", view: "backhouse", icon: "▣", label: { th: "ประกาศ", en: "Announcements" }, defaultAccess: user => user?.role === "admin" || user?.isCoAdmin },
  { key: "staffLogs", view: "staffLogs", icon: "≡", label: { th: "Staff Log", en: "Staff Log" }, defaultAccess: user => user?.role === "admin" || user?.isCoAdmin },
  { key: "permissions", view: "permissions", icon: "⚙", label: { th: "สิทธิ์การใช้งาน", en: "Permissions" }, defaultAccess: user => user?.role === "admin" || user?.isCoAdmin }
];
const permissionActionItems = [
  { key: "assignL1", label: { th: "มอบหมายงาน L1", en: "Assign L1" }, description: { th: "สร้าง/มอบหมายงานให้พนักงานทั่วไปได้", en: "Create and assign jobs to staff" }, defaultAccess: user => !!(user?.role === "admin" || user?.isCoAdmin || user?.assignL1 || user?.canAssign) },
  { key: "assignL2", label: { th: "มอบหมายงาน L2", en: "Assign L2" }, description: { th: "มอบหมายงานให้พนักงานและคณะกรรมการได้", en: "Assign jobs to staff and committee" }, defaultAccess: user => !!(user?.role === "admin" || user?.isCoAdmin || user?.assignL2) },
  { key: "editGoogleFormJob", label: { th: "แก้ไขงานจาก Google Form", en: "Edit Google Form jobs" }, description: { th: "แก้ไขรายละเอียดงานส่วนกลางที่นำเข้าจาก Google Form พร้อมเก็บค่าเดิมลง Log งาน", en: "Edit common-area Google Form job details and keep original values in the job log" }, defaultAccess: user => !!(user?.role === "admin" || user?.isCoAdmin || user?.assignL1 || user?.assignL2 || user?.canAssign) },
  { key: "createJob", label: { th: "สร้างงานใหม่", en: "Create jobs" }, description: { th: "เปิดปุ่มสร้างงานใหม่", en: "Show and allow create job action" }, defaultAccess: user => user?.role === "admin" || user?.isCoAdmin || user?.role === "staff" },
  { key: "manageTeam", label: { th: "แก้ไขทีมงาน", en: "Manage team" }, description: { th: "เพิ่ม/แก้ไขทีมงานและข้อมูลพนักงาน", en: "Add and edit staff records" }, defaultAccess: user => user?.role === "admin" || user?.isCoAdmin },
  { key: "manageResidents", label: { th: "แก้ไขลูกบ้าน", en: "Manage residents" }, description: { th: "เพิ่ม/แก้ไขรายชื่อลูกบ้านและรถ", en: "Add and edit resident records" }, defaultAccess: user => user?.role === "admin" || user?.isCoAdmin },
  { key: "manageAnnouncements", label: { th: "จัดการประกาศ", en: "Manage announcements" }, description: { th: "อัปโหลด/เรียง/ลบประกาศ", en: "Upload, reorder, and remove announcements" }, defaultAccess: user => user?.role === "admin" || user?.isCoAdmin },
  { key: "manageOrganization", label: { th: "จัด Organization Chart", en: "Manage organization chart" }, description: { th: "ลากโปรไฟล์ลง Organization Slots", en: "Assign profiles to organization slots" }, defaultAccess: user => user?.role === "admin" || user?.isCoAdmin },
  { key: "importExport", label: { th: "Import / Export", en: "Import / Export" }, description: { th: "นำเข้าและส่งออก CSV/รายงาน", en: "Import and export CSV/reports" }, defaultAccess: user => user?.role === "admin" || user?.isCoAdmin },
  { key: "viewStaffLog", label: { th: "ดู Staff Log", en: "View Staff Log" }, description: { th: "เปิดดูบันทึกกิจกรรมทีมงาน", en: "View staff activity logs" }, defaultAccess: user => user?.role === "admin" || user?.isCoAdmin },
  { key: "managePermissions", label: { th: "จัดการสิทธิ์", en: "Manage permissions" }, description: { th: "เปิดหน้า Permission Center นี้", en: "Open this Permission Center" }, defaultAccess: user => user?.role === "admin" || user?.isCoAdmin },
  { key: "manageRoutineTemplates", label: { th: "จัดการแม่แบบ Daily PM", en: "Manage Daily PM templates" }, description: { th: "สร้าง แก้ไข Duplicate และเปิด/ปิด Template งานประจำ", en: "Create, edit, duplicate, and enable/disable routine templates" }, defaultAccess: user => user?.role === "admin" || user?.isCoAdmin || inferDepartment(user || {}) === "juristic" },
  { key: "verifyRoutineTasks", label: { th: "ตรวจรับงานประจำ", en: "Verify routine tasks" }, description: { th: "Approve / Reject งานประจำที่ส่งมาตรวจรับ", en: "Approve or reject submitted routine tasks" }, defaultAccess: user => user?.role === "admin" || user?.isCoAdmin || ["juristic", "committee"].includes(inferDepartment(user || {})) || user?.canAssign }
];
const permissionLabel = (item) => item?.label?.[currentLang] || item?.label?.th || item?.key || "";
const permissionDescription = (item) => item?.description?.[currentLang] || item?.description?.th || "";
function userPermissions(user) {
  return user?.permissions && typeof user.permissions === "object" ? user.permissions : {};
}
function permissionDefault(user, type, key) {
  const item = type === "sidebar"
    ? permissionSidebarItems.find(entry => entry.key === key || entry.view === key)
    : permissionActionItems.find(entry => entry.key === key);
  return !!item?.defaultAccess?.(user);
}
function hasPermission(user, type, key) {
  if (!user) return false;
  if (user.role === "admin") return true;
  const scoped = userPermissions(user)[type] || {};
  if (Object.prototype.hasOwnProperty.call(scoped, key)) return !!scoped[key];
  return permissionDefault(user, type, key);
}
function hasSidebarAccess(view, user = currentUser) {
  if (!user) return false;
  if (view === "logs") return user.role === "admin";
  const item = permissionSidebarItems.find(entry => entry.view === view || entry.key === view);
  if (!item) return user.role !== "resident";
  return hasPermission(user, "sidebar", item.key);
}
function hasActionPermission(key, user = currentUser) {
  return hasPermission(user, "actions", key);
}
const canAssign = () => currentUser?.role === "admin" || hasActionPermission("assignL1") || hasActionPermission("assignL2") || currentUser?.canAssign;
const canEditGoogleFormJobDetails = (job, user = currentUser) => !!(
  user &&
  job &&
  (job.source === "Google Form" || job.raw) &&
  inferMainCategory(job) === "common" &&
  (user.role === "admin" || hasPermission(user, "actions", "editGoogleFormJob") || hasPermission(user, "actions", "assignL1") || hasPermission(user, "actions", "assignL2") || user.canAssign)
);
const canManageTeam = () => currentUser?.role === "admin" || hasActionPermission("manageTeam");
const canManageResidents = () => currentUser?.role === "admin" || hasActionPermission("manageResidents");
const canManageAnnouncements = () => currentUser?.role === "admin" || hasActionPermission("manageAnnouncements");
const canManageOrganization = () => currentUser?.role === "admin" || hasActionPermission("manageOrganization");
const canManagePermissions = () => currentUser?.role === "admin" || hasActionPermission("managePermissions");
const canViewResidentWork = () => currentUser?.role !== "resident" && hasSidebarAccess("residentWork");
const canGrantCoAdmin = () => currentUser?.role === "admin";
const categoryLabel = (key) => key ? (categoryMeta[key]?.[currentLang] || categoryMeta[key]?.th || key) : (currentLang === "th" ? "ยังไม่แบ่งประเภท" : "Uncategorized");
const statusLabel = (status, subStatus = "") => statusLabelNew(status, subStatus);
const priorityLabel = (priority) => t(`priority.${priority}`);
const formatDate = (iso) => iso ? new Intl.DateTimeFormat(currentLang === "th" ? "th-TH" : "en-US", { day: "numeric", month: "short", year: "2-digit" }).format(new Date(`${iso}T00:00:00`)) : "-";
function normalizeStatus(status) {
  return legacyStatusMap[status] || status || "open";
}
function statusLabelNew(status, subStatus = "") {
  if (subStatus && subStatusMeta[subStatus]) return subStatusMeta[subStatus][currentLang] || subStatusMeta[subStatus].th;
  const normalized = normalizeStatus(status);
  return statusMeta[normalized]?.[currentLang] || statusMeta[normalized]?.th || t(`status.${status}`) || status || "-";
}
function inferMainCategory(job) {
  if (job.mainCategory) return job.mainCategory;
  const room = String(getText(job.roomNo || job.room || "")).toLowerCase();
  if (job.areaType === "common" || job.sharePublic || room.includes("common") || room.includes("ส่วนกลาง")) return "common";
  return "resident";
}
function generateClosePin() {
  const activePins = new Set(jobs.filter(job => normalizeStatus(job.status) !== "completed" && normalizeStatus(job.status) !== "rejected").map(job => job.closePin).filter(Boolean));
  for (let i = 0; i < 100; i++) {
    const pin = String(Math.floor(Math.random() * 10000)).padStart(4, "0");
    if (!activePins.has(pin)) return pin;
  }
  return String(Date.now()).slice(-4);
}
function normalizeJob(job) {
  if (!job) return null;
  const normalizedStatus = normalizeStatus(job.status);
  const roomText = getText(job.room || job.roomNo || "");
  const titleText = getText(job.title || job.issueDescription || roomText || "Work Order");
  const now = new Date().toISOString();
  job.status = normalizedStatus;
  job.title = job.title || titleText;
  job.jobDate = job.jobDate || job.date || now.slice(0, 10);
  job.startTime = job.startTime || "";
  job.endTime = job.endTime || "";
  job.dueDate = job.dueDate || job.nextUpdateDate || defaultDueDate(job);
  job.roomNo = job.roomNo || roomText;
  job.room = job.room || job.roomNo;
  job.building = job.building || "";
  job.floor = job.floor || "";
  job.contactName = job.contactName || getUserName(getUser(job.reporter)) || "";
  job.contactPhone = job.contactPhone || "";
  job.issueDescription = job.issueDescription || getText(job.description) || getText(job.title) || "";
  job.description = job.description || job.issueDescription;
  job.initialAttachments = job.initialAttachments || job.attachments || [];
  job.attachments = job.attachments || job.initialAttachments || [];
  job.assignedBy = job.assignedBy || job.createdBy || "";
  job.assignedByName = job.assignedByName || getUserName(getUser(job.assignedBy)) || "";
  if (job.proposedAssignee && !job.assignee) {
    job.assignee = job.proposedAssignee;
    delete job.proposedAssignee;
    delete job.assignmentDeadline;
  }
  job.assigneeName = job.assigneeName || getUserName(getUser(job.assignee)) || "";
  job.category = job.category || "building";
  job.mainCategory = inferMainCategory(job);
  job.areaType = job.areaType || (job.mainCategory === "common" ? "common" : "unit");
  if (job.mainCategory === "common") job.sharePublic = true;
  job.subStatus = job.subStatus || "";
  job.note = job.note || "";
  job.closePin = job.closePin || generateClosePin();
  job.timeline = job.timeline || [];
  job.statusUpdates = job.statusUpdates || [];
  job.createdAt = job.createdAt || `${job.date || job.jobDate}T${job.time || "00:00"}:00`;
  job.updatedAt = job.updatedAt || job.createdAt;
  job.completedAt = normalizedStatus === "completed" ? (job.completedAt || now) : (job.completedAt || "");
  job.noPinAvailable = !!job.noPinAvailable;
  job.hasScheduleConflict = !!job.hasScheduleConflict;
  if (!job.timeline.length) job.timeline.push({
    id: `log-${Date.now()}-${Math.random().toString(16).slice(2)}`,
    at: job.createdAt,
    by: job.reporter || job.createdBy || "",
    byName: getUserName(getUser(job.reporter || job.createdBy)) || "System",
    action: "created",
    fromStatus: "",
    toStatus: job.status,
    message: "สร้างงาน / นำเข้าข้อมูลงานเดิม",
    data: {}
  });
  return job;
}
function addJobTimeline(jobId, log) {
  const job = jobs.find(item => item.id === jobId);
  if (!job) return;
  job.timeline = job.timeline || [];
  job.timeline.unshift({
    id: crypto.randomUUID?.() || `log-${Date.now()}`,
    at: new Date().toISOString(),
    by: currentUser?.id || "",
    byName: currentUser ? getUserName(currentUser) : "System",
    action: log.action || "update",
    fromStatus: log.fromStatus || "",
    toStatus: log.toStatus || job.status,
    message: log.message || "",
    data: log.data || {}
  });
  job.updatedAt = new Date().toISOString();
}
function effectiveEndTime(startTime, endTime) {
  if (endTime) return endTime;
  if (!startTime) return "";
  const [h, m] = startTime.split(":").map(Number);
  const date = new Date(2000, 0, 1, h || 0, m || 0);
  date.setHours(date.getHours() + 1);
  return `${String(date.getHours()).padStart(2, "0")}:${String(date.getMinutes()).padStart(2, "0")}`;
}
function timeToMinutes(value) {
  const [h = 0, m = 0] = String(value || "00:00").split(":").map(Number);
  return h * 60 + m;
}
function checkScheduleConflict(assigneeId, jobDate, startTime, endTime, excludeJobId = "") {
  if (!assigneeId || !jobDate || !startTime) return [];
  const end = effectiveEndTime(startTime, endTime);
  const newStart = timeToMinutes(startTime);
  const newEnd = timeToMinutes(end);
  return jobs.map(normalizeJob).filter(job =>
    job.id !== excludeJobId &&
    job.assignee === assigneeId &&
    job.jobDate === jobDate &&
    !["completed", "rejected"].includes(normalizeStatus(job.status)) &&
    job.startTime
  ).filter(job => {
    const oldStart = timeToMinutes(job.startTime);
    const oldEnd = timeToMinutes(effectiveEndTime(job.startTime, job.endTime));
    return newStart < oldEnd && newEnd > oldStart;
  });
}
function canAssignJob(user = currentUser) { return canAssign(); }
function canViewJob(user, job) {
  if (!user) return false;
  if (user.role === "admin") return true;
  if (user.isCoAdmin || user.canAssign || user.assignL1 || user.assignL2) return true;
  if (user.role === "staff") return job.assignee === user.id;
  if (user.role === "resident") return getText(job.roomNo || job.room) === user.room || job.reporter === user.id;
  return false;
}
function canUpdateJob(user, job) {
  if (!user || !job) return false;
  return user.role === "admin" || user.isCoAdmin || job.assignee === user.id || job.assignedBy === user.id;
}
function canSeeClosePin(user, job) {
  if (!user || !job) return false;
  return user.role === "admin" || user.isCoAdmin || job.assignedBy === user.id || job.reporter === user.id || (user.role === "resident" && getText(job.roomNo || job.room) === user.room);
}
function canVerifyCompletion(user, job) {
  if (!user || !job || job.subStatus !== "waiting_owner_or_admin_verification") return false;
  return user.role === "admin" || user.isCoAdmin || job.assignedBy === user.id || (user.role === "resident" && getText(job.roomNo || job.room) === user.room);
}
function createJob(payload) {
  const assignee = payload.assignee ? getUser(payload.assignee) : null;
  const sequence = String(jobs.length + 1).padStart(3, "0");
  const job = normalizeJob({
    id: payload.id || `JC-260626-${sequence}`,
    source: payload.source || "WebApp",
    raw: !payload.assignee,
    title: payload.title || payload.issueDescription,
    jobDate: payload.jobDate,
    startTime: payload.startTime || "",
    endTime: payload.endTime || "",
    dueDate: payload.dueDate || "",
    slaNote: payload.slaNote || "",
    routineTaskId: payload.routineTaskId || "",
    roomNo: payload.roomNo,
    room: payload.roomNo,
    building: payload.building || "",
    floor: payload.floor || "",
    contactName: payload.contactName,
    contactPhone: payload.contactPhone,
    issueDescription: payload.issueDescription,
    description: payload.issueDescription,
    initialAttachments: payload.initialAttachments || [],
    attachments: payload.initialAttachments || [],
    assignedBy: payload.assignee ? currentUser.id : "",
    assignedByName: payload.assignee ? getUserName(currentUser) : "",
    assignee: payload.assignee || null,
    assigneeName: assignee ? getUserName(assignee) : "",
    mainCategory: payload.mainCategory || "resident",
    areaType: payload.mainCategory === "common" ? "common" : "unit",
    sharePublic: payload.mainCategory === "common",
    category: payload.category || "building",
    priority: payload.priority || "normal",
    status: payload.assignee ? "received" : "open",
    subStatus: "",
    note: payload.note || "",
    closePin: generateClosePin(),
    reporter: currentUser.id,
    createdBy: currentUser.id,
    date: payload.jobDate,
    time: payload.startTime || new Date().toLocaleTimeString("th-TH", { hour: "2-digit", minute: "2-digit" }),
    createdAt: new Date().toISOString(),
    updatedAt: new Date().toISOString(),
    hasScheduleConflict: !!payload.hasScheduleConflict,
    conflictConfirmedAt: payload.conflictConfirmedAt || "",
    conflictConfirmedBy: payload.conflictConfirmedBy || ""
  });
  job.timeline = [];
  jobs.unshift(job);
  addJobTimeline(job.id, { action: "created", toStatus: job.status, message: `สร้างงานโดย ${getUserName(currentUser)}` });
  if (assignee) addJobTimeline(job.id, { action: "assigned", toStatus: job.status, message: `มอบหมายให้ ${getUserName(assignee)}` });
  if (job.hasScheduleConflict) addJobTimeline(job.id, { action: "schedule_conflict_confirmed", toStatus: job.status, message: `มอบหมายงานซ้อนช่วงเวลา โดยได้รับการยืนยันจาก ${getUserName(currentUser)}` });
  saveJobs();
  addLog("WEBAPP_JOB_CREATED", `New job ${job.id}${assignee ? ` assigned to ${getUserName(assignee)}` : " sent to Pool"}`);
  return job;
}
function assignJob(jobId, assigneeId, extra = {}) {
  const job = normalizeJob(jobs.find(item => item.id === jobId));
  const assignee = getUser(assigneeId);
  if (!job || !assignee) return null;
  const fromStatus = job.status;
  job.raw = false;
  job.assignee = assigneeId;
  delete job.proposedAssignee;
  delete job.assignmentDeadline;
  job.assigneeName = getUserName(assignee);
  job.assignedBy = currentUser.id;
  job.assignedByName = getUserName(currentUser);
  job.category = extra.category || job.category || "building";
  job.mainCategory = extra.mainCategory || job.mainCategory || inferMainCategory(job);
  job.areaType = job.mainCategory === "common" ? "common" : "unit";
  if (job.mainCategory === "common") job.sharePublic = true;
  job.status = "received";
  job.updatedAt = new Date().toISOString();
  Object.assign(job, extra);
  addJobTimeline(job.id, { action: "assigned", fromStatus, toStatus: job.status, message: `มอบหมายให้ ${getUserName(assignee)} โดย ${getUserName(currentUser)}` });
  if (job.hasScheduleConflict) addJobTimeline(job.id, { action: "schedule_conflict_confirmed", toStatus: job.status, message: `มอบหมายงานซ้อนช่วงเวลา โดยได้รับการยืนยันจาก ${getUserName(currentUser)}` });
  saveJobs();
  addLog("JOB_ASSIGNED", `${job.id} assigned to ${getUserName(assignee)}`);
  return job;
}
function validateStatusUpdate(job, payload) {
  const errors = [];
  const status = payload.status;
  const requiresAttachment = !["received", "rejected"].includes(status);
  if (requiresAttachment && !payload.attachments?.length) errors.push("กรุณาแนบรูปอย่างน้อย 1 รูป");
  if ((payload.attachments?.length || 0) > 3) errors.push("แนบรูปได้สูงสุด 3 รูป");
  if (status === "pending_inspection" && !payload.inspectionDate) errors.push("กรุณาเลือกวันที่เข้าตรวจสอบ");
  if ((status === "repaired_follow_up" || status === "temporary_waiting_parts") && !payload.nextUpdateDate) errors.push("กรุณาเลือกวันที่อัปเดต/ติดตามครั้งถัดไป");
  if (status === "inspected_waiting_repair") {
    if (!payload.updateDate) errors.push("กรุณาเลือกวันที่ตรวจสอบ / วันที่อัปเดต");
    if (!payload.cause) errors.push("กรุณากรอกสาเหตุ");
    if (!payload.solution) errors.push("กรุณากรอกแนวทางแก้ไข");
  }
  if (status === "completed") {
    if (payload.noPinAvailable) {
      if (!payload.noPinReason) errors.push("กรุณากรอกเหตุผลที่ไม่มี PIN");
      if (!payload.attachments?.length) errors.push("กรุณาแนบรูปอย่างน้อย 1 รูปเมื่อไม่มี PIN");
    } else if (payload.closePin !== job.closePin) errors.push("PIN ไม่ถูกต้อง");
  }
  if (payload.attachments?.some(file => file.size && file.size > 5 * 1024 * 1024)) errors.push("รูปภาพต้องมีขนาดไม่เกิน 5MB ต่อไฟล์");
  return errors;
}
function updateJobStatus(jobId, payload) {
  const job = normalizeJob(jobs.find(item => item.id === jobId));
  if (!job) return { errors: ["ไม่พบงาน"] };
  const errors = validateStatusUpdate(job, payload);
  if (errors.length) return { errors };
  const fromStatus = job.status;
  const now = new Date().toISOString();
  if (payload.status === "completed" && payload.noPinAvailable) {
    job.status = "pending_inspection";
    job.subStatus = "waiting_owner_or_admin_verification";
    job.noPinAvailable = true;
    job.noPinReason = payload.noPinReason;
    job.pendingVerificationAt = now;
    job.completedAt = "";
  } else {
    job.status = payload.status;
    job.subStatus = "";
    if (payload.status === "completed") {
      job.completedAt = now;
      job.completedWorkDate = payload.completedWorkDate || now.slice(0, 10);
    }
  }
  if (payload.status === "received") job.acceptedAt = now;
  if (payload.status === "pending_inspection" && payload.inspectionDate) job.inspectionDate = payload.inspectionDate;
  if ((payload.status === "repaired_follow_up" || payload.status === "temporary_waiting_parts") && payload.nextUpdateDate) job.nextUpdateDate = payload.nextUpdateDate;
  if (payload.status === "inspected_waiting_repair") {
    job.inspectionDate = payload.updateDate;
    job.cause = payload.cause;
    job.solution = payload.solution;
  }
  job.note = payload.note || job.note || "";
  if (payload.attachments?.length) job.attachments = [...(job.attachments || []), ...payload.attachments];
  job.statusUpdates = job.statusUpdates || [];
  job.statusUpdates.unshift({ at: now, by: currentUser.id, byName: getUserName(currentUser), ...payload, attachmentsCount: payload.attachments?.length || 0 });
  addJobTimeline(job.id, {
    action: payload.noPinAvailable ? "complete_without_pin" : "status_update",
    fromStatus,
    toStatus: job.status,
    message: payload.noPinAvailable
      ? `ช่างแจ้งว่าแก้ไขงานเสร็จแล้ว แต่ไม่สามารถขอ PIN ได้ เนื่องจาก ${payload.noPinReason}`
      : `เปลี่ยนสถานะเป็น ${statusLabel(job.status)} โดย ${getUserName(currentUser)}${payload.cause ? ` สาเหตุ: ${payload.cause} แนวทางแก้ไข: ${payload.solution}` : ""}`,
    data: { subStatus: job.subStatus, noPinAvailable: job.noPinAvailable, attachmentsCount: payload.attachments?.length || 0 }
  });
  saveJobs();
  addLog("JOB_UPDATED", `${job.id} updated to ${statusLabel(job.status, job.subStatus)}`);
  return { job, errors: [] };
}
function verifyCompletion(jobId) {
  const job = normalizeJob(jobs.find(item => item.id === jobId));
  if (!canVerifyCompletion(currentUser, job)) return false;
  const fromStatus = job.status;
  job.status = "completed";
  job.subStatus = "";
  job.completedAt = new Date().toISOString();
  job.completedWorkDate = job.completedWorkDate || job.completedAt.slice(0, 10);
  addJobTimeline(job.id, { action: "verified_completed", fromStatus, toStatus: "completed", message: `ยืนยันปิดงานโดย ${getUserName(currentUser)}` });
  saveJobs();
  addLog("JOB_COMPLETION_VERIFIED", `${job.id} verified completed by ${getUserName(currentUser)}`);
  return true;
}
function getDashboardCounts(scopeJobs = jobs.filter(job => canViewJob(currentUser, normalizeJob(job)))) {
  const normalized = scopeJobs.map(normalizeJob).filter(Boolean);
  return {
    open: normalized.filter(job => normalizeStatus(job.status) === "open").length,
    received: normalized.filter(job => normalizeStatus(job.status) === "received").length,
    pendingInspection: normalized.filter(job => normalizeStatus(job.status) === "pending_inspection" && job.subStatus !== "waiting_owner_or_admin_verification").length,
    inspectedWaitingRepair: normalized.filter(job => normalizeStatus(job.status) === "inspected_waiting_repair").length,
    repairedFollowUp: normalized.filter(job => normalizeStatus(job.status) === "repaired_follow_up").length,
    temporaryWaitingParts: normalized.filter(job => normalizeStatus(job.status) === "temporary_waiting_parts").length,
    waitingVerification: normalized.filter(job => job.subStatus === "waiting_owner_or_admin_verification").length,
    completed: normalized.filter(job => normalizeStatus(job.status) === "completed" && job.completedAt).length,
    rejected: normalized.filter(job => normalizeStatus(job.status) === "rejected").length,
    conflict: normalized.filter(job => job.hasScheduleConflict).length,
    noPin: normalized.filter(job => job.noPinAvailable).length
  };
}
function todayIso() {
  return new Date().toISOString().slice(0, 10);
}
function addDaysIso(days) {
  const date = new Date();
  date.setDate(date.getDate() + days);
  return date.toISOString().slice(0, 10);
}
function addDaysFromIso(iso, days) {
  const date = iso ? new Date(`${iso}T00:00:00`) : new Date();
  date.setDate(date.getDate() + days);
  return date.toISOString().slice(0, 10);
}
function defaultDueDate(job = {}) {
  const base = job.jobDate || job.date || todayIso();
  const days = job.priority === "urgent" ? 1 : job.priority === "high" ? 3 : 7;
  return addDaysFromIso(base, days);
}
function dueInfo(job = {}) {
  job = normalizeJob(job);
  if (!job.dueDate || ["completed", "rejected"].includes(normalizeStatus(job.status))) {
    return { state: "done", label: currentLang === "th" ? "ไม่มี SLA ค้าง" : "No active SLA", days: 0 };
  }
  const today = todayIso();
  const due = new Date(`${job.dueDate}T00:00:00`);
  const now = new Date(`${today}T00:00:00`);
  const days = Math.round((due - now) / 86400000);
  if (days < 0) return { state: "overdue", days, label: currentLang === "th" ? `เกินกำหนด ${Math.abs(days)} วัน` : `${Math.abs(days)} days overdue` };
  if (days === 0) return { state: "today", days, label: currentLang === "th" ? "ครบกำหนดวันนี้" : "Due today" };
  if (days <= 2) return { state: "soon", days, label: currentLang === "th" ? `เหลือ ${days} วัน` : `${days} days left` };
  return { state: "normal", days, label: currentLang === "th" ? `เหลือ ${days} วัน` : `${days} days left` };
}
function isJobInDashboardRange(job) {
  job = normalizeJob(job);
  const jobDate = job.jobDate || job.date || job.createdAt?.slice(0, 10) || todayIso();
  const today = todayIso();
  if (dashboardRange === "today") return jobDate === today;
  const days = Number(dashboardRange) || 7;
  return jobDate >= addDaysIso(-(days - 1)) && jobDate <= today;
}
function getDashboardRangeJobs(view = "dashboard") {
  return getViewJobs(view).filter(isJobInDashboardRange);
}
function dashboardRangeLabel() {
  if (dashboardRange === "today") return currentLang === "th" ? "วันนี้" : "today";
  return currentLang === "th" ? `${dashboardRange} วัน` : `${dashboardRange} days`;
}
function getNotifications() {
  if (!currentUser) return [];
  const today = todayIso();
  return jobs.map(normalizeJob).filter(job => canViewJob(currentUser, job)).flatMap(job => {
    const items = [];
    const due = dueInfo(job);
    if (due.state === "overdue") items.push({ type: "overdue", job, label: due.label });
    else if (due.state === "today" || due.state === "soon") items.push({ type: "due", job, label: due.label });
    if (job.raw || (!job.assignee && normalizeStatus(job.status) === "open")) items.push({ type: "pool", job, label: currentLang === "th" ? "งานใหม่รอมอบหมาย" : "New job waiting assignment" });
    if (job.nextUpdateDate && job.nextUpdateDate <= today && !["completed", "rejected"].includes(normalizeStatus(job.status))) items.push({ type: "follow", job, label: currentLang === "th" ? "ถึงกำหนดติดตาม/อัปเดต" : "Follow-up due" });
    if (job.hasScheduleConflict) items.push({ type: "conflict", job, label: currentLang === "th" ? "มีเวลางานซ้อน" : "Schedule conflict" });
    if (job.subStatus === "waiting_owner_or_admin_verification") items.push({ type: "verify", job, label: currentLang === "th" ? "รอตรวจสอบยืนยันปิดงาน" : "Waiting close verification" });
    return items;
  }).slice(0, 20);
}
function renderNotifications() {
  const notifications = getNotifications();
  $("#notificationCount").textContent = notifications.length;
  $(".notification-btn")?.classList.toggle("has-alert", notifications.length > 0);
  const panel = $("#notificationPanel");
  if (!panel) return;
  panel.innerHTML = `<div class="notification-head"><strong>${currentLang === "th" ? "การแจ้งเตือน" : "Notifications"}</strong><small>${notifications.length} ${currentLang === "th" ? "รายการ" : "items"}</small></div>` +
    (notifications.length ? notifications.map(item => `<button type="button" class="notification-item" data-job-id="${item.job.id}">
      <span class="notification-type">${esc(item.label)}</span>
      <strong>${esc(getText(item.job.title))}</strong>
      <small>${esc(item.job.id)} · ${formatDate(item.job.jobDate)} · ${statusLabel(item.job.status, item.job.subStatus)}</small>
    </button>`).join("") : `<div class="notification-empty">${currentLang === "th" ? "ยังไม่มีรายการที่ต้องติดตาม" : "No alerts right now"}</div>`);
}
function getReportCounts(scopeJobs = jobs) {
  const counts = getDashboardCounts(scopeJobs);
  return {
    total: scopeJobs.length,
    open: counts.open,
    inProgress: counts.received + counts.pendingInspection + counts.inspectedWaitingRepair + counts.repairedFollowUp + counts.temporaryWaitingParts,
    waitingVerification: counts.waitingVerification,
    realCompleted: counts.completed,
    rejected: counts.rejected
  };
}
function inferDepartment(user) {
  if (user.department) return user.department;
  if (user.role === "resident") return "resident";
  if (user.team === "cleaning") return "housekeeping";
  if (user.team === "security") return "security_staff";
  if (user.team === "garden") return "gardener";
  if (user.team) return "technician";
  return user.canAssign ? "juristic" : "juristic";
}

function saveJobs() { localStorage.setItem("juristicJobsV2", JSON.stringify(jobs)); queueRemoteSync(); }
function saveLogs() { localStorage.setItem("juristicAdminLogs", JSON.stringify(adminLogs)); queueRemoteSync(); }
function saveStaffLogs() { localStorage.setItem("juristicStaffLogs", JSON.stringify(staffLogs)); queueRemoteSync(); }
function saveResidentRooms() { localStorage.setItem("juristicResidentRooms", JSON.stringify(residentRooms)); queueRemoteSync(); }
function saveTeamOverrides() { localStorage.setItem("juristicTeamOverrides", JSON.stringify(teamOverrides)); }
function saveChatThreads() { localStorage.setItem("juristicChatThreads", JSON.stringify(chatThreads)); queueRemoteSync(); }
function saveRoutineData() {
  localStorage.setItem("juristicRoutineTemplates", JSON.stringify(routineTemplates));
  localStorage.setItem("juristicRoutineTasks", JSON.stringify(routineTasks));
  localStorage.setItem("juristicRoutineTaskResults", JSON.stringify(routineTaskResults));
  localStorage.setItem("juristicRoutineTaskAttachments", JSON.stringify(routineTaskAttachments));
  localStorage.setItem("juristicRoutineTaskLogs", JSON.stringify(routineTaskLogs));
  queueRemoteSync();
}
function saveCustomUsers() { localStorage.setItem("juristicCustomUsers", JSON.stringify(customUsers)); }
function saveDeletedUsers() { localStorage.setItem("juristicDeletedUsers", JSON.stringify(deletedUserIds)); }
function saveBackhouseAnnouncements() { localStorage.setItem("juristicBackhouseAnnouncements", JSON.stringify(announcements)); }
function saveOrganizationAssignments() { localStorage.setItem("juristicOrganizationAssignments", JSON.stringify(organizationAssignments)); }
function isPdfAnnouncement(item) {
  const firstPage = item?.pages?.[0] || "";
  return item?.type === "pdf" || firstPage.startsWith("data:application/pdf") || firstPage.includes(".pdf");
}
function isRealPdfSource(src = "") {
  return String(src).startsWith("data:application/pdf") || /\.pdf($|[?#])/i.test(String(src));
}
function isLegacyPdfPlaceholder(item, src = "") {
  return isPdfAnnouncement(item) && !isRealPdfSource(src);
}
function announcementTypeLabel(item) {
  if (isPdfAnnouncement(item)) return "PDF";
  if (item?.type === "document") return "DOC";
  return "IMG";
}
function announcementFirstPage(item) {
  return item?.pages?.[0] || photo("ANN", "#0f766e");
}
function announcementPreviewMarkup(item, className = "announcement-preview") {
  const src = announcementFirstPage(item);
  if (isPdfAnnouncement(item) && isRealPdfSource(src)) {
    const pdfSrc = `${src}#page=1&toolbar=0&navpanes=0&scrollbar=0`;
    return `<object class="${className} pdf-preview" data="${pdfSrc}" type="application/pdf" aria-label="${esc(getText(item.title))}">
      <a href="${src}" target="_blank" rel="noreferrer">${currentLang === "th" ? "เปิดไฟล์ PDF" : "Open PDF"}</a>
    </object>`;
  }
  if (isLegacyPdfPlaceholder(item, src)) {
    return `<div class="${className} announcement-pdf-missing">
      <strong>PDF</strong>
      <span>${currentLang === "th" ? "ไฟล์นี้ถูกอัปโหลดก่อนระบบเก็บต้นฉบับ PDF กรุณาอัปโหลดไฟล์ใหม่เพื่อแสดงหน้าแรกและดาวน์โหลด" : "This file was uploaded before original PDF storage was enabled. Please re-upload it to preview and download."}</span>
    </div>`;
  }
  return `<img class="${className}" src="${src}" alt="${esc(getText(item.title))}">`;
}
function dataUrlToBlob(src = "") {
  const match = String(src).match(/^data:([^;,]+)?(;base64)?,(.*)$/);
  if (!match) return null;
  const mime = match[1] || "application/octet-stream";
  const isBase64 = Boolean(match[2]);
  const data = match[3] || "";
  const binary = isBase64 ? atob(data) : decodeURIComponent(data);
  const bytes = new Uint8Array(binary.length);
  for (let i = 0; i < binary.length; i += 1) bytes[i] = binary.charCodeAt(i);
  return new Blob([bytes], { type: mime });
}
function fileExtensionFromDataUrl(src = "") {
  const mime = String(src).match(/^data:([^;,]+)/)?.[1] || "";
  if (!mime && /\.pdf($|[?#])/i.test(String(src))) return "pdf";
  if (mime.includes("pdf")) return "pdf";
  if (mime.includes("png")) return "png";
  if (mime.includes("jpeg") || mime.includes("jpg")) return "jpg";
  if (mime.includes("webp")) return "webp";
  if (mime.includes("svg")) return "svg";
  return "file";
}
function safeFileName(name = "announcement") {
  return String(name || "announcement").trim().replace(/[\\/:*?"<>|]+/g, "-").replace(/\s+/g, "-").slice(0, 80) || "announcement";
}
function revokeActiveAnnouncementDownloadUrl() {
  if (!activeAnnouncementDownloadUrl) return;
  URL.revokeObjectURL(activeAnnouncementDownloadUrl);
  activeAnnouncementDownloadUrl = "";
}
function announcementDownloadInfo(item, page) {
  const src = item?.pages?.[page] || announcementFirstPage(item);
  if (!src) return null;
  if (isLegacyPdfPlaceholder(item, src)) {
    revokeActiveAnnouncementDownloadUrl();
    return { legacy: true, href: "#", filename: "" };
  }
  const blob = String(src).startsWith("data:") ? dataUrlToBlob(src) : null;
  if (blob) {
    const href = URL.createObjectURL(blob);
    revokeActiveAnnouncementDownloadUrl();
    activeAnnouncementDownloadUrl = href;
    return {
      legacy: false,
      href,
      filename: `${safeFileName(getText(item.title))}-page-${page + 1}.${fileExtensionFromDataUrl(src)}`
    };
  }
  revokeActiveAnnouncementDownloadUrl();
  return {
    legacy: false,
    href: src,
    filename: `${safeFileName(getText(item.title))}-page-${page + 1}.${fileExtensionFromDataUrl(src)}`
  };
}
function downloadActiveAnnouncementPage() {
  const item = announcements[activeAnnouncementIndex];
  if (!item) return;
  const src = item.pages?.[activeAnnouncementPage] || announcementFirstPage(item);
  if (!src) return;
  if (isLegacyPdfPlaceholder(item, src)) {
    showToast(currentLang === "th" ? "ไฟล์ PDF เดิมไม่ได้เก็บต้นฉบับไว้ กรุณาอัปโหลดใหม่" : "Original PDF was not stored. Please re-upload it.");
    return;
  }
  const link = $("#announcementDownloadLink");
  if (link?.href && link.getAttribute("href") !== "#") link.click();
}
function syncOrganizationDraftFromSaved() {
  organizationDraftAssignments = { ...organizationAssignments };
  organizationDirty = false;
}
function saveOrganizationDraft() {
  organizationAssignments = { ...organizationDraftAssignments };
  saveOrganizationAssignments();
  organizationDirty = false;
}
function canImportFinance() { return currentUser?.role === "admin" || hasActionPermission("importExport") || currentUser?.role === "staff" || inferDepartment(currentUser) === "committee"; }
function downloadTextFile(filename, content, mime = "text/plain;charset=utf-8") {
  const blob = new Blob([content], { type: mime });
  const a = document.createElement("a");
  a.href = URL.createObjectURL(blob);
  a.download = filename;
  document.body.appendChild(a);
  a.click();
  URL.revokeObjectURL(a.href);
  a.remove();
}
function jobTimelineRows(job) {
  job = normalizeJob(job);
  const timelineRows = (job.timeline || []).map(log => ({
    at: log.at,
    by: log.byName || "",
    action: log.action || "",
    status: statusLabel(log.toStatus || job.status, log.data?.subStatus || ""),
    message: log.message || "",
    attachments: []
  }));
  const updateRows = (job.statusUpdates || []).map(update => ({
    at: update.at,
    by: update.byName || "",
    action: "status_update",
    status: statusLabel(update.status || job.status, update.subStatus || ""),
    message: [update.note, update.cause, update.solution, update.noPinReason].filter(Boolean).join(" · "),
    attachments: update.attachments || []
  }));
  const initialRows = (job.initialAttachments || []).length ? [{
    at: job.createdAt,
    by: job.contactName || getUserName(getUser(job.reporter)) || "Reporter",
    action: "initial_photos",
    status: statusLabel(job.status, job.subStatus),
    message: currentLang === "th" ? "รูปภาพตอนเปิดงาน" : "Initial report photos",
    attachments: job.initialAttachments || []
  }] : [];
  return [...initialRows, ...updateRows, ...timelineRows].sort((a, b) => new Date(a.at) - new Date(b.at));
}
function exportJobCsv(jobId) {
  const job = normalizeJob(jobs.find(item => item.id === jobId));
  if (!job) return;
  const assignee = getUser(job.assignee);
  const reporter = getUser(job.reporter);
  const due = dueInfo(job);
  const headers = ["job_id", "title", "source", "status", "main_category", "sub_category", "priority", "room", "building", "floor", "contact_name", "contact_phone", "reporter", "assignee", "assigned_by", "job_date", "due_date", "due_status", "next_update_date", "description", "timeline_at", "timeline_by", "timeline_action", "timeline_status", "timeline_message", "attachment_count", "attachment_refs"];
  const base = [
    job.id, getText(job.title), job.source || "", statusLabel(job.status, job.subStatus), mainCategoryLabel(job.mainCategory),
    categoryLabel(job.category), priorityLabel(job.priority), job.roomNo || getText(job.room), job.building || "", job.floor || "",
    job.contactName || "", job.contactPhone || "", reporter ? getUserName(reporter) : "", assignee ? getUserName(assignee) : "",
    job.assignedByName || getUserName(getUser(job.assignedBy)) || "", job.jobDate || "", job.dueDate || "", due.label,
    job.nextUpdateDate || "", job.issueDescription || getText(job.description) || ""
  ];
  const rows = jobTimelineRows(job).map((row, rowIndex) => [
    ...base,
    row.at || "", row.by || "", row.action || "", row.status || "", row.message || "",
    row.attachments?.length || 0,
    (row.attachments || []).map((_, index) => `timeline_${rowIndex + 1}_image_${index + 1}`).join(";")
  ]);
  const csv = [headers, ...(rows.length ? rows : [[...base, "", "", "", "", "", 0, ""]])].map(csvLine).join("\n");
  downloadTextFile(`${job.id}-tracking-report.csv`, "\ufeff" + csv, "text/csv;charset=utf-8");
  addLog("JOB_EXPORTED_CSV", `${job.id} exported to CSV by ${getUserName(currentUser)}`);
}
function printJobPdf(jobId) {
  const job = normalizeJob(jobs.find(item => item.id === jobId));
  if (!job) return;
  const assignee = getUser(job.assignee);
  const reporter = getUser(job.reporter);
  const due = dueInfo(job);
  const rows = jobTimelineRows(job);
  const imagesHtml = (attachments = []) => attachments.length
    ? `<div class="pdf-images">${attachments.map((src, index) => `<figure><img src="${src}" alt="attachment ${index + 1}"><figcaption>${currentLang === "th" ? "รูปที่" : "Image"} ${index + 1}</figcaption></figure>`).join("")}</div>`
    : "";
  const timelineHtml = rows.map(row => `<section class="pdf-timeline-item">
    <div><time>${new Date(row.at).toLocaleString(currentLang === "th" ? "th-TH" : "en-US")}</time><strong>${esc(row.status || row.action)}</strong><small>${esc(row.by || "")}</small></div>
    <p>${esc(row.message || "-")}</p>
    ${imagesHtml(row.attachments)}
  </section>`).join("") || `<p>${currentLang === "th" ? "ยังไม่มี Timeline" : "No timeline yet"}</p>`;
  const html = `<!doctype html><html><head><meta charset="utf-8"><title>${esc(job.id)} Report</title>
    <style>
      body{font-family:Arial,'Noto Sans Thai',sans-serif;color:#17372e;margin:28px;background:#fff}
      h1{margin:0 0 4px;font-size:26px}.muted{color:#66756f}.badge{display:inline-block;border-radius:999px;padding:6px 10px;background:#e8f4ef;color:#0d6f5a;font-weight:700}
      .grid{display:grid;grid-template-columns:repeat(2,1fr);gap:10px;margin:18px 0}.field{border:1px solid #dce8e2;border-radius:12px;padding:10px}.field span{display:block;color:#77847f;font-size:12px}.field strong{display:block;margin-top:4px}
      .pdf-timeline-item{break-inside:avoid;border-left:4px solid #0d7c66;padding:10px 0 12px 14px;margin:12px 0}.pdf-timeline-item time{display:block;color:#77847f;font-size:12px}.pdf-timeline-item small{display:block;color:#77847f}
      .pdf-images{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin-top:10px}.pdf-images figure{margin:0;border:1px solid #dce8e2;border-radius:12px;padding:8px}.pdf-images img{width:100%;max-height:210px;object-fit:cover;border-radius:8px}.pdf-images figcaption{font-size:11px;color:#77847f;margin-top:5px}
      @media print{button{display:none}body{margin:16px}.pdf-images{grid-template-columns:repeat(2,1fr)}}
    </style></head><body>
    <button onclick="window.print()" style="float:right;padding:10px 14px;border-radius:10px;border:0;background:#0d7c66;color:white">Save / Print PDF</button>
    <h1>${esc(getText(job.title))}</h1><div class="muted">${esc(job.id)} · ${formatDate(job.jobDate)} · ${esc(job.source || "WebApp")}</div>
    <p><span class="badge">${esc(statusLabel(job.status, job.subStatus))}</span> <span class="badge">${esc(due.label)}</span></p>
    <div class="grid">
      <div class="field"><span>Room / Area</span><strong>${esc(job.roomNo || getText(job.room) || "-")}</strong></div>
      <div class="field"><span>Category</span><strong>${esc(mainCategoryLabel(job.mainCategory))} / ${esc(categoryLabel(job.category))}</strong></div>
      <div class="field"><span>Reporter</span><strong>${esc(reporter ? getUserName(reporter) : job.contactName || "-")}</strong></div>
      <div class="field"><span>Assignee</span><strong>${esc(assignee ? getUserName(assignee) : "-")}</strong></div>
      <div class="field"><span>Due date</span><strong>${job.dueDate ? esc(formatDate(job.dueDate)) : "-"}</strong></div>
      <div class="field"><span>Next update</span><strong>${job.nextUpdateDate ? esc(formatDate(job.nextUpdateDate)) : "-"}</strong></div>
    </div>
    <h2>Issue detail</h2><p>${esc(job.issueDescription || getText(job.description) || "-")}</p>
    <h2>Timeline & Attachments</h2>${timelineHtml}
    <script>setTimeout(()=>window.print(),500)</script></body></html>`;
  const win = window.open("", "_blank");
  if (!win) return showToast(currentLang === "th" ? "กรุณาอนุญาต popup เพื่อ Export PDF" : "Please allow popups to export PDF");
  win.document.open();
  win.document.write(html);
  win.document.close();
  addLog("JOB_EXPORTED_PDF", `${job.id} exported to PDF by ${getUserName(currentUser)}`);
}
function exportFinanceReportCsv() {
  const rows = [
    ["category", "this_month", "this_year", "status"],
    ["Common fee", "742000", "6810000", "Normal"],
    ["Maintenance", "186500", "1940000", "Watch"],
    ["Utilities", "214300", "2320000", "Normal"],
    ["Contractor/Service", "98000", "1020000", "Normal"]
  ];
  downloadTextFile("juristic-finance-report.csv", rows.map(row => row.map(value => `"${String(value).replaceAll('"', '""')}"`).join(",")).join("\n"), "text/csv;charset=utf-8");
  addLog("FINANCE_EXPORTED", `Finance report exported by ${getUserName(currentUser)}`);
  showToast(currentLang === "th" ? "Export งบการเงินแล้ว" : "Finance report exported");
}
function remoteSnapshot() {
  return {
    jobs,
    adminLogs,
    staffLogs,
    residentRooms,
    chatThreads,
    routineTemplates,
    routineTasks,
    routineTaskResults,
    routineTaskAttachments,
    routineTaskLogs,
    teamOverrides,
    customUsers,
    deletedUserIds,
    updatedAt: new Date().toISOString()
  };
}
async function remoteRequest(action, payload = {}) {
  if (!remoteSyncReady) return null;
  const response = await fetch(remoteConfig.APPS_SCRIPT_URL, {
    method: "POST",
    headers: { "Content-Type": "text/plain;charset=utf-8" },
    body: JSON.stringify({ action, payload })
  });
  const text = await response.text();
  return text ? JSON.parse(text) : null;
}
async function loadRemoteSnapshot() {
  if (!remoteSyncReady) return;
  try {
    remoteSyncBusy = true;
    const data = await remoteRequest("getSnapshot");
    const snapshot = data?.snapshot || {};
    if (Array.isArray(snapshot.jobs) && snapshot.jobs.length) {
      jobs = snapshot.jobs.map(normalizeJob);
      localStorage.setItem("juristicJobsV2", JSON.stringify(jobs));
    }
    if (Array.isArray(snapshot.adminLogs)) {
      adminLogs = snapshot.adminLogs;
      localStorage.setItem("juristicAdminLogs", JSON.stringify(adminLogs));
    }
    if (Array.isArray(snapshot.staffLogs)) {
      staffLogs = snapshot.staffLogs;
      localStorage.setItem("juristicStaffLogs", JSON.stringify(staffLogs));
    }
    if (Array.isArray(snapshot.residentRooms)) {
      residentRooms = snapshot.residentRooms;
      localStorage.setItem("juristicResidentRooms", JSON.stringify(residentRooms));
    }
    if (Array.isArray(snapshot.chatThreads)) {
      chatThreads = snapshot.chatThreads;
      localStorage.setItem("juristicChatThreads", JSON.stringify(chatThreads));
    }
    if (Array.isArray(snapshot.routineTemplates)) routineTemplates = snapshot.routineTemplates;
    if (Array.isArray(snapshot.routineTasks)) routineTasks = snapshot.routineTasks;
    if (Array.isArray(snapshot.routineTaskResults)) routineTaskResults = snapshot.routineTaskResults;
    if (Array.isArray(snapshot.routineTaskAttachments)) routineTaskAttachments = snapshot.routineTaskAttachments;
    if (Array.isArray(snapshot.routineTaskLogs)) routineTaskLogs = snapshot.routineTaskLogs;
    saveRoutineData();
    if (snapshot.teamOverrides && typeof snapshot.teamOverrides === "object") {
      teamOverrides = snapshot.teamOverrides;
      localStorage.setItem("juristicTeamOverrides", JSON.stringify(teamOverrides));
      applyTeamOverrides();
    }
    renderAll();
  } catch (error) {
    console.warn("Remote sync load failed", error);
  } finally {
    remoteSyncBusy = false;
  }
}
function queueRemoteSync() {
  if (!remoteSyncReady || remoteSyncBusy) return;
  window.clearTimeout(queueRemoteSync.timer);
  queueRemoteSync.timer = window.setTimeout(async () => {
    try {
      await remoteRequest("saveSnapshot", remoteSnapshot());
    } catch (error) {
      console.warn("Remote sync save failed", error);
    }
  }, 600);
}
function applyTeamOverrides() {
  users.forEach(user => {
    const override = teamOverrides[user.id];
    if (!override) return;
    Object.assign(user, override);
  });
}
function splitFullName(user) {
  const parts = (user.name || "").trim().split(/\s+/);
  return {
    firstName: user.firstName || parts[0] || "",
    lastName: user.lastName || parts.slice(1).join(" ") || ""
  };
}
function fullName(firstName, lastName) {
  return [firstName, lastName].filter(Boolean).join(" ").trim();
}
function profileMarkup(user) {
  return user.profileImage
    ? `<img class="profile-avatar" src="${user.profileImage}" alt="${getUserName(user)}">`
    : `<span class="avatar">${initials(getUserName(user))}</span>`;
}
function normalizeSearchText(text) {
  return String(text || "").trim().toLowerCase();
}
function esc(value) {
  return String(value ?? "").replace(/[&<>"']/g, char => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" }[char]));
}
function matchesTeamFilters(user) {
  const q = normalizeSearchText(teamSearch);
  const dept = inferDepartment(user);
  const parts = splitFullName(user);
  const matchesQuery = !q ||
    normalizeSearchText(parts.firstName).startsWith(q) ||
    normalizeSearchText(parts.lastName).startsWith(q) ||
    normalizeSearchText(user.nickName).startsWith(q);
  const matchesDept = teamDepartmentFilter === "all" || dept === teamDepartmentFilter;
  return matchesQuery && matchesDept;
}
function assigneeCandidates() {
  let candidates = activeUsers().filter(user => user.role !== "resident");
  if (currentUser?.role === "admin" || currentUser?.isCoAdmin || currentUser?.assignL2) return candidates;
  if (currentUser?.assignL1) return candidates.filter(user => user.department !== "committee");
  if (currentUser?.canAssign) return candidates.filter(user => user.department !== "committee");
  return [];
}
function userJobRows(userId) {
  return jobs.filter(job =>
    job.assignee === userId ||
    job.reporter === userId ||
    job.createdBy === userId ||
    job.assignedBy === userId
  );
}
function exportUserJobsCsv(user) {
  const rows = userJobRows(user.id);
  const headers = ["job_id", "title", "room", "source", "status", "category", "priority", "assignee", "reporter", "date", "time", "description"];
  const csv = [headers, ...rows.map(job => [
    job.id,
    getText(job.title),
    getText(job.room),
    job.source,
    job.status,
    job.category || "",
    job.priority || "",
    job.assignee || "",
    job.reporter || "",
    job.date || "",
    job.time || "",
    getText(job.description)
  ])].map(row => row.map(cell => `"${String(cell ?? "").replaceAll('"', '""')}"`).join(",")).join("\n");
  const blob = new Blob(["\ufeff" + csv], { type: "text/csv;charset=utf-8" });
  const url = URL.createObjectURL(blob);
  const a = document.createElement("a");
  a.href = url;
  a.download = `${user.id}-job-log.csv`;
  a.click();
  URL.revokeObjectURL(url);
}
function exportEmployeesCsv() {
  const headers = ["id", "password", "first_name", "last_name", "nickname", "position", "department", "department_key", "role", "co_admin", "assign_l1", "assign_l2", "room_or_staff_code"];
  const csv = [headers, ...activeUsers().map(user => {
    const parts = splitFullName(user);
    const departmentKey = inferDepartment(user);
    return [
      user.id,
      currentUser?.role === "admin" ? (user.password || "") : "",
      user.firstName || parts.firstName,
      user.lastName || parts.lastName,
      user.nickName || "",
      user.position || "",
      departmentLabel(departmentKey),
      departmentKey,
      roleLabel(user),
      user.isCoAdmin ? "yes" : "no",
      user.assignL1 ? "yes" : "no",
      user.assignL2 ? "yes" : "no",
      user.room || ""
    ];
  })].map(row => row.map(cell => `"${String(cell ?? "").replaceAll('"', '""')}"`).join(",")).join("\n");
  const blob = new Blob(["\ufeff" + csv], { type: "text/csv;charset=utf-8" });
  const url = URL.createObjectURL(blob);
  const a = document.createElement("a");
  a.href = url;
  a.download = "employees-export.csv";
  a.click();
  URL.revokeObjectURL(url);
  addLog("EMPLOYEES_EXPORTED", `Employees CSV exported by ${getUserName(currentUser)}`);
}
function truthyCsv(value) {
  return ["yes", "true", "1", "on", "y"].includes(String(value || "").trim().toLowerCase());
}
function departmentKeyFromCsv(value) {
  const raw = String(value || "").trim();
  const normalized = raw.toLowerCase();
  const matched = departments.find(dept =>
    dept.key === raw ||
    dept.en.toLowerCase() === normalized ||
    dept.th === raw
  );
  return matched?.key || raw || "juristic";
}
function roleFromDepartment(department) {
  return ["committee", "juristic", "technician", "housekeeping", "security_staff", "gardener", "contractor"].includes(department) ? "staff" : "resident";
}
async function importEmployeesCsv(file) {
  if (!canManageTeam()) return;
  const text = await file.text();
  const rows = parseCsv(text.replace(/^\ufeff/, ""));
  const headers = rows.shift()?.map(header => header.trim()) || [];
  if (!headers.includes("id") && !headers.includes("room_or_staff_code")) return showToast("CSV ต้องมีคอลัมน์ id หรือ room_or_staff_code");
  let added = 0, updated = 0;
  rows.forEach((row, index) => {
    const record = Object.fromEntries(headers.map((header, i) => [header, row[i] || ""]));
    const id = (record.id || "").trim() || `u${Date.now()}${index}`;
    const room = (record.room_or_staff_code || record.room || id).trim().toUpperCase();
    const existing = users.find(user => user.id === id || String(user.room || "").toUpperCase() === room);
    const department = departmentKeyFromCsv(record.department_key || record.department);
    const next = {
      id: existing?.id || id,
      password: currentUser.role === "admin" ? (record.password || existing?.password || "1234") : (existing?.password || "1234"),
      firstName: (record.first_name || existing?.firstName || "").trim(),
      lastName: (record.last_name || existing?.lastName || "").trim(),
      nickName: (record.nickname || existing?.nickName || "").trim(),
      position: (record.position || existing?.position || "").trim(),
      department,
      room,
      role: existing?.role === "admin" ? "admin" : roleFromDepartment(department),
      roleKey: departmentToRoleKey(department),
      isCoAdmin: currentUser.role === "admin" ? truthyCsv(record.co_admin) : !!existing?.isCoAdmin,
      assignL1: truthyCsv(record.assign_l1),
      assignL2: truthyCsv(record.assign_l2)
    };
    next.name = fullName(next.firstName, next.lastName) || existing?.name || next.room;
    next.enName = next.name;
    if (existing) {
      if (existing.role === "admin") return;
      Object.assign(existing, next);
      teamOverrides[existing.id] = { ...(teamOverrides[existing.id] || {}), ...next };
      deletedUserIds = deletedUserIds.filter(userId => userId !== existing.id);
      updated++;
    } else {
      users.push(next);
      customUsers.push(next);
      teamOverrides[next.id] = { ...next };
      deletedUserIds = deletedUserIds.filter(userId => userId !== next.id);
      added++;
    }
  });
  saveCustomUsers();
  saveTeamOverrides();
  saveDeletedUsers();
  addLog("EMPLOYEES_IMPORTED", `${added} added, ${updated} updated by ${getUserName(currentUser)}`);
  renderAll();
  showToast(`Import ทีมงานแล้ว: เพิ่ม ${added} / แก้ไข ${updated}`);
}
function csvCell(value) {
  return `"${String(value ?? "").replaceAll('"', '""')}"`;
}
function csvLine(values) {
  return values.map(csvCell).join(",");
}
function parseCsv(text) {
  const rows = [];
  let row = [], cell = "", quoted = false;
  for (let i = 0; i < text.length; i++) {
    const char = text[i];
    const next = text[i + 1];
    if (quoted) {
      if (char === '"' && next === '"') { cell += '"'; i++; }
      else if (char === '"') quoted = false;
      else cell += char;
    } else if (char === '"') quoted = true;
    else if (char === ",") { row.push(cell); cell = ""; }
    else if (char === "\n") { row.push(cell); rows.push(row); row = []; cell = ""; }
    else if (char !== "\r") cell += char;
  }
  row.push(cell);
  if (row.some(value => value !== "")) rows.push(row);
  return rows;
}
function occupantToCsv(person) {
  return [person.firstName, person.lastName, person.nickName, person.phone, person.status, person.isCurrentResident ?? person.isPrimary ? "yes" : "no"].map(value => String(value || "").replaceAll("|", "/")).join("|");
}
function carToCsv(car) {
  return [car.prefix, car.number, car.province, car.brand, car.model, car.color].map(value => String(value || "").replaceAll("|", "/")).join("|");
}
function residentLoginForRoom(room) {
  const roomNos = [room.roomNo1, room.roomNo2].map(value => String(value || "").toUpperCase()).filter(Boolean);
  return users.find(user => user.role === "resident" && roomNos.includes(String(user.room || "").toUpperCase())) || null;
}
function residentExportCounts() {
  return {
    occupants: Math.max(5, ...residentRooms.map(room => (room.occupants || []).length)),
    cars: Math.max(5, ...residentRooms.map(room => (room.cars || []).length))
  };
}
function occupantHeaders(index) {
  return [
    `occupant_${index}_first_name`,
    `occupant_${index}_last_name`,
    `occupant_${index}_nickname`,
    `occupant_${index}_phone`,
    `occupant_${index}_status`,
    `occupant_${index}_is_current`
  ];
}
function carHeaders(index) {
  return [
    `car_${index}_prefix`,
    `car_${index}_number`,
    `car_${index}_province`,
    `car_${index}_brand`,
    `car_${index}_model`,
    `car_${index}_color`
  ];
}
function occupantValues(person = {}) {
  return [
    person.firstName || "",
    person.lastName || "",
    person.nickName || "",
    person.phone || "",
    person.status || "",
    person.isCurrentResident ?? person.isPrimary ? "yes" : "no"
  ];
}
function carValues(car = {}) {
  return [car.prefix || "", car.number || "", car.province || "", car.brand || "", car.model || "", car.color || ""];
}
function exportResidentsCsv() {
  const counts = residentExportCounts();
  const occupantHeaderList = Array.from({ length: counts.occupants }, (_, index) => occupantHeaders(index + 1)).flat();
  const carHeaderList = Array.from({ length: counts.cars }, (_, index) => carHeaders(index + 1)).flat();
  const headers = [
    "room_id", "login_id", "password",
    "room_no_1", "room_no_2", "building", "common_fee_status", "primary_person_key",
    "owner_first_name", "owner_last_name", "owner_nickname", "owner_phone", "owner_is_current",
    ...occupantHeaderList,
    ...carHeaderList
  ];
  const rows = residentRooms.map(room => {
    const login = residentLoginForRoom(room);
    const occupants = room.occupants || [];
    const cars = room.cars || [];
    return [
    room.id || "",
    login?.room || room.roomNo1 || room.roomNo2 || "",
    login?.password || "",
    room.roomNo1,
    room.roomNo2,
    room.building || "",
    room.commonFeeStatus || "unpaid",
    room.personOrder?.[0] || "",
    room.owner?.firstName || "",
    room.owner?.lastName || "",
    room.owner?.nickName || "",
    room.owner?.phone || "",
    room.owner?.isCurrentResident ?? room.owner?.isPrimary ? "yes" : "no",
    ...Array.from({ length: counts.occupants }, (_, index) => occupantValues(occupants[index])).flat(),
    ...Array.from({ length: counts.cars }, (_, index) => carValues(cars[index])).flat()
  ];
  });
  const csv = [headers, ...rows].map(csvLine).join("\n");
  const blob = new Blob(["\ufeff" + csv], { type: "text/csv;charset=utf-8" });
  const url = URL.createObjectURL(blob);
  const a = document.createElement("a");
  a.href = url;
  a.download = "resident-rooms-export.csv";
  a.click();
  URL.revokeObjectURL(url);
  addLog("RESIDENTS_EXPORTED", `Resident CSV exported by ${getUserName(currentUser)}`);
}
function parseOccupants(value) {
  return String(value || "").split(";").map(item => item.trim()).filter(Boolean).map(item => {
    const [firstName = "", lastName = "", nickName = "", phone = "", status = "resident", isCurrent = "no"] = item.split("|");
    return { firstName, lastName, nickName, phone, status: status || "resident", isCurrentResident: ["yes", "true", "1"].includes(String(isCurrent).toLowerCase()) };
  });
}
function parseCars(value) {
  return String(value || "").split(";").map(item => item.trim()).filter(Boolean).map(item => {
    const [prefix = "", number = "", province = "", brand = "", model = "", color = ""] = item.split("|");
    return { prefix, number, province, brand, model, color };
  });
}
function truthyCsv(value) {
  return ["yes", "true", "1", "y"].includes(String(value || "").toLowerCase());
}
function parseWideOccupants(record) {
  const occupants = [];
  for (let index = 1; index <= 30; index += 1) {
    const firstName = record[`occupant_${index}_first_name`] || "";
    const lastName = record[`occupant_${index}_last_name`] || "";
    const nickName = record[`occupant_${index}_nickname`] || "";
    const phone = record[`occupant_${index}_phone`] || "";
    const status = record[`occupant_${index}_status`] || "resident";
    const isCurrent = record[`occupant_${index}_is_current`] || "";
    if (![firstName, lastName, nickName, phone].some(Boolean)) continue;
    occupants.push({ firstName, lastName, nickName, phone, status: status || "resident", isCurrentResident: truthyCsv(isCurrent) });
  }
  return occupants;
}
function parseWideCars(record) {
  const cars = [];
  for (let index = 1; index <= 30; index += 1) {
    const prefix = record[`car_${index}_prefix`] || "";
    const number = record[`car_${index}_number`] || "";
    const province = record[`car_${index}_province`] || "";
    const brand = record[`car_${index}_brand`] || "";
    const model = record[`car_${index}_model`] || "";
    const color = record[`car_${index}_color`] || "";
    if (![prefix, number, province, brand, model, color].some(Boolean)) continue;
    cars.push({ prefix, number, province, brand, model, color });
  }
  return cars;
}
function ensureResidentLogin(room, record) {
  const loginId = String(record.login_id || "").trim().toUpperCase();
  const password = String(record.password || "").trim();
  if (!loginId && !password) return;
  const existing = residentLoginForRoom(room) || users.find(user => user.role === "resident" && String(user.room || "").toUpperCase() === loginId);
  if (existing) {
    if (loginId) existing.room = loginId;
    if (password) existing.password = password;
    teamOverrides[existing.id] = { ...(teamOverrides[existing.id] || {}), room: existing.room, password: existing.password };
    return;
  }
  const ownerName = fullName(room.owner?.firstName, room.owner?.lastName) || loginId || room.roomNo1;
  const newUser = {
    id: `resident-${Date.now()}-${Math.random().toString(16).slice(2, 7)}`,
    name: ownerName,
    enName: ownerName,
    room: loginId || room.roomNo1 || room.roomNo2,
    role: "resident",
    roleKey: "role.resident",
    password: password || "1234"
  };
  users.push(newUser);
  customUsers.push(newUser);
  teamOverrides[newUser.id] = { ...newUser };
}
async function importResidentsCsv(file) {
  const text = await file.text();
  const rows = parseCsv(text.replace(/^\ufeff/, ""));
  const headers = rows.shift()?.map(header => header.trim()) || [];
  if (!headers.includes("room_no_1") || !headers.includes("room_no_2")) return showToast("CSV ต้องมีคอลัมน์ room_no_1 และ room_no_2");
  let imported = 0;
  rows.forEach(row => {
    const record = Object.fromEntries(headers.map((header, index) => [header, row[index] || ""]));
    const roomNo1 = record.room_no_1.trim();
    const roomNo2 = record.room_no_2.trim();
    if (!roomNo1 && !roomNo2) return;
    const existing = residentRooms.find(room => room.id === record.room_id || room.roomNo1 === roomNo1 || room.roomNo2 === roomNo2);
    const occupants = headers.some(header => header.startsWith("occupant_")) ? parseWideOccupants(record) : parseOccupants(record.occupants);
    const cars = headers.some(header => header.startsWith("car_")) ? parseWideCars(record) : parseCars(record.cars);
    const next = {
      id: existing?.id || `room-${Date.now()}-${imported}`,
      roomNo1,
      roomNo2,
      building: record.building.trim(),
      commonFeeStatus: record.common_fee_status === "paid" ? "paid" : "unpaid",
      owner: {
        firstName: record.owner_first_name || "",
        lastName: record.owner_last_name || "",
        nickName: record.owner_nickname || "",
        phone: record.owner_phone || "",
        status: "owner",
        isCurrentResident: truthyCsv(record.owner_is_current || record.owner_is_primary || "yes")
      },
      occupants,
      cars,
      personOrder: [record.primary_person_key || "owner", "owner", ...occupants.map((_, index) => `occupant-${index}`)].filter((value, index, array) => value && array.indexOf(value) === index)
    };
    if (existing) Object.assign(existing, next);
    else if (residentRooms.length < 882) residentRooms.push(next);
    ensureResidentLogin(existing || next, record);
    imported++;
  });
  saveCustomUsers();
  saveTeamOverrides();
  saveResidentRooms();
  addLog("RESIDENTS_IMPORTED", `${imported} resident room rows imported by ${getUserName(currentUser)}`);
  renderResidents();
  showToast(`Import CSV แล้ว ${imported} รายการ`);
}
function addLog(action, detail = "", actor = currentUser) {
  const actorRole = actor?.role === "admin" ? "admin" : actor?.isCoAdmin ? "coadmin" : actor?.role === "staff" ? "staff" : "system";
  const entry = { id: crypto.randomUUID?.() || String(Date.now()), at: new Date().toISOString(), actor: actor ? getUserName(actor) : "System", actorId: actor?.id || "", actorRole, action, detail };
  if (actorRole === "admin" || actorRole === "coadmin") {
    adminLogs.unshift(entry);
    adminLogs = adminLogs.slice(0, 300);
    saveLogs();
  } else if (actorRole === "staff") {
    staffLogs.unshift(entry);
    staffLogs = staffLogs.slice(0, 300);
    saveStaffLogs();
  }
}
function showToast(message) {
  const el = $("#toast"); el.textContent = message; el.classList.add("show");
  clearTimeout(showToast.timer); showToast.timer = setTimeout(() => el.classList.remove("show"), 2600);
}
function updateAuthStats() {
  const normalizedJobs = jobs.map(normalizeJob).filter(Boolean);
  const totalStat = $("#authTotalIssues");
  const solvedStat = $("#authSolvedIssues");
  if (totalStat) totalStat.textContent = String(normalizedJobs.length);
  if (solvedStat) solvedStat.textContent = String(normalizedJobs.filter(job => normalizeStatus(job.status) === "completed").length);
}
function translateStaticDom() {
  document.documentElement.lang = currentLang;
  document.title = t("meta.title");
  $$("[data-i18n]").forEach(el => el.textContent = t(el.dataset.i18n));
  $$("[data-i18n-html]").forEach(el => el.innerHTML = t(el.dataset.i18nHtml));
  $$("[data-i18n-placeholder]").forEach(el => el.placeholder = t(el.dataset.i18nPlaceholder));
  $$("[data-i18n-aria-label]").forEach(el => el.setAttribute("aria-label", t(el.dataset.i18nAriaLabel)));
  ["#languageToggle", "#authLanguageToggle"].forEach(selector => {
    const button = $(selector);
    if (!button) return;
    button.textContent = currentLang === "th" ? "EN" : "TH";
  });
  $("#authTitle").textContent = setupMode ? t("auth.setupTitle") : t("auth.loginTitle");
  $("#authSubtitle").textContent = setupMode ? t("auth.setupSubtitle") : t("auth.loginSubtitle");
  $("#setupToggle").textContent = setupMode ? t("auth.backToLogin") : t("auth.setupLink");
  $(".auth-submit [data-i18n]").textContent = setupMode ? t("auth.createAccount") : t("auth.loginButton");
  updateAuthStats();
}
function setLanguage(lang) {
  currentLang = lang === "en" ? "en" : "th";
  localStorage.setItem("juristicLang", currentLang);
  translateStaticDom();
  updateCurrentDate();
  if (currentUser) {
    updateUserChrome();
    setupRoleMenu();
    populateControls();
    renderAll();
    navigate(currentView);
  }
}
function detectDeviceMode() {
  const preview = new URLSearchParams(location.search).get("device");
  if (preview === "mobile" || preview === "desktop") return preview;
  return /Android|iPhone|iPod|IEMobile|Opera Mini/i.test(navigator.userAgent) || window.matchMedia("(max-width: 820px)").matches ? "mobile" : "desktop";
}
function applyDeviceMode() {
  deviceMode = detectDeviceMode();
  document.body.classList.remove("device-mobile", "device-desktop");
  document.body.classList.add(`device-${deviceMode}`);
}
function updateCurrentDate() {
  $("#currentDateLabel").textContent = new Intl.DateTimeFormat(currentLang === "th" ? "th-TH" : "en-US", { weekday: "long", day: "numeric", month: "long", year: "numeric" }).format(new Date());
}
function updateUserChrome() {
  const displayName = getUserName(currentUser);
  $("#sidebarName").textContent = displayName;
  $("#sidebarRole").textContent = roleLabel(currentUser) + (currentUser.isCoAdmin ? ` · ${t("role.coAdmin")}` : "") + (currentUser.role === "resident" ? ` · ${currentUser.room}` : currentUser.canAssign ? " · Assign" : "");
  $("#sidebarAvatar").textContent = initials(displayName);
  $("#welcomeName").textContent = currentLang === "th" ? `คุณ${currentUser.name.split(" ")[0]}` : displayName.split(" ")[0];
}
function setupRoleMenu() {
  const isAdmin = currentUser.role === "admin";
  const isResident = currentUser.role === "resident";
  const teamManager = canManageTeam();
  const residentWorkAllowed = canViewResidentWork();
  ensureBackhouseNav();
  ensureBackhouseView();
  ensureOrganizationNav();
  ensureOrganizationView();
  ensurePermissionNav();
  ensurePermissionView();
  ensureChatNav();
  ensureChatView();
  ensureRoutineNav();
  ensureRoutineViews();
  $$(".admin-only").forEach(el => el.classList.toggle("hidden", !isAdmin));
  $$(".manage-team-only").forEach(el => el.classList.toggle("hidden", !teamManager));
  permissionSidebarItems.forEach(item => {
    $$(`[data-view='${item.view}']`).forEach(el => el.classList.toggle("hidden", !hasSidebarAccess(item.view)));
  });
  $$(".google-form-cta").forEach(el => el.classList.toggle("hidden", !canSeeGoogleFormsLink()));
  $$("[data-view='logs']").forEach(el => el.classList.toggle("hidden", currentUser?.role !== "admin"));
  $$("#createJobBtn, #mobileCreateJobBtn").forEach(el => el.classList.toggle("hidden", !hasActionPermission("createJob")));
  if (isResident) {
    $$("[data-view='dashboard'] [data-i18n], [data-view='dashboard'] small").forEach(el => el.textContent = currentLang === "th" ? "ภาพรวม" : "Overview");
    $$("[data-view='roomjobs'] [data-i18n], [data-view='roomjobs'] small").forEach(el => el.textContent = currentLang === "th" ? "ห้องของฉัน" : "My Room");
  }
  organizeSidebarGroups();
}
function canSeeGoogleFormsLink(user = currentUser) {
  if (!user) return false;
  return user.role === "admin" || user.role === "resident" || inferDepartment(user) === "committee";
}
function organizeSidebarGroups() {
  const nav = $(".main-nav");
  if (!nav) return;
  nav.querySelectorAll(".nav-section-label").forEach(label => label.remove());
  const groups = [
    { key: "overview", label: "", views: ["dashboard"] },
    { key: "work", label: { th: "งานและการติดตาม", en: "Work & Tracking" }, views: ["commonWork", "pool", "residentWork", "myjobs", "juristicTeam", "technicianJobs", "housekeepingJobs", "roomjobs"] },
    { key: "routine", label: { th: "งานประจำ / Daily PM", en: "Routine / Daily PM" }, views: ["routineDashboard", "routineToday", "routineVerify", "routineTemplates", "routineCalendar", "routineReports", "routineSettings"] },
    { key: "info", label: { th: "ข้อมูลโครงการ", en: "Project Info" }, views: ["calendar", "finance", "backhouse"] },
    { key: "people", label: { th: "ผู้ใช้งานและโครงสร้าง", en: "People & Organization" }, views: ["residents", "team", "organization"] },
    { key: "system", label: { th: "ระบบและบันทึก", en: "System & Logs" }, views: ["permissions", "staffLogs", "logs"] }
  ];
  groups.forEach(group => {
    const buttons = group.views.map(view => nav.querySelector(`[data-view='${view}']`)).filter(Boolean);
    if (!buttons.length) return;
    let labelEl = null;
    if (group.label) {
      labelEl = document.createElement("button");
      labelEl.type = "button";
      labelEl.className = "nav-section-label";
      labelEl.dataset.navGroup = group.key;
      labelEl.dataset.collapsed = collapsedSidebarGroups[group.key] ? "true" : "false";
      labelEl.innerHTML = `<span>${group.label[currentLang] || group.label.th}</span><b>${collapsedSidebarGroups[group.key] ? "▸" : "▾"}</b>`;
      nav.appendChild(labelEl);
    }
    buttons.forEach(button => nav.appendChild(button));
    if (labelEl) {
      const hasVisibleButton = buttons.some(button => !button.classList.contains("hidden"));
      labelEl.classList.toggle("hidden", !hasVisibleButton);
      buttons.forEach(button => button.classList.toggle("nav-group-collapsed", !!collapsedSidebarGroups[group.key] && !button.classList.contains("active")));
    }
  });
}
function showApp(user) {
  currentUser = user;
  $("#authView").classList.add("hidden");
  $("#appView").classList.remove("hidden");
  setupRoleMenu();
  updateUserChrome();
  updateCurrentDate();
  sessionStorage.setItem("juristicUser", user.id);
  if (user.role === "admin") addLog("ADMIN_LOGIN", `${getUserName(user)} signed in`, user);
  else if (user.isCoAdmin) addLog("CO_ADMIN_LOGIN", `${getUserName(user)} signed in`, user);
  else if (user.role === "staff") addLog("STAFF_LOGIN", `${getUserName(user)} signed in`, user);
  populateControls();
  renderAll();
  loadRemoteSnapshot();
  navigate("dashboard");
}
function ensureBackhouseNav() {
  if (!$(".main-nav [data-view='backhouse']")) {
    const logsButton = $(".main-nav [data-view='logs']");
    const button = document.createElement("button");
    button.className = "nav-item manage-team-only";
    button.dataset.view = "backhouse";
    button.innerHTML = `<span>▣</span> <span data-i18n="nav.backhouse">${t("nav.backhouse")}</span>`;
    logsButton?.before(button);
  }
}
function ensureBackhouseView() {
  if ($("#backhouseView")) return;
  $("#logsView")?.insertAdjacentHTML("beforebegin", `<section id="backhouseView" class="view"></section>`);
}
function ensureOrganizationNav() {
  if ($(".main-nav [data-view='organization']")) return;
  const backhouseButton = $(".main-nav [data-view='backhouse']");
  const logsButton = $(".main-nav [data-view='logs']");
  const button = document.createElement("button");
  button.className = "nav-item manage-team-only";
  button.dataset.view = "organization";
  button.innerHTML = `<span>◎</span> <span data-i18n="nav.organization">${t("nav.organization")}</span>`;
  (backhouseButton || logsButton)?.before(button);
}
function ensureOrganizationView() {
  if ($("#organizationView")) return;
  $("#backhouseView")?.insertAdjacentHTML("beforebegin", `<section id="organizationView" class="view"></section>`);
}
function ensurePermissionNav() {
  if ($(".main-nav [data-view='permissions']")) return;
  const logsButton = $(".main-nav [data-view='logs']");
  const button = document.createElement("button");
  button.className = "nav-item manage-permissions-only";
  button.dataset.view = "permissions";
  button.innerHTML = `<span>⚙</span> <span data-i18n="nav.permissions">${t("nav.permissions")}</span>`;
  logsButton?.before(button);
}
function ensurePermissionView() {
  if ($("#permissionsView")) return;
  $("#logsView")?.insertAdjacentHTML("beforebegin", `<section id="permissionsView" class="view"></section>`);
}
function ensureChatNav() {
  $$(".main-nav [data-view='chat']").forEach(button => button.remove());
  const slot = $("#sidebarChatSlot");
  if (slot && !slot.querySelector("[data-view='chat']")) {
    const button = document.createElement("button");
    button.className = "nav-item sidebar-chat-button";
    button.dataset.view = "chat";
    button.innerHTML = `<span>✉</span> <span data-i18n="nav.chat">${t("nav.chat")}</span>`;
    slot.appendChild(button);
  }
  if (!$(".mobile-nav [data-view='chat']")) {
    const calendarButton = $(".mobile-nav [data-view='calendar']");
    const button = document.createElement("button");
    button.className = "mobile-nav-item";
    button.dataset.view = "chat";
    button.innerHTML = `<span>✉</span><small data-i18n="nav.chatShort">${t("nav.chatShort")}</small>`;
    calendarButton?.after(button);
  }
}
function ensureChatView() {
  if ($("#chatView")) return;
  $("#calendarView")?.insertAdjacentHTML("afterend", `<section id="chatView" class="view"></section>`);
}
function ensureRoutineNav() {
  const nav = $(".main-nav");
  if (!nav) return;
  [
    ["routineDashboard", "▣", "nav.routineDashboard"],
    ["routineToday", "☑", "nav.routineToday"],
    ["routineVerify", "◇", "nav.routineVerify"],
    ["routineTemplates", "▤", "nav.routineTemplates"],
    ["routineCalendar", "◷", "nav.routineCalendar"],
    ["routineReports", "▥", "nav.routineReports"],
    ["routineSettings", "⚙", "nav.routineSettings"]
  ].forEach(([view, icon, key]) => {
    if (nav.querySelector(`[data-view='${view}']`)) return;
    const button = document.createElement("button");
    button.className = "nav-item";
    button.dataset.view = view;
    button.innerHTML = `<span>${icon}</span> <span data-i18n="${key}">${t(key)}</span>`;
    nav.appendChild(button);
  });
}
function ensureDashboardLayout() {
  const dashboard = $("#dashboardView");
  const welcome = dashboard?.querySelector(".welcome-row");
  const categoryPanel = dashboard?.querySelector(".category-panel");
  ensureMainCategoryOverviewUi();
  const mainPanel = dashboard?.querySelector("#mainCategoryOverviewPanel");
  if (dashboard && welcome && mainPanel && welcome.nextElementSibling !== mainPanel) {
    welcome.insertAdjacentElement("afterend", mainPanel);
  }
  if (dashboard && mainPanel && categoryPanel && mainPanel.nextElementSibling !== categoryPanel) {
    mainPanel.insertAdjacentElement("afterend", categoryPanel);
  } else if (dashboard && welcome && categoryPanel && welcome.nextElementSibling !== categoryPanel && !mainPanel) {
    welcome.insertAdjacentElement("afterend", categoryPanel);
  }
  const grid = dashboard?.querySelector(".dashboard-grid");
  if (grid) grid.classList.add("dashboard-grid-hidden");
}
function canManageResidentChats(user = currentUser) {
  if (!user) return false;
  return user.role === "admin" || user.isCoAdmin || ["juristic", "committee"].includes(inferDepartment(user)) || user.canAssign || user.assignL1 || user.assignL2;
}
function chatThreadTitle(thread) {
  return getText(thread.title) || thread.roomNo || (thread.type === "common" ? (currentLang === "th" ? "ส่วนกลาง" : "Common") : "-");
}
function chatThreadLastMessage(thread) {
  return thread.messages?.[thread.messages.length - 1] || null;
}
function ensureResidentPrivateChat(user = currentUser) {
  if (!user || user.role !== "resident") return;
  const exists = chatThreads.some(thread => thread.type === "resident_private" && (thread.residentId === user.id || thread.roomNo === user.room));
  if (exists) return;
  chatThreads.push({
    id: `chat-room-${String(user.room || user.id).toLowerCase().replace(/[^a-z0-9]+/g, "")}`,
    type: "resident_private",
    title: { th: `${user.room} · ${user.name}`, en: `${user.room} · ${user.enName || user.name}` },
    roomNo: user.room,
    residentId: user.id,
    updatedAt: new Date().toISOString(),
    messages: []
  });
  saveChatThreads();
}
function ensureAllResidentPrivateChats() {
  if (!canManageResidentChats()) return;
  let changed = false;
  activeUsers().filter(user => user.role === "resident").forEach(user => {
    const exists = chatThreads.some(thread => thread.type === "resident_private" && (thread.residentId === user.id || thread.roomNo === user.room));
    if (exists) return;
    chatThreads.push({
      id: `chat-room-${String(user.room || user.id).toLowerCase().replace(/[^a-z0-9]+/g, "")}`,
      type: "resident_private",
      title: { th: `${user.room} · ${user.name}`, en: `${user.room} · ${user.enName || user.name}` },
      roomNo: user.room,
      residentId: user.id,
      updatedAt: new Date().toISOString(),
      messages: []
    });
    changed = true;
  });
  if (changed) saveChatThreads();
}
function visibleChatThreads() {
  if (!currentUser) return [];
  ensureResidentPrivateChat();
  ensureAllResidentPrivateChats();
  const canManage = canManageResidentChats();
  return chatThreads.filter(thread => {
    if (thread.type === "common") return true;
    if (canManage) return true;
    return currentUser.role === "resident" && (thread.residentId === currentUser.id || thread.roomNo === currentUser.room);
  });
}
function chatScopeThreads() {
  const threads = visibleChatThreads();
  if (chatScope === "common") return threads.filter(thread => thread.type === "common");
  return threads.filter(thread => thread.type === "resident_private");
}
function matchesChatSearch(thread) {
  const query = chatSearch.trim().toLowerCase();
  const filterOk = chatRoomFilter === "all" || String(thread.roomNo || "").toLowerCase().startsWith(chatRoomFilter.toLowerCase());
  if (!filterOk) return false;
  if (!query) return true;
  const resident = getUser(thread.residentId);
  const haystack = [
    chatThreadTitle(thread),
    thread.roomNo,
    getUserName(resident),
    resident?.room,
    ...(thread.messages || []).map(message => message.text)
  ].join(" ").toLowerCase();
  return haystack.includes(query);
}
function ensureActiveChatRoom(threads) {
  if (!threads.length) {
    activeChatRoomId = "";
    return null;
  }
  const existing = threads.find(thread => thread.id === activeChatRoomId);
  if (existing) return existing;
  activeChatRoomId = threads[0].id;
  return threads[0];
}
function chatSenderLabel(message) {
  const user = getUser(message.by);
  if (!user) return currentLang === "th" ? "ระบบ" : "System";
  if (currentUser?.role === "resident" && user.role !== "resident") return currentLang === "th" ? "นิติบุคคล" : "Juristic";
  return getUserName(user);
}
function chatRoomFiltersHtml() {
  const roomLetters = [...new Set(chatThreads.filter(t => t.type === "resident_private" && t.roomNo).map(t => t.roomNo.slice(0, 1)))].sort();
  return `<select id="chatRoomFilter">
    <option value="all">${currentLang === "th" ? "ทุกตึก / ทุกห้อง" : "All buildings / rooms"}</option>
    ${roomLetters.map(letter => `<option value="${letter}" ${chatRoomFilter === letter ? "selected" : ""}>${currentLang === "th" ? "ตึก" : "Building"} ${letter}</option>`).join("")}
  </select>`;
}
function renderChat() {
  ensureChatView();
  const view = $("#chatView");
  if (!view) return;
  const isResident = currentUser?.role === "resident";
  if (isResident && !["common", "private"].includes(chatScope)) chatScope = "common";
  if (!isResident && !["common", "residents"].includes(chatScope)) chatScope = "common";
  const scopes = isResident
    ? [{ key: "common", label: currentLang === "th" ? "ส่วนกลาง" : "Common" }, { key: "private", label: currentLang === "th" ? "ส่วนตัวกับนิติ" : "Private with Juristic" }]
    : [{ key: "common", label: currentLang === "th" ? "ส่วนกลาง" : "Common" }, { key: "residents", label: currentLang === "th" ? "ลูกบ้านทั้งหมด" : "All residents" }];
  const threads = chatScopeThreads().filter(matchesChatSearch).sort((a, b) => new Date(b.updatedAt || 0) - new Date(a.updatedAt || 0));
  const activeThread = ensureActiveChatRoom(threads);
  const messageQuery = chatSearch.trim().toLowerCase();
  const messages = (activeThread?.messages || []).filter(message => !messageQuery || message.text.toLowerCase().includes(messageQuery) || chatSenderLabel(message).toLowerCase().includes(messageQuery));
  view.innerHTML = `<div class="chat-layout">
    <aside class="chat-sidebar panel">
      <div class="chat-scope-tabs">${scopes.map(scope => `<button type="button" class="${chatScope === scope.key ? "active" : ""}" data-chat-scope="${scope.key}">${scope.label}</button>`).join("")}</div>
      <div class="chat-search-row">
        <div class="search-box"><span>⌕</span><input id="chatSearchInput" value="${esc(chatSearch)}" placeholder="${currentLang === "th" ? "ค้นหาห้อง / เลขห้อง / ชื่อ / ข้อความ..." : "Search room / resident / message..."}"></div>
        ${!isResident ? chatRoomFiltersHtml() : ""}
      </div>
      <div class="chat-room-list">${threads.map(thread => {
        const last = chatThreadLastMessage(thread);
        return `<button class="chat-room ${thread.id === activeChatRoomId ? "active" : ""}" type="button" data-chat-room="${thread.id}">
          <span class="chat-room-avatar">${thread.type === "common" ? "C" : initials(chatThreadTitle(thread))}</span>
          <span><strong>${esc(chatThreadTitle(thread))}</strong><small>${last ? esc(last.text) : (currentLang === "th" ? "ยังไม่มีข้อความ" : "No messages yet")}</small></span>
          <em>${last ? new Date(last.at).toLocaleDateString(currentLang === "th" ? "th-TH" : "en-US", { day: "2-digit", month: "short" }) : ""}</em>
        </button>`;
      }).join("") || emptyState(currentLang === "th" ? "ไม่พบห้องแชท" : "No chat rooms found")}</div>
    </aside>
    <section class="chat-panel panel">
      ${activeThread ? `<div class="chat-header">
        <div>
          <span>${activeThread.type === "common" ? (currentLang === "th" ? "ห้องแชทส่วนกลาง" : "Common chat") : (currentLang === "th" ? "แชทส่วนตัว" : "Private chat")}</span>
          <h3>${esc(chatThreadTitle(activeThread))}</h3>
          <p>${activeThread.type === "common" ? (currentLang === "th" ? "ทุกข้อความในห้องนี้เป็นการสื่อสารส่วนกลาง" : "Messages here are shared common communications") : `${currentLang === "th" ? "เลขห้อง" : "Room"} ${esc(activeThread.roomNo || "-")}`}</p>
        </div>
      </div>
      <div class="chat-message-list">${messages.map(message => {
        const isMine = message.by === currentUser.id;
        return `<div class="chat-message ${isMine ? "mine" : ""}">
          <div class="chat-bubble">
            <strong>${esc(chatSenderLabel(message))}</strong>
            <p>${esc(message.text)}</p>
            <time>${new Date(message.at).toLocaleString(currentLang === "th" ? "th-TH" : "en-US")}</time>
          </div>
        </div>`;
      }).join("") || `<p class="muted-line">${currentLang === "th" ? "ไม่พบข้อความที่ค้นหา" : "No matching messages"}</p>`}</div>
      <form class="chat-compose" data-chat-send="${activeThread.id}">
        <input name="message" placeholder="${currentLang === "th" ? "พิมพ์ข้อความ..." : "Type a message..."}" autocomplete="off">
        <button class="primary-btn" type="submit">${currentLang === "th" ? "ส่ง" : "Send"}</button>
      </form>` : emptyState(currentLang === "th" ? "เลือกห้องแชทเพื่อเริ่มต้น" : "Select a chat room to start")}
    </section>
  </div>`;
}
const routineDepartmentLabel = (key) => routineDepartments.find(item => item.key === key)?.[currentLang] || routineDepartments.find(item => item.key === key)?.th || key || "-";
const routineStatusLabel = (status) => routineStatusMeta[status]?.[currentLang] || routineStatusMeta[status]?.th || status || "-";
const routineStatusTone = (status) => routineStatusMeta[status]?.tone || "gray";
const isRoutineView = (view = currentView) => ["routineDashboard", "routineToday", "routineVerify", "routineTemplates", "routineCalendar", "routineReports", "routineSettings"].includes(view);
function canVerifyRoutineTask(user = currentUser, task = null) {
  if (!user) return false;
  if (user.role === "admin" || user.isCoAdmin || hasActionPermission("verifyRoutineTasks", user)) return true;
  if (task && task.verifier_id === user.id) return true;
  return false;
}
function canManageRoutineTemplates(user = currentUser) {
  return !!(user && (user.role === "admin" || user.isCoAdmin || hasActionPermission("manageRoutineTemplates", user)));
}
function routineTemplate(taskOrTemplate) {
  return routineTemplates.find(template => template.id === taskOrTemplate?.template_id || template.id === taskOrTemplate?.id);
}
function routineTaskVisibleToUser(task, user = currentUser) {
  if (!user || !task) return false;
  if (user.role === "admin" || user.isCoAdmin || ["juristic", "committee"].includes(inferDepartment(user))) return true;
  if (task.assignee_id === user.id || task.verifier_id === user.id) return true;
  return task.assignee_team_id && task.assignee_team_id === inferDepartment(user);
}
function addRoutineLog(taskId, action, fromStatus = "", toStatus = "", message = "") {
  routineTaskLogs.unshift({
    id: `rlog-${Date.now()}-${Math.random().toString(16).slice(2)}`,
    task_id: taskId,
    action,
    from_status: fromStatus,
    to_status: toStatus,
    actor_id: currentUser?.id || "system",
    actor_name: currentUser ? getUserName(currentUser) : "System",
    message,
    created_at: new Date().toISOString()
  });
  routineTaskLogs = routineTaskLogs.slice(0, 1000);
}
function routineTaskIdFor(template, date) {
  return `RT-${date.replaceAll("-", "")}-${template.id.replace("rtemp-", "").slice(0, 10).toUpperCase()}`;
}
function routineInitialStatus(template, date = todayIso()) {
  if (date < todayIso()) return "missed";
  const now = new Date();
  const start = new Date(`${date}T${template.start_time || "00:00"}:00`);
  const end = new Date(`${date}T${template.end_time || "23:59"}:00`);
  if (date > todayIso()) return "scheduled";
  if (now < start) return "scheduled";
  if (now > end) return "ready";
  return "ready";
}
function generateRoutineTasksForDate(date = todayIso()) {
  let changed = false;
  routineTemplates.filter(template => template.is_active).forEach(template => {
    const id = routineTaskIdFor(template, date);
    if (routineTasks.some(task => task.id === id)) return;
    const task = {
      id,
      template_id: template.id,
      task_date: date,
      title: template.title,
      department: template.department,
      area_name: template.area_name,
      start_time: template.start_time,
      end_time: template.end_time,
      assignee_id: template.assignee_id,
      assignee_team_id: template.assignee_team_id,
      verifier_id: template.verifier_id,
      verifier_role: template.verifier_role,
      status: routineInitialStatus(template, date),
      started_at: "",
      submitted_at: "",
      verified_at: "",
      verified_by: "",
      reject_reason: "",
      is_late: false,
      note: "",
      abnormal_note: "",
      created_at: new Date().toISOString(),
      updated_at: new Date().toISOString()
    };
    routineTasks.push(task);
    addRoutineLog(task.id, "task_generated", "", task.status, `${task.title} generated from template ${template.id}`);
    changed = true;
  });
  if (changed) saveRoutineData();
}
function updateRoutineDerivedStatuses() {
  const now = new Date();
  let changed = false;
  routineTasks.forEach(task => {
    if (!["scheduled", "ready"].includes(task.status)) return;
    const end = new Date(`${task.task_date}T${task.end_time || "23:59"}:00`);
    const start = new Date(`${task.task_date}T${task.start_time || "00:00"}:00`);
    const next = now > end ? "missed" : now >= start ? "ready" : "scheduled";
    if (task.status !== next) {
      addRoutineLog(task.id, next === "missed" ? "task_missed" : "task_ready", task.status, next, `${task.title} auto status update`);
      task.status = next;
      task.updated_at = new Date().toISOString();
      changed = true;
    }
  });
  if (changed) saveRoutineData();
}
function routineChecklistItems(task) {
  return [...(routineTemplate(task)?.checklist || [])].sort((a, b) => (a.sort_order || 0) - (b.sort_order || 0));
}
function routineTaskResultsFor(taskId) {
  return routineTaskResults.filter(item => item.task_id === taskId);
}
function routineTaskAttachmentsFor(taskId, fileType = "") {
  return routineTaskAttachments.filter(item => item.task_id === taskId && (!fileType || item.file_type === fileType));
}
function routineResultValue(taskId, checklistId) {
  return routineTaskResults.find(item => item.task_id === taskId && item.checklist_item_id === checklistId)?.value || "";
}
function routineEvidenceCounts(task) {
  return {
    photos: routineTaskAttachmentsFor(task.id, "image").length,
    videos: routineTaskAttachmentsFor(task.id, "video").length
  };
}
function validateRoutineSubmission(task) {
  const template = routineTemplate(task);
  const results = routineTaskResultsFor(task.id);
  const missing = routineChecklistItems(task).filter(item => item.is_required).filter(item => {
    const result = results.find(row => row.checklist_item_id === item.id);
    return !result || result.value === "" || result.value === false || result.value === "false";
  });
  if (missing.length) return `${currentLang === "th" ? "กรุณาทำ Checklist ให้ครบ" : "Please complete required checklist"}: ${missing.map(item => item.label).join(", ")}`;
  const counts = routineEvidenceCounts(task);
  if (template?.requires_photo && counts.photos < Number(template.min_photos || 0)) return currentLang === "th" ? `รูปภาพขั้นต่ำ ${template.min_photos} รูป` : `Minimum ${template.min_photos} photos required`;
  if (template?.requires_video && counts.videos < Number(template.min_videos || 0)) return currentLang === "th" ? `วิดีโอขั้นต่ำ ${template.min_videos} คลิป` : `Minimum ${template.min_videos} videos required`;
  return "";
}
function routineVisibleTasks(view = currentView) {
  generateRoutineTasksForDate(routineDashboardDate);
  generateRoutineTasksForDate(todayIso());
  updateRoutineDerivedStatuses();
  return routineTasks.filter(task => routineTaskVisibleToUser(task)).filter(task => {
    if (view === "routineToday") return task.task_date === todayIso();
    if (view === "routineVerify") return ["submitted", "pending_verification", "late_submitted"].includes(task.status);
    if (routineDashboardDate && ["routineDashboard", "routineReports", "routineCalendar"].includes(view)) return task.task_date === routineDashboardDate || view !== "routineDashboard";
    return true;
  }).filter(task => routineDepartmentFilter === "all" || task.department === routineDepartmentFilter).filter(task => routineStatusFilter === "all" || task.status === routineStatusFilter);
}
function setRoutineTaskStatus(task, status, action, message = "") {
  const from = task.status;
  task.status = status;
  task.updated_at = new Date().toISOString();
  addRoutineLog(task.id, action, from, status, message || `${task.title}: ${from} → ${status}`);
  saveRoutineData();
}
async function stampRoutineImage(file, task) {
  const src = await readFileAsDataUrl(file);
  try {
    const image = await loadImage(src);
    const canvas = document.createElement("canvas");
    canvas.width = image.width;
    canvas.height = image.height;
    const ctx = canvas.getContext("2d");
    ctx.drawImage(image, 0, 0);
    const lines = ["Juristic Care", new Date().toLocaleString("th-TH"), `ผู้ส่งงาน: ${getUserName(currentUser)}`, `งาน: ${task.title}`, `พื้นที่: ${task.area_name}`, `Task ID: ${task.id}`];
    const padding = 18;
    ctx.font = `${Math.max(16, Math.round(canvas.width / 42))}px Arial`;
    const lineHeight = Math.max(22, Math.round(canvas.width / 32));
    const boxHeight = lineHeight * lines.length + padding;
    ctx.fillStyle = "rgba(0,0,0,.58)";
    ctx.fillRect(0, canvas.height - boxHeight, canvas.width, boxHeight);
    ctx.fillStyle = "white";
    lines.forEach((line, index) => ctx.fillText(line, padding, canvas.height - boxHeight + padding + lineHeight * index));
    return canvas.toDataURL("image/jpeg", 0.9);
  } catch {
    return src;
  }
}
async function addRoutineAttachments(task, files, type) {
  const added = [];
  for (const file of files) {
    const dataUrl = type === "image" ? await stampRoutineImage(file, task) : await readFileAsDataUrl(file);
    const item = {
      id: `ratt-${Date.now()}-${Math.random().toString(16).slice(2)}`,
      task_id: task.id,
      file_url: dataUrl,
      file_type: type,
      stamped_file_url: type === "image" ? dataUrl : "",
      uploaded_by: currentUser.id,
      uploaded_at: new Date().toISOString(),
      file_hash: `${file.name}-${file.size}-${file.lastModified || ""}`,
      metadata: { file_name: file.name, file_size: file.size, device_info: navigator.userAgent, task_id: task.id }
    };
    routineTaskAttachments.push(item);
    added.push(item);
  }
  if (added.length) addRoutineLog(task.id, "attachment_uploaded", task.status, task.status, `${added.length} ${type} attachment(s) uploaded`);
  saveRoutineData();
}
function ensureMainCategoryOverviewUi() {
  if ($("#mainCategoryOverviewPanel")) return;
  const dashboard = $("#dashboardView");
  const categoryPanel = dashboard?.querySelector(".category-panel");
  const html = `
    <section class="panel main-category-overview-panel" id="mainCategoryOverviewPanel">
      <div class="panel-header">
        <div><h3>ภาพรวมตามประเภทหลักของเหตุที่เกิดขึ้น</h3><p>แบ่งงานส่วนกลางและงานลูกบ้านตามสถานะ เสร็จแล้ว / กำลังดำเนินการ / เปิดงานใหม่</p></div>
      </div>
      <div id="mainCategoryOverviewBars" class="main-category-overview-bars"></div>
    </section>`;
  if (categoryPanel) categoryPanel.insertAdjacentHTML("beforebegin", html);
  else dashboard?.insertAdjacentHTML("beforeend", html);
}
function routineToolbarHtml(showStatus = true) {
  return `<div class="routine-toolbar">
    <label>${currentLang === "th" ? "วันที่" : "Date"}<input id="routineDashboardDate" type="date" value="${routineDashboardDate}"></label>
    <label>${currentLang === "th" ? "หมวดงาน" : "Department"}<select id="routineDepartmentFilter"><option value="all">${currentLang === "th" ? "ทุกหมวด" : "All"}</option>${routineDepartments.map(dept => `<option value="${dept.key}" ${routineDepartmentFilter === dept.key ? "selected" : ""}>${dept[currentLang]}</option>`).join("")}</select></label>
    ${showStatus ? `<label>${currentLang === "th" ? "สถานะ" : "Status"}<select id="routineStatusFilter"><option value="all">${currentLang === "th" ? "ทุกสถานะ" : "All status"}</option>${Object.keys(routineStatusMeta).map(status => `<option value="${status}" ${routineStatusFilter === status ? "selected" : ""}>${routineStatusLabel(status)}</option>`).join("")}</select></label>` : ""}
  </div>`;
}
function routineTaskCard(task, options = {}) {
  const assignee = getUser(task.assignee_id);
  const counts = routineEvidenceCounts(task);
  const template = routineTemplate(task);
  return `<article class="routine-card">
    <div class="routine-card-head">
      <div><span>${task.start_time}-${task.end_time}</span><h4>${esc(task.title)}</h4><p>${esc(task.area_name)} · ${routineDepartmentLabel(task.department)}</p></div>
      <i class="routine-status routine-${routineStatusTone(task.status)}">${routineStatusLabel(task.status)}</i>
    </div>
    <div class="routine-meta-grid">
      <div><small>${currentLang === "th" ? "ผู้รับผิดชอบ" : "Assignee"}</small><strong>${assignee ? getUserName(assignee) : "-"}</strong></div>
      <div><small>${currentLang === "th" ? "Checklist" : "Checklist"}</small><strong>${routineTaskResultsFor(task.id).length}/${routineChecklistItems(task).length}</strong></div>
      <div><small>${currentLang === "th" ? "หลักฐาน" : "Evidence"}</small><strong>📷 ${counts.photos}/${template?.min_photos || 0} · 🎥 ${counts.videos}/${template?.min_videos || 0}</strong></div>
    </div>
    <div class="inline-actions routine-actions">
      ${["ready", "scheduled", "missed", "rework", "rejected"].includes(task.status) && routineTaskVisibleToUser(task) ? `<button class="secondary-btn" type="button" data-routine-start="${task.id}">${currentLang === "th" ? "เริ่มงาน" : "Start"}</button>` : ""}
      <button class="primary-btn" type="button" data-routine-open="${task.id}">${currentLang === "th" ? "เปิดรายละเอียด" : "Open"}</button>
      ${options.verify && canVerifyRoutineTask(currentUser, task) ? `<button class="secondary-btn" type="button" data-routine-approve="${task.id}">${currentLang === "th" ? "Approve" : "Approve"}</button><button class="danger-btn" type="button" data-routine-reject="${task.id}">${currentLang === "th" ? "Reject" : "Reject"}</button>` : ""}
    </div>
    ${activeRoutineTaskId === task.id ? routineTaskDetailHtml(task) : ""}
  </article>`;
}
function routineTaskDetailHtml(task) {
  const template = routineTemplate(task);
  const attachments = routineTaskAttachmentsFor(task.id);
  const logs = routineTaskLogs.filter(log => log.task_id === task.id).slice(0, 8);
  return `<div class="routine-detail">
    <p>${esc(template?.description || "")}</p>
    <form data-routine-result-form="${task.id}">
      <div class="routine-checklist">${routineChecklistItems(task).map(item => {
        const value = routineResultValue(task.id, item.id);
        if (item.input_type === "checkbox") return `<label class="routine-check"><input type="checkbox" name="${item.id}" value="true" ${value === true || value === "true" ? "checked" : ""}> <span>${esc(item.label)}${item.is_required ? " *" : ""}<small>${esc(item.help_text || "")}</small></span></label>`;
        return `<label>${esc(item.label)}${item.is_required ? " *" : ""}<input name="${item.id}" value="${esc(value)}" placeholder="${esc(item.help_text || item.input_type)}"></label>`;
      }).join("")}</div>
      <label>${currentLang === "th" ? "หมายเหตุ / ความผิดปกติ" : "Note / abnormality"}<textarea name="note" rows="2">${esc(task.note || "")}</textarea></label>
      <div class="routine-evidence-box">
        <label class="secondary-btn">📷 ${currentLang === "th" ? "แนบ/ถ่ายรูป" : "Photo"}<input type="file" name="routinePhotos" accept="image/*" capture="environment" multiple hidden></label>
        <label class="secondary-btn">🎥 ${currentLang === "th" ? "แนบ/ถ่ายวิดีโอ" : "Video"}<input type="file" name="routineVideos" accept="video/*" capture="environment" multiple hidden></label>
      </div>
      <div class="inline-actions"><button class="secondary-btn" type="submit">${currentLang === "th" ? "บันทึก Checklist" : "Save checklist"}</button><button class="primary-btn" type="button" data-routine-submit="${task.id}">${currentLang === "th" ? "ส่งตรวจรับ" : "Submit for verification"}</button><button class="secondary-btn" type="button" data-routine-create-repair="${task.id}">${currentLang === "th" ? "สร้างงานซ่อมต่อเนื่อง" : "Create repair ticket"}</button></div>
    </form>
    <div class="routine-attachments">${attachments.map(att => att.file_type === "image" ? `<img src="${att.stamped_file_url || att.file_url}" alt="routine evidence">` : `<video src="${att.file_url}" controls></video>`).join("") || `<small>${currentLang === "th" ? "ยังไม่มีหลักฐาน" : "No evidence yet"}</small>`}</div>
    <div class="routine-log-mini"><strong>Routine Logs</strong>${logs.map(log => `<small>${new Date(log.created_at).toLocaleString(currentLang === "th" ? "th-TH" : "en-US")} · ${esc(log.action)} · ${esc(log.actor_name)}</small>`).join("")}</div>
  </div>`;
}
function renderRoutineDashboard() {
  generateRoutineTasksForDate(routineDashboardDate);
  const tasks = routineVisibleTasks("routineDashboard");
  const counts = Object.fromEntries(Object.keys(routineStatusMeta).map(key => [key, tasks.filter(task => task.status === key).length]));
  $("#routineDashboardView").innerHTML = `${routineToolbarHtml()}
    <div class="routine-stat-grid">
      <article><span>${currentLang === "th" ? "งานทั้งหมดวันนี้" : "Total"}</span><strong>${tasks.length}</strong></article>
      <article><span>${currentLang === "th" ? "ยังไม่เริ่ม" : "Not started"}</span><strong>${(counts.scheduled || 0) + (counts.ready || 0)}</strong></article>
      <article><span>${currentLang === "th" ? "กำลังทำ" : "In progress"}</span><strong>${counts.in_progress || 0}</strong></article>
      <article><span>${currentLang === "th" ? "รอตรวจรับ" : "Pending"}</span><strong>${(counts.pending_verification || 0) + (counts.submitted || 0) + (counts.late_submitted || 0)}</strong></article>
      <article><span>${currentLang === "th" ? "เสร็จสมบูรณ์" : "Completed"}</span><strong>${(counts.completed || 0) + (counts.approved || 0)}</strong></article>
      <article><span>${currentLang === "th" ? "เลยเวลา/ตีกลับ" : "Missed/Rejected"}</span><strong>${(counts.missed || 0) + (counts.rejected || 0) + (counts.rework || 0)}</strong></article>
    </div>
    <div class="routine-card-list">${tasks.map(task => routineTaskCard(task)).join("") || emptyState(currentLang === "th" ? "ไม่มีงานประจำวันที่ตรงเงื่อนไข" : "No routine tasks")}</div>`;
}
function renderRoutineToday() {
  const tasks = routineVisibleTasks("routineToday").sort((a, b) => a.start_time.localeCompare(b.start_time));
  $("#routineTodayView").innerHTML = `<div class="routine-mobile-title"><h3>${currentLang === "th" ? "งานวันนี้ของฉัน" : "My tasks today"}</h3><p>${formatDate(todayIso())}</p></div><div class="routine-card-list">${tasks.map(task => routineTaskCard(task)).join("") || emptyState(currentLang === "th" ? "วันนี้ยังไม่มีงานประจำ" : "No routine tasks today")}</div>`;
}
function renderRoutineVerify() {
  const tasks = routineVisibleTasks("routineVerify");
  $("#routineVerifyView").innerHTML = `${routineToolbarHtml()}<div class="routine-card-list">${tasks.map(task => routineTaskCard(task, { verify: true })).join("") || emptyState(currentLang === "th" ? "ไม่มีงานรอตรวจรับ" : "No tasks waiting for verification")}</div>`;
}
function renderRoutineTemplates() {
  $("#routineTemplatesView").innerHTML = `<div class="panel"><div class="panel-header"><div><h3>Routine Templates</h3><p>${currentLang === "th" ? "แม่แบบงานซ้ำ พร้อม Checklist และเงื่อนไขหลักฐาน" : "Recurring templates with checklist and evidence rules"}</p></div></div>
    <div class="routine-template-grid">${routineTemplates.map(template => `<article class="routine-template-card">
      <div><span>${routineDepartmentLabel(template.department)} · ${template.frequency}</span><h4>${esc(template.title)}</h4><p>${esc(template.area_name)} · ${template.start_time}-${template.end_time}</p></div>
      <small>📷 ${template.min_photos}-${template.max_photos} · 🎥 ${template.min_videos} · ${template.requires_meter_reading ? "Meter" : "No meter"}</small>
      <div class="inline-actions">${canManageRoutineTemplates() ? `<button class="secondary-btn" type="button" data-routine-duplicate-template="${template.id}">Duplicate</button><button class="secondary-btn" type="button" data-routine-toggle-template="${template.id}">${template.is_active ? "Disable" : "Enable"}</button>` : ""}</div>
    </article>`).join("")}</div></div>`;
}
function renderRoutineCalendar() {
  generateRoutineTasksForDate(routineDashboardDate);
  const days = Array.from({ length: 7 }, (_, index) => addDaysFromIso(todayIso(), index));
  $("#routineCalendarView").innerHTML = `<div class="routine-calendar-grid">${days.map(day => {
    const dayTasks = routineTasks.filter(task => task.task_date === day && routineTaskVisibleToUser(task));
    return `<article><strong>${formatDate(day)}</strong>${dayTasks.map(task => `<button type="button" data-routine-open="${task.id}"><span>${task.start_time}</span>${esc(task.title)}<small>${routineStatusLabel(task.status)}</small></button>`).join("") || `<p>${currentLang === "th" ? "ไม่มีงาน" : "No tasks"}</p>`}</article>`;
  }).join("")}</div>`;
}
function renderRoutineReports() {
  const tasks = routineTasks.filter(task => routineTaskVisibleToUser(task));
  const completed = tasks.filter(task => ["approved", "completed"].includes(task.status)).length;
  const late = tasks.filter(task => task.is_late || task.status === "late_submitted").length;
  const missed = tasks.filter(task => task.status === "missed").length;
  const rejected = tasks.filter(task => ["rejected", "rework"].includes(task.status)).length;
  const byDept = routineDepartments.map(dept => ({ dept, count: tasks.filter(task => task.department === dept.key).length }));
  $("#routineReportsView").innerHTML = `<div class="routine-stat-grid">
    <article><span>${currentLang === "th" ? "อัตราทำงานครบ" : "Completion"}</span><strong>${tasks.length ? Math.round(completed / tasks.length * 100) : 0}%</strong></article>
    <article><span>${currentLang === "th" ? "ส่งช้า" : "Late"}</span><strong>${late}</strong></article>
    <article><span>Missed</span><strong>${missed}</strong></article>
    <article><span>Reject</span><strong>${rejected}</strong></article>
  </div>
  <div class="panel"><div class="panel-header"><div><h3>${currentLang === "th" ? "รายงานแยกตามหมวด" : "By department"}</h3></div><button class="secondary-btn" id="exportRoutineCsvBtn" type="button">Export CSV</button></div>
  <div class="finance-table">${byDept.map(item => `<div class="finance-row"><span>${item.dept[currentLang]}</span><strong>${item.count}</strong><span>${tasks.length ? Math.round(item.count / tasks.length * 100) : 0}%</span><span></span></div>`).join("")}</div></div>`;
}
function renderRoutineSettings() {
  $("#routineSettingsView").innerHTML = `<div class="routine-settings-grid">
    <article><h4>${currentLang === "th" ? "หมวดงาน" : "Categories"}</h4><p>${routineDepartments.map(item => item[currentLang]).join(", ")}</p></article>
    <article><h4>${currentLang === "th" ? "พื้นที่ส่วนกลาง" : "Common areas"}</h4><p>ห้องไฟ, ห้องปั๊มน้ำ, ห้องขยะ, ฟิตเนส, Parking B1</p></article>
    <article><h4>${currentLang === "th" ? "ค่าอนุโลม Default" : "Default grace"}</h4><p>10 นาที</p></article>
    <article><h4>LINE Notification</h4><p>${currentLang === "th" ? "เตรียมโครงสร้าง Event ไว้แล้ว ใช้เป็น Notification เท่านั้น" : "Event structure ready for notification only"}</p></article>
  </div>`;
}
function ensureRoutineViews() {
  const anchor = $("#calendarView") || $("#listView");
  ["routineDashboard", "routineToday", "routineVerify", "routineTemplates", "routineCalendar", "routineReports", "routineSettings"].forEach(view => {
    if (!$(`#${view}View`)) anchor?.insertAdjacentHTML("afterend", `<section id="${view}View" class="view"></section>`);
  });
}
function renderRoutineModule() {
  ensureRoutineViews();
  if ($("#routineDashboardView")) renderRoutineDashboard();
  if ($("#routineTodayView")) renderRoutineToday();
  if ($("#routineVerifyView")) renderRoutineVerify();
  if ($("#routineTemplatesView")) renderRoutineTemplates();
  if ($("#routineCalendarView")) renderRoutineCalendar();
  if ($("#routineReportsView")) renderRoutineReports();
  if ($("#routineSettingsView")) renderRoutineSettings();
}
function financeRows() {
  return [
    { category: "ค่าส่วนกลาง", month: "฿ 742,000", year: "฿ 6,810,000", status: "ปกติ", tone: "paid" },
    { category: "ค่าซ่อมบำรุง", month: "฿ 186,500", year: "฿ 1,940,000", status: "เฝ้าระวัง", tone: "unpaid" },
    { category: "ค่าสาธารณูปโภค", month: "฿ 214,300", year: "฿ 2,320,000", status: "ปกติ", tone: "paid" },
    { category: "ผู้รับเหมา/บริการ", month: "฿ 98,000", year: "฿ 1,020,000", status: "ปกติ", tone: "paid" }
  ];
}
function renderFinance() {
  const canImport = canImportFinance();
  $("#financeView").innerHTML = `
    <div class="finance-grid">
      <article class="finance-card highlight"><span>รายรับปีนี้</span><strong>฿ 8.42M</strong><small>ค่าส่วนกลางและรายรับอื่น</small></article>
      <article class="finance-card"><span>รายจ่ายปีนี้</span><strong>฿ 6.78M</strong><small>ซ่อมบำรุง สาธารณูปโภค และบริการ</small></article>
      <article class="finance-card"><span>ค้างชำระ</span><strong>฿ 1.16M</strong><small>ห้องที่ยังไม่ชำระค่าส่วนกลาง</small></article>
      <article class="finance-card"><span>เงินสำรอง</span><strong>฿ 14.35M</strong><small>กองทุนและเงินสำรองอาคาร</small></article>
    </div>
    <div class="panel">
      <div class="panel-header">
        <div><h3>งบการเงินโดยสรุป</h3><p>${canImport ? "พนักงานนิติบุคคล คณะกรรมการ Admin และ Co-Admin สามารถ Import ได้" : "ลูกบ้านดูและ Export ได้ แต่ไม่สามารถ Import ได้"}</p></div>
        <div class="inline-actions">
          <button class="secondary-btn ${canImport ? "" : "hidden"}" id="financeImportBtn" type="button">Import CSV</button>
          <button class="secondary-btn" id="financeExportBtn" type="button">Export Report</button>
          <input id="financeImportInput" type="file" accept=".csv" hidden>
        </div>
      </div>
      <div class="finance-table">
        <div class="finance-row head"><span>หมวด</span><span>เดือนนี้</span><span>ปีนี้</span><span>สถานะ</span></div>
        ${financeRows().map(row => `<div class="finance-row"><span>${row.category}</span><strong>${row.month}</strong><strong>${row.year}</strong><i class="payment-badge ${row.tone}">${row.status}</i></div>`).join("")}
      </div>
    </div>`;
}
function selectedCalendarEvent() {
  return calendarEvents.find(item => item.id === selectedCalendarEventId) || calendarEvents[0];
}
function selectedVisibleCalendarEvent() {
  const item = calendarEvents.find(event => event.id === selectedCalendarEventId);
  if (!item) return null;
  const date = new Date(`${item.date}T00:00:00`);
  return date.getMonth() === selectedCalendarMonth && date.getFullYear() === selectedCalendarYear ? item : null;
}
function calendarEventsForDay(day) {
  return calendarEvents
    .filter(item => {
      const date = new Date(`${item.date}T00:00:00`);
      return item.day === Number(day) && date.getMonth() === selectedCalendarMonth && date.getFullYear() === selectedCalendarYear;
    })
    .sort((a, b) => a.time.localeCompare(b.time));
}
function todayCalendarEvents() {
  return calendarEventsForDay(27);
}
function upcomingCalendarEvents(limit = 5) {
  return calendarEvents
    .filter(item => item.day >= 27)
    .sort((a, b) => a.date.localeCompare(b.date) || a.time.localeCompare(b.time))
    .slice(0, limit);
}
function calendarMonthName() {
  return new Intl.DateTimeFormat(currentLang === "th" ? "th-TH" : "en-US", { month: "long" }).format(new Date(selectedCalendarYear, selectedCalendarMonth, 1));
}
function calendarMonthNameAt(month) {
  return new Intl.DateTimeFormat(currentLang === "th" ? "th-TH" : "en-US", { month: "short" }).format(new Date(selectedCalendarYear, month, 1));
}
function calendarYearLabel() {
  const year = currentLang === "th" ? selectedCalendarYear + 543 : selectedCalendarYear;
  return String(year);
}
function calendarYearOptionLabel(year) {
  return String(currentLang === "th" ? year + 543 : year);
}
function calendarPickerHtml() {
  if (calendarPickerMode === "month") {
    return `<div class="calendar-picker">${Array.from({ length: 12 }, (_, month) => `<button type="button" class="${month === selectedCalendarMonth ? "active" : ""}" data-calendar-month-option="${month}">${calendarMonthNameAt(month)}</button>`).join("")}</div>`;
  }
  if (calendarPickerMode === "year") {
    const start = selectedCalendarYear - 2;
    return `<div class="calendar-picker year-picker">${Array.from({ length: 7 }, (_, offset) => start + offset).map(year => `<button type="button" class="${year === selectedCalendarYear ? "active" : ""}" data-calendar-year-option="${year}">${calendarYearOptionLabel(year)}</button>`).join("")}</div>`;
  }
  return "";
}
function daysInSelectedMonth() {
  return new Date(selectedCalendarYear, selectedCalendarMonth + 1, 0).getDate();
}
function setCalendarMonthYear(month, year) {
  selectedCalendarMonth = Math.max(0, Math.min(11, Number(month)));
  selectedCalendarYear = Math.max(2000, Math.min(2100, Number(year)));
  const maxDay = daysInSelectedMonth();
  selectedCalendarDay = Math.min(selectedCalendarDay, maxDay);
  localStorage.setItem("juristicSelectedCalendarMonth", String(selectedCalendarMonth));
  localStorage.setItem("juristicSelectedCalendarYear", String(selectedCalendarYear));
  localStorage.setItem("juristicSelectedCalendarDay", String(selectedCalendarDay));
  const firstEvent = calendarEventsForDay(selectedCalendarDay)[0] || calendarEvents.find(item => {
    const date = new Date(`${item.date}T00:00:00`);
    return date.getMonth() === selectedCalendarMonth && date.getFullYear() === selectedCalendarYear;
  });
  if (firstEvent) {
    selectedCalendarEventId = firstEvent.id;
    selectedCalendarDay = firstEvent.day;
    localStorage.setItem("juristicSelectedCalendarEvent", selectedCalendarEventId);
    localStorage.setItem("juristicSelectedCalendarDay", String(selectedCalendarDay));
  }
}
function shiftCalendarMonth(delta) {
  const date = new Date(selectedCalendarYear, selectedCalendarMonth + delta, 1);
  setCalendarMonthYear(date.getMonth(), date.getFullYear());
  renderCalendar();
}
function goToTodayCalendar() {
  const now = new Date();
  selectedCalendarMonth = now.getMonth();
  selectedCalendarYear = now.getFullYear();
  selectedCalendarDay = now.getDate();
  localStorage.setItem("juristicSelectedCalendarMonth", String(selectedCalendarMonth));
  localStorage.setItem("juristicSelectedCalendarYear", String(selectedCalendarYear));
  localStorage.setItem("juristicSelectedCalendarDay", String(selectedCalendarDay));
  const firstEvent = calendarEventsForDay(selectedCalendarDay)[0];
  if (firstEvent) {
    selectedCalendarEventId = firstEvent.id;
    localStorage.setItem("juristicSelectedCalendarEvent", selectedCalendarEventId);
  }
  renderCalendar();
}
function promptCalendarMonth() {
  const value = prompt(currentLang === "th" ? "เลือกเดือน (1-12)" : "Choose month (1-12)", String(selectedCalendarMonth + 1));
  if (!value) return;
  const month = Number(value) - 1;
  if (Number.isInteger(month) && month >= 0 && month <= 11) {
    setCalendarMonthYear(month, selectedCalendarYear);
    renderCalendar();
  } else {
    showToast(currentLang === "th" ? "กรุณาระบุเดือน 1-12" : "Please enter month 1-12");
  }
}
function promptCalendarYear() {
  const value = prompt(currentLang === "th" ? "เลือกปี ค.ศ. เช่น 2026" : "Choose year, e.g. 2026", String(selectedCalendarYear));
  if (!value) return;
  const year = Number(value);
  if (Number.isInteger(year) && year >= 2000 && year <= 2100) {
    setCalendarMonthYear(selectedCalendarMonth, year);
    renderCalendar();
  } else {
    showToast(currentLang === "th" ? "กรุณาระบุปี ค.ศ. 2000-2100" : "Please enter year 2000-2100");
  }
}
function icsDateTime(event) {
  return `${event.date.replaceAll("-", "")}T${event.time.replace(":", "")}00`;
}
function buildCalendarIcs() {
  const stamp = new Date().toISOString().replace(/[-:]/g, "").split(".")[0] + "Z";
  const events = calendarEvents.map(event => [
    "BEGIN:VEVENT",
    `UID:${event.id}@juristic-care.local`,
    `DTSTAMP:${stamp}`,
    `DTSTART:${icsDateTime(event)}`,
    `DTEND:${icsDateTime({ ...event, time: event.time.replace(/(\d{2}):(\d{2})/, (_, h, m) => `${String(Math.min(Number(h) + 1, 23)).padStart(2, "0")}:${m}`) })}`,
    `SUMMARY:${getText(event.title)}`,
    `LOCATION:${getText(event.location)}`,
    `DESCRIPTION:${getText(event.detail)}`,
    "END:VEVENT"
  ].join("\r\n")).join("\r\n");
  return ["BEGIN:VCALENDAR", "VERSION:2.0", "PRODID:-//Juristic Care//Calendar Mockup//TH", "CALSCALE:GREGORIAN", "METHOD:PUBLISH", events, "END:VCALENDAR"].join("\r\n");
}
async function shareCalendarToPhone() {
  const fileName = "juristic-care-calendar.ics";
  const ics = buildCalendarIcs();
  const blob = new Blob([ics], { type: "text/calendar;charset=utf-8" });
  if (typeof File !== "undefined" && navigator.share && navigator.canShare) {
    const file = new File([blob], fileName, { type: "text/calendar" });
    try {
      if (navigator.canShare({ files: [file] })) {
        await navigator.share({
          title: "Juristic Care Calendar",
          text: currentLang === "th" ? "เพิ่มปฏิทินนิติบุคคลลงโทรศัพท์" : "Add Juristic Care calendar to your phone",
          files: [file]
        });
        showToast(currentLang === "th" ? "เปิดหน้าต่างแชร์ปฏิทินแล้ว" : "Calendar share sheet opened");
        return;
      }
    } catch (error) {}
  }
  downloadTextFile(fileName, ics, "text/calendar;charset=utf-8");
  showToast(currentLang === "th" ? "ดาวน์โหลดไฟล์ .ics สำหรับเพิ่มเข้า Calendar แล้ว" : "Downloaded .ics calendar file");
}
function renderCalendar() {
  const selected = selectedVisibleCalendarEvent();
  const eventByDay = new Map(calendarEvents.map(item => [item.day, calendarEventsForDay(item.day)]));
  const maxDay = daysInSelectedMonth();
  const cells = Array.from({ length: 35 }, (_, i) => {
    const day = i + 1;
    if (day > maxDay) return `<span></span>`;
    const dayEvents = eventByDay.get(day) || [];
    const isToday = day === 27 && selectedCalendarMonth === 5 && selectedCalendarYear === 2026;
    const classes = [isToday ? "today" : "", dayEvents.length ? "has-event" : "", day === selectedCalendarDay ? "selected-event" : ""].filter(Boolean).join(" ");
    return `<button class="${classes}" type="button" data-calendar-day="${day}"><strong>${day}</strong>${dayEvents.length ? `<small>${dayEvents.length} รายการ</small>` : ""}</button>`;
  }).join("");
  const selectedDayEvents = calendarEventsForDay(selectedCalendarDay);
  const eventList = selectedDayEvents.length
    ? selectedDayEvents.map(item => `<button type="button" class="${item.id === selected?.id ? "active" : ""}" data-calendar-event="${item.id}"><strong>${getText(item.title)}</strong><small>${formatDate(item.date)} · ${item.time}</small></button>`).join("")
    : `<div class="empty-state compact"><strong>${currentLang === "th" ? "วันนี้ไม่มีรายการ" : "No events this day"}</strong><span>${currentLang === "th" ? "เลือกวันที่มีจุดสีเพื่อดูรายการ" : "Choose a highlighted day to see events."}</span></div>`;
  const detailHtml = selected ? `
    <div class="calendar-detail-card">
      <strong>${getText(selected.title)}</strong>
      <small>${formatDate(selected.date)} · ${selected.time} · ${getText(selected.owner)}</small>
      <p>${getText(selected.detail)}</p>
      <span>${getText(selected.location)}</span>
    </div>
  ` : `<div class="calendar-detail-card empty-detail"><strong>${currentLang === "th" ? "ยังไม่ได้เลือกรายการ" : "No event selected"}</strong><p>${currentLang === "th" ? "เลือกวันที่มีรายการ แล้วคลิกรายการเพื่อดูรายละเอียด" : "Choose a day with events, then click an event to see details."}</p></div>`;
  $("#calendarView").innerHTML = `
    <div class="panel">
      <div class="panel-header">
        <div><h3>ปฏิทินนิติบุคคล</h3><p>${googleCalendarConnected ? "เชื่อมต่อ Google Calendar mock สำเร็จ พร้อมแสดงรายการในตาราง Calendar" : "กดเชื่อมต่อเพื่อทดสอบ Google Calendar mock และเลือกวันเพื่อดูรายละเอียด"}</p></div>
        <div class="inline-actions">
          <button class="secondary-btn" id="connectGoogleCalendarBtn" type="button">${googleCalendarConnected ? "เชื่อมต่อแล้ว" : "เชื่อมต่อ Google Calendar"}</button>
          <button class="secondary-btn" id="shareCalendarBtn" type="button">แชร์ปฏิทิน</button>
        </div>
      </div>
      <div class="calendar-detail-top">
        <h3>รายละเอียดรายการ</h3>
        ${detailHtml}
      </div>
      <div class="calendar-layout">
        <div class="calendar-box">
          <div class="calendar-month">
            <button class="icon-btn" type="button" data-calendar-prev aria-label="เดือนก่อนหน้า">‹</button>
            <div class="calendar-month-title">
              <button type="button" data-calendar-pick-month>${calendarMonthName()}</button>
              <button type="button" data-calendar-pick-year>${calendarYearLabel()}</button>
            </div>
            <button class="icon-btn" type="button" data-calendar-next aria-label="เดือนถัดไป">›</button>
            <button class="calendar-today-btn" type="button" data-calendar-today>วันนี้</button>
          </div>
          ${calendarPickerHtml()}
          <div class="calendar-grid calendar-grid-buttons">
            <b>จ</b><b>อ</b><b>พ</b><b>พฤ</b><b>ศ</b><b>ส</b><b>อา</b>${cells}
          </div>
        </div>
        <div class="calendar-events">
          <h3>รายการวันที่ ${selectedCalendarDay} ${calendarMonthName()} ${calendarYearLabel()}</h3>
          <div class="calendar-event-list">
            ${eventList}
          </div>
        </div>
      </div>
      <div class="calendar-summary-grid">
        <section class="calendar-summary-card">
          <h3>กิจกรรมทั้งหมดที่เกิดขึ้นในวันนี้</h3>
          ${todayCalendarEvents().map(item => `<button type="button" data-calendar-event="${item.id}"><strong>${getText(item.title)}</strong><small>${item.time} · ${getText(item.owner)}</small></button>`).join("") || `<p class="muted-line">ยังไม่มีกิจกรรมวันนี้</p>`}
        </section>
        <section class="calendar-summary-card">
          <h3>กิจกรรมที่กำลังจะมาถึง 5 กิจกรรม</h3>
          ${upcomingCalendarEvents().map(item => `<button type="button" data-calendar-event="${item.id}"><strong>${getText(item.title)}</strong><small>${formatDate(item.date)} · ${item.time}</small></button>`).join("")}
        </section>
      </div>
    </div>`;
}
function ensureBackhouseUploadModal() {
  if ($("#backhouseUploadModal")) return;
  document.body.insertAdjacentHTML("beforeend", `
    <div id="backhouseUploadModal" class="modal-backdrop hidden" role="dialog" aria-modal="true" aria-labelledby="backhouseUploadTitle">
      <div class="modal">
        <div class="modal-header">
          <div><span class="ticket-number">ANNOUNCEMENT</span><h2 id="backhouseUploadTitle">${currentLang === "th" ? "อัปโหลดประกาศ" : "Upload announcement"}</h2></div>
          <button class="icon-btn" data-close-backhouse-upload aria-label="${t("common.close")}">×</button>
        </div>
        <form id="backhouseUploadForm" class="create-form">
          <div class="form-row full">
            <label>${currentLang === "th" ? "หัวข้อที่จะแสดง" : "Display title"}<input name="title" required maxlength="120"></label>
          </div>
          <div class="form-row full">
            <label>${currentLang === "th" ? "เนื้อหาโดยย่อ" : "Short summary"}<textarea name="summary" rows="3" required maxlength="280"></textarea></label>
          </div>
          <div class="form-row full">
            <label>${currentLang === "th" ? "แนบเอกสารหรือรูปภาพ" : "Attach document or image"}<input name="announcementFile" type="file" accept="image/*,.pdf,application/pdf" required></label>
          </div>
          <p class="muted-line">${currentLang === "th" ? "ระบบจะแสดงหน้าแรกของเอกสารหรือรูปภาพนี้ในหน้าภาพรวมเสมอ" : "The dashboard always shows the first page or image as the preview."}</p>
          <div class="form-actions">
            <button type="button" class="secondary-btn" data-close-backhouse-upload>${currentLang === "th" ? "ยกเลิก" : "Cancel"}</button>
            <button type="submit" class="primary-btn">${currentLang === "th" ? "เพิ่มประกาศ" : "Add announcement"}</button>
          </div>
        </form>
      </div>
    </div>
  `);
}
function renderBackhouse() {
  ensureBackhouseView();
  ensureBackhouseUploadModal();
  const dirtyText = backhouseDirty ? "มีการเปลี่ยนแปลงที่ยังไม่บันทึก" : "รายการที่บันทึกแล้วจะแสดงในหน้า ภาพรวม";
  $("#backhouseView").innerHTML = `
    <div class="panel backhouse-panel">
      <div class="panel-header">
        <div><h3>${t("page.backhouse.title")}</h3><p>${dirtyText}</p></div>
        <div class="inline-actions">
          <button class="secondary-btn" id="openBackhouseUploadBtn" type="button">${currentLang === "th" ? "อัปโหลดประกาศ" : "Upload announcement"}</button>
          <button class="primary-btn" id="saveBackhouseBtn" type="button">${currentLang === "th" ? "บันทึก" : "Save"}</button>
        </div>
      </div>
      <div class="backhouse-list">
        ${announcements.map((item, index) => `<article class="backhouse-item" data-backhouse-item="${index}">
          ${announcementPreviewMarkup(item, "backhouse-preview")}
          <div><strong>${getText(item.title)}</strong><small>${formatDate(item.date)} · ${announcementTypeLabel(item)}</small><p>${getText(item.summary)}</p></div>
          <div class="backhouse-actions">
            <button class="secondary-btn" type="button" data-backhouse-move="up" data-index="${index}" ${index === 0 ? "disabled" : ""}>ขึ้น</button>
            <button class="secondary-btn" type="button" data-backhouse-move="down" data-index="${index}" ${index === announcements.length - 1 ? "disabled" : ""}>ลง</button>
            <button class="danger-btn" type="button" data-backhouse-remove="${index}">ลบ</button>
          </div>
        </article>`).join("")}
      </div>
    </div>`;
}
function renderAll() {
  ensureDashboardLayout();
  const dashboardJobs = getDashboardRangeJobs("dashboard");
  const counts = getDashboardCounts(dashboardJobs);
  const open = counts.open;
  $("#poolBadge").textContent = jobs.filter(j => j.raw && j.source === "Google Form").length;
  $("#mobilePoolBadge").textContent = $("#poolBadge").textContent;
  $("#statWaiting").textContent = open;
  $("#statProgress").textContent = counts.received;
  $("#statDone").textContent = counts.completed;
  $("#todayWork").textContent = `${dashboardJobs.filter(j => normalizeStatus(j.status) !== "completed").length} ${currentLang === "th" ? "งาน" : "jobs"} (${dashboardRangeLabel()})`;
  $$(".quick-filter [data-dashboard-range]").forEach(btn => btn.classList.toggle("active", btn.dataset.dashboardRange === dashboardRange));
  renderNotifications();
  renderDashboardJobs();
  renderAnnouncements();
  renderMainCategoryOverview();
  renderProjectOrganization();
  renderActivities();
  renderCategories();
  renderFinance();
  renderCalendar();
  renderChat();
  renderRoutineModule();
  renderBackhouse();
  renderOrganization();
  renderPermissions();
  renderTeam();
  renderResidents();
  renderStaffLogs();
  renderLogs();
  if (!["dashboard", "team", "residents", "organization", "permissions", "backhouse", "staffLogs", "logs", "finance", "calendar", "chat", "routineDashboard", "routineToday", "routineVerify", "routineTemplates", "routineCalendar", "routineReports", "routineSettings"].includes(currentView)) renderJobList();
}
function jobRow(job, poolMode = false) {
  job = normalizeJob(job);
  const c = categoryMeta[job.category] || { icon: "?", color: "#b56b28", soft: "#fff0e8" };
  const assignee = getUser(job.assignee);
  const categoryText = `${mainCategoryLabel(job.mainCategory)} / ${categoryLabel(job.category)}`;
  const due = dueInfo(job);
  const dueBadge = due.state === "done" ? "" : `<span class="due-badge due-${due.state}">${esc(due.label)}</span>`;
  const badges = [
    due.state === "done" ? "" : due.label,
    job.attachments?.length ? "📷" : "",
    job.hasScheduleConflict ? "เวลาซ้อน" : "",
    job.subStatus === "waiting_owner_or_admin_verification" ? "รอตรวจสอบ" : "",
    job.noPinAvailable ? "ไม่มี PIN" : ""
  ].filter(Boolean).map(text => `<span class="raw-badge">${text}</span>`).join(" ");
  return `<article class="job-row" data-job-id="${job.id}">
    <span class="category-icon" style="color:${c.color};background:${c.soft}">${job.raw ? "RAW" : c.icon}</span>
    <div class="job-info"><div class="job-title">${getText(job.title)} ${job.raw ? `<span class="raw-badge">POOL</span>` : ""} ${badges}</div><div class="job-meta"><span>${job.id}</span><span>${formatDate(job.jobDate)} ${job.startTime || ""}${job.endTime ? `-${job.endTime}` : ""}</span><span>${job.roomNo}${job.building ? ` · ตึก ${job.building}` : ""}${job.floor ? ` · ชั้น ${job.floor}` : ""}</span><span>${job.contactName || "-"} ${job.contactPhone || ""}</span></div></div>
    <span class="priority ${job.priority}">${priorityLabel(job.priority)}</span>
    ${assignee ? `<div class="assignee"><span class="avatar">${initials(getUserName(assignee))}</span><span>${getUserName(assignee).split(" ")[0]}</span></div>` : `<div class="assignee muted">${t("common.unassigned")}</div>`}
    <span class="status-pill status-${job.status}">${statusLabel(job.status, job.subStatus)}</span>
    <span class="more">›</span>
  </article>`;
}
function renderDashboardJobs() {
  const mode = getViewMode("dashboard");
  $("#dashboardJobs").classList.toggle("card-view", mode === "grid");
  $("#dashboardJobs").innerHTML = getDashboardRangeJobs("dashboard").slice(0, 5).map(j => jobRow(j)).join("") || emptyState(t("empty.dashboardJobs"));
  syncViewSwitches();
}
function ensureAnnouncementUi() {
  const grid = document.querySelector(".dashboard-grid");
  if (grid) grid.classList.add("dashboard-grid-hidden");
  if ($("#announcementList")) return;
  const categoryPanel = document.querySelector(".category-panel");
  if (!categoryPanel) return;
  categoryPanel.insertAdjacentHTML("afterend", `
    <section class="panel announcement-panel">
      <div class="panel-header">
        <div><h3>${t("dashboard.announcements")}</h3><p>${t("dashboard.announcementsSub")}</p></div>
        <div class="announcement-count" id="announcementCount"></div>
      </div>
      <div id="announcementList" class="announcement-list"></div>
    </section>
  `);
  document.body.insertAdjacentHTML("beforeend", `
    <div id="announcementModal" class="modal-backdrop hidden" role="dialog" aria-modal="true" aria-labelledby="announcementTitle">
      <div class="modal announcement-modal">
        <div class="modal-header">
          <div><span class="eyebrow dark" id="announcementMeta">ANNOUNCEMENT</span><h2 id="announcementTitle">ประกาศ</h2></div>
          <div class="announcement-modal-header-actions">
            <button class="secondary-btn announcement-download-btn" type="button" data-announcement-download>${currentLang === "th" ? "ดาวน์โหลด" : "Download"}</button>
            <button class="icon-btn" data-close-announcement aria-label="${t("common.close")}">×</button>
          </div>
        </div>
        <div id="announcementBody" class="modal-body"></div>
        <div class="announcement-modal-actions">
          <button class="secondary-btn" type="button" data-announcement-prev>${currentLang === "th" ? "ย้อนกลับ" : "Previous"}</button>
          <span id="announcementPage"></span>
          <button class="primary-btn" type="button" data-announcement-next>${currentLang === "th" ? "ถัดไป" : "Next"}</button>
        </div>
      </div>
    </div>
  `);
  const downloadButton = document.querySelector("[data-announcement-download]");
  if (downloadButton && downloadButton.tagName !== "A") {
    const downloadLink = document.createElement("a");
    downloadLink.className = downloadButton.className;
    downloadLink.id = "announcementDownloadLink";
    downloadLink.href = "#";
    downloadLink.setAttribute("download", "");
    downloadLink.setAttribute("data-announcement-download", "");
    downloadLink.textContent = downloadButton.textContent;
    downloadButton.replaceWith(downloadLink);
  }
}
function renderAnnouncements() {
  ensureAnnouncementUi();
  const list = $("#announcementList");
  if (!list) return;
  $("#announcementCount").textContent = `${announcements.length} ${currentLang === "th" ? "ประกาศ" : "items"}`;
  list.innerHTML = announcements.map((item, index) => `
    <button class="announcement-card" type="button" data-open-announcement="${index}">
      ${announcementPreviewMarkup(item)}
      <span>${announcementTypeLabel(item)}</span>
      <strong>${getText(item.title)}</strong>
      <small>${formatDate(item.date)} · ${getText(item.audience)}</small>
      <p>${getText(item.summary)}</p>
    </button>
  `).join("");
}
function mainCategoryStatusCounts(items, mainCategory) {
  const scoped = items.map(normalizeJob).filter(job => job.mainCategory === mainCategory);
  return {
    total: scoped.length,
    completed: scoped.filter(job => normalizeStatus(job.status) === "completed").length,
    progress: scoped.filter(job => mainCategoryProgressStatuses.has(normalizeStatus(job.status))).length,
    open: scoped.filter(job => normalizeStatus(job.status) === "open").length
  };
}
function mainCategorySegment(label, value, total, type) {
  if (!value || !total) return "";
  const width = value / total * 100;
  return `<span class="main-segment ${type}" style="width:${width}%" title="${label}: ${value}"></span>`;
}
function mainCategoryStatChip(label, value, total, type) {
  const percent = total ? Math.round(value / total * 100) : 0;
  return `<div class="main-category-stat-chip ${type}">
    <span class="stat-dot"></span>
    <div class="main-category-stat-content"><strong>${value}</strong><small>${label}</small></div>
    <em>${percent}%</em>
  </div>`;
}
function renderMainCategoryOverview() {
  ensureMainCategoryOverviewUi();
  const container = $("#mainCategoryOverviewBars");
  if (!container) return;
  const scopedJobs = getDashboardRangeJobs("dashboard");
  const rows = [
    { key: "common", label: mainCategoryLabel("common") },
    { key: "resident", label: mainCategoryLabel("resident") }
  ];
  container.innerHTML = rows.map(row => {
    const counts = mainCategoryStatusCounts(scopedJobs, row.key);
    const range = dashboardRangeLabel();
    return `<article class="main-category-bar-row">
      <div class="main-category-bar-heading">
        <div><strong>${row.label}</strong><small>${counts.total} งานในช่วง${range}</small></div>
        <b>${counts.total}</b>
      </div>
      <div class="main-category-bar-body">
        <div class="main-category-bar-track ${counts.total ? "" : "empty"}" aria-label="${row.label}">
          ${mainCategorySegment("เสร็จแล้ว", counts.completed, counts.total, "completed")}
          ${mainCategorySegment("กำลังดำเนินการ", counts.progress, counts.total, "in-progress")}
          ${mainCategorySegment("เปิดงานใหม่", counts.open, counts.total, "open")}
          ${!counts.total ? `<span class="main-segment empty" style="width:100%"></span>` : ""}
        </div>
        <div class="main-category-stat-grid">
          ${mainCategoryStatChip("เสร็จแล้ว", counts.completed, counts.total, "completed")}
          ${mainCategoryStatChip("กำลังดำเนินการ", counts.progress, counts.total, "in-progress")}
          ${mainCategoryStatChip("เปิดงานใหม่", counts.open, counts.total, "open")}
        </div>
      </div>
    </article>`;
  }).join("");
}
function openAnnouncement(index, page = 0) {
  activeAnnouncementIndex = (index + announcements.length) % announcements.length;
  const item = announcements[activeAnnouncementIndex];
  activeAnnouncementPage = (page + item.pages.length) % item.pages.length;
  $("#announcementTitle").textContent = getText(item.title);
  $("#announcementMeta").textContent = `${formatDate(item.date)} · ${getText(item.audience)}`;
  const activePageItem = { ...item, pages: [item.pages[activeAnnouncementPage]] };
  $("#announcementBody").innerHTML = `${announcementPreviewMarkup(activePageItem, "announcement-large")}<p class="muted-line">${getText(item.summary)}</p>`;
  $("#announcementPage").textContent = `${activeAnnouncementPage + 1} / ${item.pages.length}`;
  const downloadBtn = $("#announcementDownloadLink") || $("[data-announcement-download]");
  if (downloadBtn) downloadBtn.textContent = currentLang === "th" ? "ดาวน์โหลด" : "Download";
  if (downloadBtn) {
    const info = announcementDownloadInfo(item, activeAnnouncementPage);
    downloadBtn.textContent = currentLang === "th" ? "ดาวน์โหลด" : "Download";
    if (!info || info.legacy) {
      downloadBtn.href = "#";
      downloadBtn.removeAttribute("download");
      downloadBtn.classList.add("disabled");
      downloadBtn.setAttribute("aria-disabled", "true");
      downloadBtn.title = currentLang === "th" ? "ไฟล์ PDF เดิมไม่ได้เก็บต้นฉบับไว้ กรุณาอัปโหลดใหม่" : "Original PDF was not stored. Please re-upload it.";
    } else {
      downloadBtn.href = info.href;
      downloadBtn.setAttribute("download", info.filename);
      downloadBtn.classList.remove("disabled");
      downloadBtn.removeAttribute("aria-disabled");
      downloadBtn.removeAttribute("title");
    }
  }
  $("#announcementModal").classList.remove("hidden");
}
function viewModeKey(scope) {
  return scope === "jobs" ? `jobs:${currentView}` : scope;
}
function getViewMode(scope) {
  return viewModes[viewModeKey(scope)] || "list";
}
function setViewMode(scope, mode) {
  viewModes[viewModeKey(scope)] = mode === "grid" ? "grid" : "list";
  localStorage.setItem("juristicViewModes", JSON.stringify(viewModes));
}
function syncViewSwitches() {
  $$("[data-view-switch]").forEach(group => {
    const mode = getViewMode(group.dataset.viewSwitch);
    group.querySelectorAll("[data-view-mode]").forEach(button => {
      const active = button.dataset.viewMode === mode;
      button.classList.toggle("active", active);
      button.setAttribute("aria-pressed", active ? "true" : "false");
    });
  });
}
function renderActivities() {
  const latest = [...adminLogs, ...staffLogs].sort((a, b) => new Date(b.at) - new Date(a.at)).slice(0, 4);
  $("#activityList").innerHTML = latest.map(log => `<div class="activity-item"><span class="avatar">LOG</span><div><p>${esc(log.action)}: ${esc(log.detail)}</p><time>${new Date(log.at).toLocaleString(currentLang === "th" ? "th-TH" : "en-US")}</time></div></div>`).join("") || `<div class="empty-state"><span>☷</span><strong>ยังไม่มี Log</strong></div>`;
}
function renderCategories() {
  const scopedDashboardJobs = getDashboardRangeJobs("dashboard").map(normalizeJob);
  const groups = [
    { key: "all", title: currentLang === "th" ? "รวมทั้งหมด" : "All jobs", filter: () => true },
    { key: "common", title: mainCategoryLabel("common"), filter: job => normalizeJob(job).mainCategory === "common" },
    { key: "resident", title: mainCategoryLabel("resident"), filter: job => normalizeJob(job).mainCategory === "resident" }
  ];
  const statusTiles = [
    { key: "open", label: currentLang === "th" ? "เปิดใหม่" : "Open", filter: job => normalizeStatus(job.status) === "open" },
    { key: "status-progress", label: currentLang === "th" ? "ดำเนินการ" : "In progress", filter: job => dashboardInProgressStatuses.has(normalizeStatus(job.status)) },
    { key: "completed", label: currentLang === "th" ? "เสร็จแล้ว" : "Done", filter: job => normalizeStatus(job.status) === "completed" },
    { key: "rejected", label: currentLang === "th" ? "ยกเลิก" : "Rejected", filter: job => normalizeStatus(job.status) === "rejected" }
  ];
  const categoryCounts = (items) => categoryOrder
    .map(key => ({ key, count: items.filter(job => job.category === key).length }))
    .filter(item => item.count > 0)
    .sort((a, b) => b.count - a.count);
  const categoryCountsAll = (items) => categoryOrder
    .map(key => ({ key, count: items.filter(job => job.category === key).length }));
  const categorySummary = (items) => {
    const counts = categoryCountsAll(items);
    return counts.map(item => {
      const c = categoryMeta[item.key];
      return `<span class="${item.count ? "" : "zero"}" style="--tone:${c.color};--soft:${c.soft}">${c.icon} ${categoryLabel(item.key)} <b>${item.count}</b></span>`;
    }).join("");
  };
  const categoryStack = (items) => {
    const total = items.length;
    const counts = categoryCounts(items).slice(0, 5);
    if (!total) return `<div class="category-stack empty"></div>`;
    return `<div class="category-stack">${counts.map(item => {
      const c = categoryMeta[item.key];
      const width = Math.max(8, Math.round((item.count / total) * 100));
      return `<i title="${categoryLabel(item.key)} ${item.count}" style="--tone:${c.color};width:${width}%"></i>`;
    }).join("")}</div>`;
  };
  $("#categoryBars").innerHTML = groups.map(group => {
    const groupJobs = scopedDashboardJobs.filter(group.filter);
    const total = groupJobs.length;
    const top = categoryCounts(groupJobs)[0];
    const topMeta = top ? categoryMeta[top.key] : null;
    return `<article class="dashboard-category-set">
      <div class="dashboard-category-head">
        <div>
          <span class="category-kicker">${currentLang === "th" ? "ประเภทงาน" : "Work group"}</span>
          <h4>${group.title}</h4>
          <small>${dashboardRangeLabel()} · ${total} ${currentLang === "th" ? "รายการ" : "items"}</small>
        </div>
        <b>${total}</b>
      </div>
      <div class="category-highlight ${top ? "" : "empty"}">
        <span>${topMeta ? topMeta.icon : "—"}</span>
        <div>
          <strong>${top ? categoryLabel(top.key) : (currentLang === "th" ? "ยังไม่มีข้อมูล" : "No data")}</strong>
          <small>${top ? `${top.count} ${currentLang === "th" ? "รายการที่พบมากสุด" : "top items"}` : (currentLang === "th" ? "ไม่มีงานในช่วงนี้" : "No jobs in range")}</small>
        </div>
      </div>
      ${categoryStack(groupJobs)}
      <div class="dashboard-category-chips">${categorySummary(groupJobs)}</div>
      <div class="category-status-grid">
        ${statusTiles.map(tile => {
          const count = groupJobs.filter(tile.filter).length;
          const percent = total ? Math.round((count / total) * 100) : 0;
          return `<div class="category-status-tile ${tile.key}"><strong>${count}</strong><span>${tile.label}</span><small>${percent}%</small></div>`;
        }).join("")}
      </div>
    </article>`;
  }).join("");
}
function organizationEligibleUsers() {
  return activeUsers().filter(user => ["committee", "juristic", "technician"].includes(inferDepartment(user)) && user.role !== "resident");
}
function countOrganizationAssignmentsByDept(assignments, dept) {
  const prefix = `${dept}-`;
  return Object.entries(assignments || {}).filter(([slotId, userId]) => userId && slotId.startsWith(prefix)).length;
}
function organizationSlotTitle(dept, index, total) {
  if (dept === "committee") return total > 1 ? `คณะกรรมการ ${index}` : "คณะกรรมการ";
  if (dept === "juristic") {
    if (index === 1) return "ผู้จัดการอาคาร";
    if (index === 2) return "ผู้ช่วยผู้จัดการอาคาร";
    return `พนักงานนิติบุคคล ${index - 2}`;
  }
  if (dept === "technician") return index === 1 ? "หัวหน้าช่างอาคาร" : `ช่างอาคาร ${index - 1}`;
  return departmentLabel(dept);
}
function organizationSlotRow(dept, index) {
  if (dept === "committee") return 1;
  if (dept === "juristic") return index === 1 ? 2 : index === 2 ? 3 : 4;
  if (dept === "technician") return index === 1 ? 3 : 4;
  return 4;
}
function makeOrganizationSlots() {
  const users = organizationEligibleUsers();
  const assignments = { ...organizationAssignments, ...organizationDraftAssignments };
  return ["committee", "juristic", "technician"].flatMap(dept => {
    const userCount = users.filter(user => inferDepartment(user) === dept).length;
    const assignedCount = countOrganizationAssignmentsByDept(assignments, dept);
    const total = Math.max(userCount, assignedCount, 0);
    return Array.from({ length: total }, (_, index) => ({
      id: `${dept}-${index + 1}`,
      row: organizationSlotRow(dept, index + 1),
      title: organizationSlotTitle(dept, index + 1, total),
      dept
    }));
  });
}
function organizationRows() {
  const slots = makeOrganizationSlots();
  return [1, 2, 3, 4].map(row => slots.filter(slot => slot.row === row));
}
function organizationUserCard(user, draggable = false) {
  if (!user) return "";
  return `<div class="org-user-card" ${draggable ? `draggable="true" data-org-drag-user="${user.id}"` : ""}>
    ${profileMarkup(user)}
    <div><strong>${getUserName(user)}</strong><small>${departmentLabel(inferDepartment(user))}${user.position ? ` · ${esc(user.position)}` : ""}</small></div>
  </div>`;
}
function organizationSlotCard(slot, compact = false, assignments = organizationAssignments) {
  const user = getUser(assignments[slot.id]);
  return `<div class="org-slot ${user ? "filled" : ""} ${compact ? "compact" : ""}" data-org-slot="${slot.id}" data-accept-dept="${slot.dept}">
    <span class="org-slot-title">${slot.title}</span>
    ${user ? organizationUserCard(user) : `<div class="org-empty"><strong>ว่าง</strong><small>ลาก ${departmentLabel(slot.dept)} มาใส่ตำแหน่งนี้</small></div>`}
    ${user && !compact ? `<button class="link-btn danger-text" type="button" data-org-clear="${slot.id}">ล้าง Slot</button>` : ""}
  </div>`;
}
function ensureProjectOrganizationUi() {
  if ($("#projectOrganizationPanel")) return;
  const announcementPanel = document.querySelector(".announcement-panel");
  const html = `
    <section class="panel project-organization-panel" id="projectOrganizationPanel">
      <div class="panel-header">
        <div><h3>ทีมบริหารโครงการ</h3><p>Organization Chart ของคณะกรรมการและทีมงานนิติบุคคล</p></div>
      </div>
      <div id="projectOrganizationChart" class="project-organization-chart"></div>
    </section>`;
  if (announcementPanel) announcementPanel.insertAdjacentHTML("afterend", html);
  else $("#dashboardView")?.insertAdjacentHTML("beforeend", html);
}
function renderProjectOrganization() {
  ensureProjectOrganizationUi();
  const chart = $("#projectOrganizationChart");
  if (!chart) return;
  chart.innerHTML = organizationRows().map((slots, rowIndex) => `
    <div class="org-row org-row-${rowIndex + 1}">
      ${slots.map(slot => organizationSlotCard(slot, true)).join("")}
    </div>
  `).join("");
}
function renderOrganization() {
  ensureOrganizationView();
  const view = $("#organizationView");
  if (!view) return;
  const assignedDraftUserIds = new Set(Object.values(organizationDraftAssignments).filter(Boolean));
  const eligible = organizationEligibleUsers().filter(user => !assignedDraftUserIds.has(user.id));
  const grouped = ["committee", "juristic", "technician"].map(dept => ({
    dept,
    users: eligible.filter(user => inferDepartment(user) === dept)
  }));
  view.innerHTML = `
    <div class="organization-layout">
      <section class="panel org-palette-panel">
        <div class="panel-header"><div><h3>โปรไฟล์ที่เลือกได้</h3><p>แสดงเฉพาะ คณะกรรมการ / เจ้าหน้าที่นิติบุคคล / เจ้าหน้าที่ช่างอาคาร จาก Sidebar ทีมงาน</p></div></div>
        <div class="org-palette">
          ${grouped.map(group => `<div class="org-palette-group"><h4>${departmentLabel(group.dept)}</h4>${group.users.map(user => organizationUserCard(user, true)).join("") || `<p class="muted-line">ยังไม่มีผู้ใช้งานในแผนกนี้</p>`}</div>`).join("")}
        </div>
      </section>
      <section class="panel org-slots-panel">
        <div class="panel-header">
          <div><h3>Organization Slots</h3><p>ลากโปรไฟล์จากซ้ายมาใส่ Slot ตามตำแหน่งโครงสร้างทีมบริหารโครงการ ต้องกดบันทึกก่อนจึงจะแสดงในหน้าภาพรวม</p></div>
          <div class="inline-actions org-slot-toolbar">
            <span class="org-dirty-badge ${organizationDirty ? "dirty" : ""}">${organizationDirty ? "ยังไม่บันทึก" : "บันทึกแล้ว"}</span>
            <button class="secondary-btn" type="button" id="clearOrganizationBtn">ล้างโปรไฟล์</button>
            <button class="primary-btn" type="button" id="saveOrganizationBtn">บันทึก</button>
          </div>
        </div>
        <div class="organization-chart-editor">
          ${organizationRows().map((slots, rowIndex) => `<div class="org-row org-row-${rowIndex + 1}">${slots.map(slot => organizationSlotCard(slot, false, organizationDraftAssignments)).join("")}</div>`).join("")}
        </div>
      </section>
    </div>`;
}
function renderTeam() {
  $("#teamDepartmentFilter").innerHTML = `<option value="all">${currentLang === "th" ? "ทุกแผนก" : "All departments"}</option>` + departments.map(dept => `<option value="${dept.key}" ${teamDepartmentFilter === dept.key ? "selected" : ""}>${dept[currentLang]}</option>`).join("");
  $("#teamSearchInput").value = teamSearch;
  const pendingDeleteJobs = jobs.filter(job => job.deleteRequest?.status === "pending");
  const pendingPanel = pendingDeleteJobs.length && currentUser.role === "admin"
    ? `<div class="delete-request-panel"><strong>${currentLang === "th" ? "คำขอลบผู้ใช้งานจาก Co-Admin" : "Delete requests from Co-Admin"}</strong>${pendingDeleteJobs.map(job => {
      const target = getUser(job.deleteRequest.userId);
      return `<div class="delete-request-row"><span>${getUserName(target)} · ${job.id}</span><button class="danger-btn" data-confirm-delete="${job.deleteRequest.userId}" data-request-job="${job.id}">${currentLang === "th" ? "ยืนยันลบและ Export CSV" : "Confirm delete & export CSV"}</button></div>`;
    }).join("")}</div>` : "";
  $("#teamGrid").insertAdjacentHTML ? null : null;
  $("#teamGrid").parentElement.querySelector(".delete-request-panel")?.remove();
  if (pendingPanel) $("#teamGrid").insertAdjacentHTML("beforebegin", pendingPanel);
  const teamMode = getViewMode("team");
  $("#teamGrid").classList.toggle("card-view", teamMode === "grid");
  $("#teamGrid").innerHTML = activeUsers().filter(matchesTeamFilters).map(u => {
    const editable = canManageTeam() && u.role !== "admin";
    const deptValue = inferDepartment(u);
    const coAdminBadge = u.isCoAdmin ? ` · ${t("role.coAdmin")}` : "";
    const assignBadge = u.assignL2 ? " · L2" : u.assignL1 ? " · L1" : u.canAssign ? " · Assign" : "";
    const teamBadge = deptValue ? ` · ${departmentLabel(deptValue)}` : u.team ? ` · ${categoryLabel(u.team)}` : "";
    const residentBadge = u.role === "resident" ? ` · ${u.room}` : "";
    const nameParts = splitFullName(u);
    const adminGrant = canGrantCoAdmin() && u.role !== "resident"
      ? `<button class="team-mini-btn" data-toggle-coadmin="${u.id}">${u.isCoAdmin ? (currentLang === "th" ? "ถอด Co-Admin" : "Revoke Co-Admin") : (currentLang === "th" ? "ตั้งเป็น Co-Admin" : "Make Co-Admin")}</button>`
      : "";
    const editTools = editable ? `<div class="team-actions">
      <input type="text" data-team-first="${u.id}" value="${nameParts.firstName}" placeholder="${currentLang === "th" ? "ชื่อ" : "First name"}">
      <input type="text" data-team-last="${u.id}" value="${nameParts.lastName}" placeholder="${currentLang === "th" ? "นามสกุล" : "Last name"}">
      <input type="text" data-team-nick="${u.id}" value="${u.nickName || ""}" placeholder="${currentLang === "th" ? "ชื่อเล่น" : "Nickname"}">
      <input type="text" data-team-position="${u.id}" value="${u.position || ""}" placeholder="${currentLang === "th" ? "ตำแหน่ง" : "Position"}">
      <select data-team-department="${u.id}">${departments.map(dept => `<option value="${dept.key}" ${deptValue === dept.key ? "selected" : ""}>${dept[currentLang]}</option>`).join("")}</select>
      <label class="team-check"><input type="checkbox" data-toggle-l1="${u.id}" ${u.assignL1 ? "checked" : ""}> ${currentLang === "th" ? "มอบหมายงาน L1" : "Assign L1"}</label>
      <label class="team-check"><input type="checkbox" data-toggle-l2="${u.id}" ${u.assignL2 ? "checked" : ""}> ${currentLang === "th" ? "มอบหมายงาน L2" : "Assign L2"}</label>
      <label class="team-mini-btn">${currentLang === "th" ? "เปลี่ยนรูป/ถ่ายรูป" : "Photo"}<input type="file" data-team-photo="${u.id}" accept="image/*" hidden></label>
      ${adminGrant}
      <button class="team-mini-btn save" data-save-team="${u.id}">${currentLang === "th" ? "บันทึก" : "Save"}</button>
      <button class="team-mini-btn danger" data-delete-user="${u.id}">${currentLang === "th" ? "ลบผู้ใช้" : "Delete"}</button>
    </div>` : "";
    const adminCredentials = currentUser.role === "admin" ? `<div class="admin-credential-row">
      <label>ID<input data-admin-user-id="${u.id}" value="${u.room || ""}"></label>
      <label>${currentLang === "th" ? "รหัสผ่าน" : "Password"}<input data-admin-password="${u.id}" value="${u.password || ""}"></label>
    </div>` : "";
    return `<div class="team-card editable-team-card">${profileMarkup(u)}<div><strong>${getUserName(u)}</strong><small>${roleLabel(u)}${coAdminBadge}${assignBadge}${teamBadge}${residentBadge}${u.nickName ? ` · ${u.nickName}` : ""}${u.position ? ` · ${u.position}` : ""}</small>${adminCredentials}${editTools}</div><span class="availability"></span></div>`;
  }).join("");
  syncViewSwitches();
}
function renderLogs() {
  if ($("#adminLogDateFilter")) $("#adminLogDateFilter").value = adminLogDateFilter;
  if ($("#adminLogRoleFilter")) $("#adminLogRoleFilter").value = adminLogRoleFilter;
  const filtered = adminLogs.filter(log => {
    const dateOk = !adminLogDateFilter || log.at?.slice(0, 10) === adminLogDateFilter;
    const role = log.actorRole || "admin";
    const roleOk = adminLogRoleFilter === "all" || role === adminLogRoleFilter;
    return dateOk && roleOk;
  });
  $("#adminLogList").innerHTML = filtered.map(logRow).join("") || emptyState("ยังไม่มี Admin Log");
}
function logRow(log) {
  const roleText = log.actorRole === "coadmin" ? "Co-Admin" : log.actorRole === "staff" ? "Staff" : log.actorRole === "admin" ? "Admin" : "System";
  return `<div class="log-row"><time>${new Date(log.at).toLocaleString(currentLang === "th" ? "th-TH" : "en-US")}</time><strong>${esc(log.action)}<br><small>${esc(log.detail)}</small></strong><span>${esc(log.actor)}<br><small>${roleText}</small></span></div>`;
}
function renderStaffLogs() {
  $("#staffLogList").innerHTML = staffLogs.map(logRow).join("") || emptyState(currentLang === "th" ? "ยังไม่มี Staff Log" : "No staff log yet");
}
function matchesPermissionFilters(user) {
  if (user.role === "admin") return false;
  const q = normalizeSearchText(permissionSearch);
  const dept = inferDepartment(user);
  const haystack = normalizeSearchText(`${getUserName(user)} ${user.name || ""} ${user.enName || ""} ${user.nickName || ""} ${user.room || ""} ${departmentLabel(dept)}`);
  const queryOk = !q || haystack.includes(q);
  const deptOk = permissionDepartmentFilter === "all" || dept === permissionDepartmentFilter;
  return queryOk && deptOk;
}
function permissionState(user, type, key) {
  const permissions = userPermissions(user);
  const scoped = permissions[type] || {};
  const customized = Object.prototype.hasOwnProperty.call(scoped, key);
  return {
    checked: customized ? !!scoped[key] : permissionDefault(user, type, key),
    customized
  };
}
function permissionSwitch(user, type, item) {
  const state = permissionState(user, type, item.key);
  return `<label class="permission-switch ${state.customized ? "customized" : ""}">
    <input type="checkbox" data-permission-user="${user.id}" data-permission-type="${type}" data-permission-key="${item.key}" ${state.checked ? "checked" : ""}>
    <span><strong>${permissionLabel(item)}</strong>${permissionDescription(item) ? `<small>${permissionDescription(item)}</small>` : ""}</span>
  </label>`;
}
const bulkPermissionGroups = [
  { key: "residents", label: { th: "ลูกบ้านทั้งหมด", en: "All residents" }, filter: user => user.role === "resident" },
  { key: "committee", label: { th: "คณะกรรมการ", en: "Committee" }, filter: user => inferDepartment(user) === "committee" },
  { key: "juristic", label: { th: "เจ้าหน้าที่นิติบุคคล", en: "Juristic staff" }, filter: user => inferDepartment(user) === "juristic" && user.role !== "admin" },
  { key: "technician", label: { th: "เจ้าหน้าที่ช่างอาคาร", en: "Technicians" }, filter: user => inferDepartment(user) === "technician" },
  { key: "housekeeping", label: { th: "แม่บ้าน", en: "Housekeeping" }, filter: user => inferDepartment(user) === "housekeeping" },
  { key: "security_staff", label: { th: "พนักงานรักษาความปลอดภัย", en: "Security" }, filter: user => inferDepartment(user) === "security_staff" },
  { key: "gardener", label: { th: "คนสวน", en: "Gardeners" }, filter: user => inferDepartment(user) === "gardener" },
  { key: "contractor", label: { th: "ผู้รับเหมา", en: "Contractors" }, filter: user => inferDepartment(user) === "contractor" },
  { key: "staff", label: { th: "ทีมงานทั้งหมด", en: "All staff" }, filter: user => user.role === "staff" && user.role !== "admin" }
];
const bulkPermissionPresets = {
  residentStandard: {
    label: { th: "สิทธิ์ลูกบ้านมาตรฐาน", en: "Resident standard" },
    sidebar: ["dashboard", "commonWork", "roomjobs", "calendar", "finance", "chat"],
    actions: []
  },
  residentPublicOnly: {
    label: { th: "ลูกบ้านดูเฉพาะข้อมูลส่วนกลาง", en: "Resident public-only" },
    sidebar: ["dashboard", "commonWork", "roomjobs", "calendar", "chat"],
    actions: []
  },
  residentRoomOnly: {
    label: { th: "ลูกบ้านดูเฉพาะห้องของฉัน", en: "Resident room-only" },
    sidebar: ["dashboard", "roomjobs", "chat"],
    actions: []
  },
  staffBasic: {
    label: { th: "สิทธิ์ทีมงานพื้นฐาน", en: "Staff basic" },
    sidebar: ["dashboard", "commonWork", "myjobs", "calendar", "chat", "routineToday"],
    actions: ["createJob"]
  },
  juristicOperator: {
    label: { th: "สิทธิ์เจ้าหน้าที่นิติ", en: "Juristic operator" },
    sidebar: ["dashboard", "commonWork", "residentWork", "finance", "calendar", "chat", "pool", "juristicTeam", "myjobs", "residents", "backhouse", "routineDashboard", "routineToday", "routineVerify", "routineTemplates", "routineCalendar", "routineReports", "routineSettings"],
    actions: ["assignL1", "editGoogleFormJob", "createJob", "manageResidents", "manageAnnouncements", "importExport", "manageRoutineTemplates", "verifyRoutineTasks"]
  },
  technicianBasic: {
    label: { th: "สิทธิ์ช่างอาคาร", en: "Technician basic" },
    sidebar: ["dashboard", "commonWork", "myjobs", "technicianJobs", "calendar", "routineToday", "routineReports"],
    actions: ["createJob"]
  },
  committeeView: {
    label: { th: "สิทธิ์คณะกรรมการดูภาพรวม", en: "Committee overview" },
    sidebar: ["dashboard", "commonWork", "residentWork", "finance", "calendar", "chat", "backhouse", "routineDashboard", "routineVerify", "routineReports"],
    actions: ["importExport", "verifyRoutineTasks"]
  },
  clearAll: {
    label: { th: "ปิดสิทธิ์ทั้งหมด", en: "Clear all access" },
    sidebar: [],
    actions: []
  }
};
const bulkLabel = (item) => item?.label?.[currentLang] || item?.label?.th || item?.key || "";
const bulkPermissionModes = {
  replace: {
    label: { th: "แทนที่สิทธิ์เดิมตาม Preset", en: "Replace with preset" },
    description: { th: "ตั้งค่าทั้ง Sidebar และ Action ให้ตรงกับ Preset นี้ทั้งหมด", en: "Set sidebar and action permissions exactly to this preset." }
  },
  grant: {
    label: { th: "เพิ่มสิทธิ์จาก Preset เท่านั้น", en: "Grant preset only" },
    description: { th: "เปิดสิทธิ์ใน Preset เพิ่ม โดยไม่ปิดสิทธิ์เดิมที่มีอยู่", en: "Turn on preset permissions without disabling existing permissions." }
  },
  revoke: {
    label: { th: "ปิดสิทธิ์ที่อยู่ใน Preset", en: "Revoke preset permissions" },
    description: { th: "ปิดเฉพาะสิทธิ์ที่อยู่ใน Preset นี้ โดยไม่แตะสิทธิ์อื่น", en: "Turn off only permissions included in this preset." }
  }
};
const bulkModeLabel = (key = bulkPermissionMode) => bulkLabel(bulkPermissionModes[key] || bulkPermissionModes.replace);
const bulkModeDescription = (key = bulkPermissionMode) => (bulkPermissionModes[key]?.description?.[currentLang] || bulkPermissionModes[key]?.description?.th || "");
function bulkPermissionUsers() {
  const group = bulkPermissionGroups.find(item => item.key === bulkPermissionGroup) || bulkPermissionGroups[0];
  return activeUsers().filter(user => user.role !== "admin" && group.filter(user));
}
function buildPermissionsFromPreset(presetKey) {
  const preset = bulkPermissionPresets[presetKey] || bulkPermissionPresets.residentStandard;
  return {
    sidebar: Object.fromEntries(permissionSidebarItems.map(item => [item.key, preset.sidebar.includes(item.key)])),
    actions: Object.fromEntries(permissionActionItems.map(item => [item.key, preset.actions.includes(item.key)]))
  };
}
function mergePermissionsForBulkUser(user, presetKey, mode = bulkPermissionMode) {
  const presetPermissions = buildPermissionsFromPreset(presetKey);
  if (mode === "replace") return presetPermissions;
  const current = userPermissions(user);
  const next = {
    sidebar: Object.fromEntries(permissionSidebarItems.map(item => [item.key, permissionState(user, "sidebar", item.key).checked])),
    actions: Object.fromEntries(permissionActionItems.map(item => [item.key, permissionState(user, "actions", item.key).checked]))
  };
  ["sidebar", "actions"].forEach(type => {
    Object.entries(presetPermissions[type]).forEach(([key, enabled]) => {
      if (!enabled) return;
      next[type][key] = mode === "revoke" ? false : true;
    });
    Object.entries(current[type] || {}).forEach(([key, value]) => {
      if (!Object.prototype.hasOwnProperty.call(next[type], key)) next[type][key] = !!value;
    });
  });
  return next;
}
function bulkPermissionPreviewHtml() {
  const users = bulkPermissionUsers();
  const preset = bulkPermissionPresets[bulkPermissionPreset] || bulkPermissionPresets.residentStandard;
  const sidebarLabels = preset.sidebar.map(key => permissionLabel(permissionSidebarItems.find(item => item.key === key))).filter(Boolean);
  const actionLabels = preset.actions.map(key => permissionLabel(permissionActionItems.find(item => item.key === key))).filter(Boolean);
  const sampleUsers = users.slice(0, 12);
  return `<div class="bulk-preview">
    <div class="bulk-preview-head">
      <div><strong>${currentLang === "th" ? "บัญชีที่จะได้รับผล" : "Affected accounts"}: ${users.length}</strong><small>${bulkModeLabel()} · ${bulkModeDescription()}</small></div>
      <span>${bulkLabel(preset)}</span>
    </div>
    <div class="bulk-preview-chips">${sampleUsers.map(user => `<em>${esc(getUserName(user))}<small>${esc(user.room || user.id)}</small></em>`).join("") || `<p class="muted-line">${currentLang === "th" ? "ไม่พบผู้ใช้งานในกลุ่มนี้" : "No users in this group"}</p>`}${users.length > sampleUsers.length ? `<em>+${users.length - sampleUsers.length}</em>` : ""}</div>
    <div class="bulk-preview-permissions">
      <div><b>Sidebar</b><p>${sidebarLabels.join(", ") || "-"}</p></div>
      <div><b>Actions</b><p>${actionLabels.join(", ") || "-"}</p></div>
    </div>
  </div>`;
}
function renderBulkPermissionPanel() {
  return `<section class="panel bulk-permission-panel">
    <div class="panel-header">
      <div>
        <h3>${currentLang === "th" ? "กำหนดสิทธิ์แบบกลุ่ม" : "Bulk Permission"}</h3>
        <p>${currentLang === "th" ? "เลือกกลุ่มและ Preset แล้ว Apply ให้หลายบัญชีพร้อมกัน ลดการติ๊กทีละคน" : "Apply a permission preset to many accounts at once."}</p>
      </div>
    </div>
    <div class="bulk-permission-grid">
      <label>${currentLang === "th" ? "กลุ่มผู้ใช้งาน" : "User group"}
        <select id="bulkPermissionGroup">${bulkPermissionGroups.map(group => `<option value="${group.key}" ${bulkPermissionGroup === group.key ? "selected" : ""}>${bulkLabel(group)}</option>`).join("")}</select>
      </label>
      <label>${currentLang === "th" ? "Preset สิทธิ์" : "Permission preset"}
        <select id="bulkPermissionPreset">${Object.entries(bulkPermissionPresets).map(([key, preset]) => `<option value="${key}" ${bulkPermissionPreset === key ? "selected" : ""}>${bulkLabel(preset)}</option>`).join("")}</select>
      </label>
      <label>${currentLang === "th" ? "วิธีนำสิทธิ์ไปใช้" : "Apply mode"}
        <select id="bulkPermissionMode">${Object.entries(bulkPermissionModes).map(([key, mode]) => `<option value="${key}" ${bulkPermissionMode === key ? "selected" : ""}>${bulkLabel(mode)}</option>`).join("")}</select>
      </label>
      <div class="bulk-permission-actions">
        <button class="secondary-btn" type="button" id="previewBulkPermissionBtn">${currentLang === "th" ? "Preview" : "Preview"}</button>
        <button class="primary-btn" type="button" id="applyBulkPermissionBtn">${currentLang === "th" ? "ใช้สิทธิ์กับกลุ่มนี้" : "Apply to group"}</button>
      </div>
    </div>
    <div id="bulkPermissionPreview">${bulkPermissionPreviewHtml()}</div>
    <label class="bulk-confirm"><input type="checkbox" id="bulkPermissionConfirm"> ${currentLang === "th" ? "ยืนยันว่าต้องการบันทึกสิทธิ์ให้ผู้ใช้งานทั้งกลุ่มตามรายการ Preview ด้านบน" : "Confirm applying these permissions to the full previewed group."}</label>
  </section>`;
}
function applyBulkPermissions() {
  if (!canManagePermissions()) return;
  const usersToUpdate = bulkPermissionUsers();
  if (!usersToUpdate.length) return showToast(currentLang === "th" ? "ไม่พบผู้ใช้งานในกลุ่มนี้" : "No users found in this group");
  if (!$("#bulkPermissionConfirm")?.checked) return showToast(currentLang === "th" ? "กรุณาติ๊กยืนยันก่อนบันทึกสิทธิ์แบบกลุ่ม" : "Please confirm before applying bulk permissions");
  usersToUpdate.forEach(user => {
    const permissions = mergePermissionsForBulkUser(user, bulkPermissionPreset, bulkPermissionMode);
    user.permissions = JSON.parse(JSON.stringify(permissions));
    teamOverrides[user.id] = { ...(teamOverrides[user.id] || {}), permissions: user.permissions };
  });
  saveTeamOverrides();
  setupRoleMenu();
  renderPermissions();
  const group = bulkPermissionGroups.find(item => item.key === bulkPermissionGroup);
  const preset = bulkPermissionPresets[bulkPermissionPreset];
  addLog("BULK_PERMISSIONS_UPDATED", `${bulkLabel(preset)} / ${bulkModeLabel()} applied to ${bulkLabel(group)} (${usersToUpdate.length} accounts) by ${getUserName(currentUser)}`);
  showToast(currentLang === "th" ? `บันทึกสิทธิ์แบบกลุ่มแล้ว ${usersToUpdate.length} บัญชี` : `Bulk permissions saved for ${usersToUpdate.length} accounts`);
}
function renderPermissions() {
  ensurePermissionView();
  const view = $("#permissionsView");
  if (!view) return;
  const visibleUsers = activeUsers().filter(matchesPermissionFilters);
  view.innerHTML = `
    <div class="permissions-page">
      <section class="panel permission-intro">
        <div class="panel-header">
          <div>
            <h3>${currentLang === "th" ? "ศูนย์จัดการสิทธิ์" : "Permission Center"}</h3>
            <p>${currentLang === "th" ? "Admin และ Co-Admin สามารถเปิด/ปิด Sidebar และอำนาจการทำงานรายบุคคลได้โดยไม่ต้องแก้โค้ด" : "Admins and Co-Admins can grant sidebar access and action authority per user without editing code."}</p>
          </div>
        </div>
        <div class="permission-note-grid">
          <div><strong>Sidebar Access</strong><small>${currentLang === "th" ? "กำหนดว่า Account นี้จะเห็นเมนูด้านซ้ายใดบ้าง" : "Controls which left-sidebar menus this account can see."}</small></div>
          <div><strong>Action Authority</strong><small>${currentLang === "th" ? "กำหนดอำนาจ เช่น สร้างงาน มอบหมายงาน จัดการทีมงาน และ Import/Export" : "Controls powers such as creating, assigning, managing records, and import/export."}</small></div>
          <div><strong>Admin Safety</strong><small>${currentLang === "th" ? "Admin เข้าถึงทุกอย่างเสมอเพื่อป้องกันการล็อกตัวเองออกจากระบบ" : "Admins always keep full access to prevent lockout."}</small></div>
        </div>
      </section>
      ${renderBulkPermissionPanel()}
      <section class="panel">
        <div class="team-toolbar permission-toolbar">
          <div class="search-box"><span>⌕</span><input id="permissionSearchInput" placeholder="${currentLang === "th" ? "ค้นหาชื่อ ชื่อเล่น รหัสพนักงาน หรือเลขห้อง..." : "Search name, nickname, staff ID, or room..."}" value="${esc(permissionSearch)}"></div>
          <select id="permissionDepartmentFilter">
            <option value="all">${currentLang === "th" ? "ทุกแผนก / ทุกประเภท" : "All departments / types"}</option>
            ${departments.map(dept => `<option value="${dept.key}" ${permissionDepartmentFilter === dept.key ? "selected" : ""}>${dept[currentLang]}</option>`).join("")}
            <option value="resident" ${permissionDepartmentFilter === "resident" ? "selected" : ""}>${currentLang === "th" ? "ลูกบ้าน" : "Residents"}</option>
          </select>
        </div>
        <div class="permission-user-list">
          ${visibleUsers.map(user => `
            <article class="permission-user-card">
              <div class="permission-user-head">
                ${profileMarkup(user)}
                <div>
                  <strong>${getUserName(user)}</strong>
                  <small>${user.room || "-"} · ${roleLabel(user)} · ${departmentLabel(inferDepartment(user))}</small>
                </div>
                ${user.isCoAdmin ? `<span class="permission-badge">Co-Admin</span>` : ""}
              </div>
              <div class="permission-columns">
                <div>
                  <h4>${currentLang === "th" ? "สิทธิ์เข้า Sidebar" : "Sidebar access"}</h4>
                  <div class="permission-grid">${permissionSidebarItems.map(item => permissionSwitch(user, "sidebar", item)).join("")}</div>
                </div>
                <div>
                  <h4>${currentLang === "th" ? "อำนาจการทำงาน" : "Action authority"}</h4>
                  <div class="permission-grid">${permissionActionItems.map(item => permissionSwitch(user, "actions", item)).join("")}</div>
                </div>
              </div>
              <div class="permission-actions">
                <button class="secondary-btn" type="button" data-reset-permissions="${user.id}">${currentLang === "th" ? "กลับค่าเริ่มต้น" : "Reset default"}</button>
                <button class="primary-btn" type="button" data-save-permissions="${user.id}">${currentLang === "th" ? "บันทึกสิทธิ์" : "Save permissions"}</button>
              </div>
            </article>`).join("") || emptyState(currentLang === "th" ? "ไม่พบผู้ใช้งานตามเงื่อนไข" : "No users match these filters")}
        </div>
      </section>
    </div>`;
}
function collectPermissionValues(userId) {
  const permissions = { sidebar: {}, actions: {} };
  $$(`[data-permission-user="${userId}"]`).forEach(input => {
    const type = input.dataset.permissionType;
    const key = input.dataset.permissionKey;
    if (!permissions[type]) permissions[type] = {};
    permissions[type][key] = !!input.checked;
  });
  return permissions;
}
function saveUserPermissions(userId) {
  if (!canManagePermissions()) return;
  const user = getUser(userId);
  if (!user || user.role === "admin") return;
  const before = JSON.stringify(user.permissions || {});
  const permissions = collectPermissionValues(userId);
  user.permissions = permissions;
  teamOverrides[user.id] = { ...(teamOverrides[user.id] || {}), permissions };
  saveTeamOverrides();
  setupRoleMenu();
  renderPermissions();
  const after = JSON.stringify(permissions);
  addLog("PERMISSIONS_UPDATED", `${getUserName(user)} permissions updated by ${getUserName(currentUser)} — ${before} → ${after}`);
  showToast(currentLang === "th" ? "บันทึกสิทธิ์เรียบร้อยแล้ว" : "Permissions saved");
}
function resetUserPermissions(userId) {
  if (!canManagePermissions()) return;
  const user = getUser(userId);
  if (!user || user.role === "admin") return;
  delete user.permissions;
  if (teamOverrides[user.id]) delete teamOverrides[user.id].permissions;
  saveTeamOverrides();
  setupRoleMenu();
  renderPermissions();
  addLog("PERMISSIONS_RESET", `${getUserName(user)} permissions reset by ${getUserName(currentUser)}`);
  showToast(currentLang === "th" ? "คืนค่าสิทธิ์เริ่มต้นแล้ว" : "Permissions reset to default");
}
function residentDisplayName(person) {
  if (!person) return "-";
  const name = fullName(person.firstName, person.lastName);
  return person.nickName ? `${name} (${person.nickName})` : name;
}
function residentPeople(room) {
  return [room.owner, ...(room.occupants || [])].filter(person => person && (person.firstName || person.lastName || person.nickName || person.phone));
}
function residentPersonEntries(room) {
  return [
    { key: "owner", label: "เจ้าของร่วม", person: room.owner },
    ...(room.occupants || []).map((person, index) => ({ key: `occupant-${index}`, label: person.status === "tenant" ? "ผู้เช่า" : "ผู้อยู่อาศัย", person, index }))
  ].filter(item => item.person && (item.person.firstName || item.person.lastName || item.person.nickName || item.person.phone));
}
function primaryResident(room) {
  const entries = residentPersonEntries(room);
  const firstKey = room.personOrder?.[0];
  return entries.find(item => item.key === firstKey)?.person || entries[0]?.person || room.owner || room.occupants?.[0];
}
function residentCarsText(room) {
  return (room.cars || []).map(car => `${car.prefix || ""}-${car.number || ""} ${car.province || ""} ${[car.brand, car.model, car.color].filter(Boolean).join(" ")}`.trim()).join(", ") || "-";
}
function paymentLabel(status) {
  return status === "paid" ? (currentLang === "th" ? "ชำระแล้ว" : "Paid") : (currentLang === "th" ? "ยังไม่ชำระ" : "Unpaid");
}
function matchesResidentFilters(room) {
  const q = normalizeSearchText(residentSearch);
  const people = residentPeople(room).map(p => `${p.firstName} ${p.lastName} ${p.nickName} ${p.phone}`).join(" ");
  const haystack = normalizeSearchText(`${room.roomNo1} ${room.roomNo2} ${room.building || ""} ${people} ${residentCarsText(room)}`);
  const qOk = !q || haystack.includes(q);
  const buildingOk = residentBuildingFilter === "all" || (room.building || "-") === residentBuildingFilter;
  return qOk && buildingOk;
}
function renderResidents() {
  $("#residentSearchInput").value = residentSearch;
  const buildings = [...new Set(residentRooms.map(room => room.building || "-"))].sort((a, b) => a.localeCompare(b, "th"));
  $("#residentBuildingFilter").innerHTML = `<option value="all">${currentLang === "th" ? "ทุกตึก" : "All buildings"}</option>` + buildings.map(building => `<option value="${esc(building)}" ${residentBuildingFilter === building ? "selected" : ""}>${esc(building)}</option>`).join("");
  const rooms = residentRooms.filter(matchesResidentFilters);
  const totalPeople = residentRooms.reduce((sum, room) => sum + residentPeople(room).length, 0);
  const totalCars = residentRooms.reduce((sum, room) => sum + (room.cars || []).length, 0);
  $("#residentRoomCount").textContent = `${residentRooms.length}/882`;
  $("#residentPeopleCount").textContent = totalPeople;
  $("#residentCarCount").textContent = totalCars;
  $("#residentUnpaidCount").textContent = residentRooms.filter(room => room.commonFeeStatus === "unpaid").length;
  const mode = getViewMode("residents");
  $(".resident-table").classList.toggle("card-view", mode === "grid");
  $("#residentRoomList").innerHTML = rooms.map(room => {
    const primary = primaryResident(room);
    if (mode === "grid") {
      return `<button class="resident-card" type="button" data-open-resident="${room.id}">
        <span class="resident-card-room"><strong>${esc(room.roomNo1)}</strong><small>${esc(room.roomNo2)} · ตึก ${esc(room.building || "-")}</small></span>
        <span class="resident-card-person">${esc(residentDisplayName(primary))}</span>
        <span class="resident-card-meta">${esc(primary?.phone || "-")}</span>
        <span class="resident-card-meta">${esc(residentCarsText(room))}</span>
        <span><i class="payment-badge ${room.commonFeeStatus}">${paymentLabel(room.commonFeeStatus)}</i></span>
      </button>`;
    }
    return `<button class="resident-row" type="button" data-open-resident="${room.id}">
      <span><strong>${esc(room.roomNo1)}</strong><small>${esc(room.roomNo2)}</small></span>
      <span>${esc(room.building || "-")}</span>
      <span>${esc(residentDisplayName(primary))}</span>
      <span>${esc(primary?.phone || "-")}</span>
      <span>${esc(residentCarsText(room))}</span>
      <span><i class="payment-badge ${room.commonFeeStatus}">${paymentLabel(room.commonFeeStatus)}</i></span>
    </button>`;
  }).join("") || emptyState(currentLang === "th" ? "ไม่พบข้อมูลลูกบ้าน" : "No resident rooms found");
  syncViewSwitches();
}
function openResidentRoom(roomId) {
  const room = residentRooms.find(item => item.id === roomId);
  if (!room) return;
  const occupants = room.occupants || [];
  const cars = room.cars || [];
  const order = room.personOrder || [];
  const allPeople = residentPersonEntries(room).sort((a, b) => {
    const ai = order.indexOf(a.key);
    const bi = order.indexOf(b.key);
    return (ai === -1 ? 999 : ai) - (bi === -1 ? 999 : bi);
  });
  $("#residentModalTitle").textContent = `${currentLang === "th" ? "รายละเอียดห้อง" : "Room detail"} ${room.roomNo1}`;
  $("#residentModalBody").innerHTML = `
    <div class="resident-detail-grid">
      <label>เลขห้องแบบที่ 1<input data-room-no1="${room.id}" value="${esc(room.roomNo1)}"></label>
      <label>เลขห้องแบบที่ 2<input data-room-no2="${room.id}" value="${esc(room.roomNo2)}"></label>
      <label>ตึก<input data-room-building="${room.id}" value="${esc(room.building || "")}"></label>
      <label>ค่าส่วนกลางปีปัจจุบัน<select data-room-payment="${room.id}">
        <option value="paid" ${room.commonFeeStatus === "paid" ? "selected" : ""}>ชำระแล้ว</option>
        <option value="unpaid" ${room.commonFeeStatus === "unpaid" ? "selected" : ""}>ยังไม่ชำระ</option>
      </select></label>
    </div>
    <div class="resident-section">
      <h3>เจ้าของร่วม <small>กำหนดได้ 1 คนต่อห้อง</small></h3>
      <div class="resident-detail-grid">
        <input data-owner-first="${room.id}" placeholder="ชื่อ" value="${esc(room.owner?.firstName || "")}">
        <input data-owner-last="${room.id}" placeholder="นามสกุล" value="${esc(room.owner?.lastName || "")}">
        <input data-owner-nick="${room.id}" placeholder="ชื่อเล่น" value="${esc(room.owner?.nickName || "")}">
        <input data-owner-phone="${room.id}" placeholder="เบอร์ติดต่อ" value="${esc(room.owner?.phone || "")}">
      </div>
    </div>
    <div class="resident-section">
      <h3>ผู้เช่า/ผู้อยู่อาศัย <small>เพิ่มได้เรื่อย ๆ</small></h3>
      <div class="resident-chip-list">${occupants.map((p, index) => `<span class="resident-chip">${esc(residentDisplayName(p))} · ${esc(p.phone || "-")} · ${p.status === "tenant" ? "ผู้เช่า" : "ผู้อยู่อาศัย"} <button data-remove-occupant="${room.id}" data-index="${index}">×</button></span>`).join("") || "<p class='muted-line'>ยังไม่มีผู้เช่าหรือผู้อยู่อาศัยเพิ่มเติม</p>"}</div>
      <div class="resident-add-grid">
        <input id="newOccFirst" placeholder="ชื่อ">
        <input id="newOccLast" placeholder="นามสกุล">
        <input id="newOccNick" placeholder="ชื่อเล่น">
        <input id="newOccPhone" placeholder="เบอร์ติดต่อ">
        <select id="newOccStatus"><option value="tenant">ผู้เช่า</option><option value="resident">ผู้อยู่อาศัย</option></select>
        <button class="secondary-btn" type="button" data-add-occupant="${room.id}">เพิ่มรายชื่อ</button>
      </div>
    </div>
    <div class="resident-section">
      <h3>รายชื่อทั้งหมดในห้อง <small>ใช้ปุ่ม ↑ ↓ เพื่อจัดลำดับเอง คนบนสุดจะแสดงเป็นผู้อยู่อาศัยหลักในหน้าลูกบ้าน</small></h3>
      <div class="resident-primary-list">${allPeople.map(item => `<div class="resident-primary-card ${(item.person.isCurrentResident ?? item.person.isPrimary) ? "selected" : ""}" data-person-card="${item.key}">
        <span><strong>${esc(residentDisplayName(item.person))}</strong><small>${esc(item.person.phone || "-")} · ${item.label}</small></span>
        <div class="resident-card-controls">
          <button type="button" data-move-person="up" aria-label="เลื่อนขึ้น">↑</button>
          <button type="button" data-move-person="down" aria-label="เลื่อนลง">↓</button>
          <label><input type="checkbox" data-current-person="${room.id}" value="${item.key}" ${(item.person.isCurrentResident ?? item.person.isPrimary) ? "checked" : ""}> อาศัยอยู่</label>
        </div>
      </div>`).join("") || "<p class='muted-line'>กรุณากรอกชื่อเจ้าของร่วมหรือเพิ่มผู้อยู่อาศัยก่อน</p>"}</div>
    </div>
    <div class="resident-section">
      <h3>ทะเบียนรถที่ลงทะเบียน</h3>
      <div class="resident-chip-list">${cars.map((car, index) => `<span class="resident-chip">${esc(car.prefix)}-${esc(car.number)} ${esc(car.province)} · ${esc([car.brand, car.model, car.color].filter(Boolean).join(" ") || "-")} <button data-remove-car="${room.id}" data-index="${index}">×</button></span>`).join("") || "<p class='muted-line'>ยังไม่มีทะเบียนรถ</p>"}</div>
      <div class="resident-add-grid car">
        <input id="newCarPrefix" maxlength="8" placeholder="1-8 ตัวหน้า">
        <input id="newCarNumber" maxlength="4" placeholder="1-4 ตัวหลัง">
        <input id="newCarProvince" placeholder="จังหวัด">
        <input id="newCarBrand" placeholder="ยี่ห้อ">
        <input id="newCarModel" placeholder="รุ่น">
        <input id="newCarColor" placeholder="สี">
        <button class="secondary-btn" type="button" data-add-car="${room.id}">เพิ่มทะเบียนรถ</button>
      </div>
    </div>
    <div class="form-actions"><button class="secondary-btn" type="button" data-close-resident>ยกเลิก</button><button class="primary-btn" type="button" data-save-resident-room="${room.id}">บันทึกข้อมูลห้อง</button></div>
  `;
  $("#residentModal").classList.remove("hidden");
}
function emptyState(text) { return `<div class="empty-state"><span>☷</span><strong>${text}</strong><p>${t("empty.sub")}</p></div>`; }
function getViewJobs(view = currentView) {
  if (!currentUser) return [];
  if (view === "commonWork") return jobs.filter(j => {
    const job = normalizeJob(j);
    return canViewJob(currentUser, job) && job.mainCategory === "common";
  });
  if (view === "commonWork") return jobs.filter(j => {
    const job = normalizeJob(j);
    return canViewJob(currentUser, job) && (job.areaType === "common" || job.sharePublic || String(job.roomNo || job.room || "").includes("ส่วนกลาง") || String(job.roomNo || job.room || "").toLowerCase().includes("common"));
  });
  if (view === "pool") return jobs.filter(j => currentUser.role !== "resident" && j.raw);
  if (view === "juristicTeam") return jobs.filter(j => currentUser.role !== "resident" && !j.raw && (j.source === "WebApp" || j.source === "Assigned" || j.assignedBy || j.createdBy));
  if (view === "myjobs") return jobs.filter(j => normalizeJob(j).assignee === currentUser.id);
  if (view === "residentWork") return canViewResidentWork() ? jobs.filter(j => {
    const job = normalizeJob(j);
    return canViewJob(currentUser, job) && job.mainCategory === "resident";
  }) : [];
  if (view === "technicianJobs") return jobs.filter(j => {
    if (currentUser.role === "resident") return false;
    const assignee = getUser(normalizeJob(j).assignee);
    return inferDepartment(assignee || {}) === "technician";
  });
  if (view === "housekeepingJobs") return jobs.filter(j => {
    if (currentUser.role === "resident") return false;
    const assignee = getUser(normalizeJob(j).assignee);
    return inferDepartment(assignee || {}) === "housekeeping";
  });
  if (view === "roomjobs") return jobs.filter(j => getText(j.roomNo || j.room) === currentUser.room);
  if (view === "dashboard") return jobs.filter(j => canViewJob(currentUser, normalizeJob(j)));
  return jobs.filter(j => canViewJob(currentUser, normalizeJob(j)));
}
const commonWorkBoardStatuses = ["open", "received", "pending_inspection", "inspected_waiting_repair", "repaired_follow_up", "temporary_waiting_parts", "waiting_owner_or_admin_verification", "completed", "rejected"];
const workBoardViews = new Set(["commonWork", "myjobs", "residentWork", "technicianJobs", "housekeepingJobs", "juristicTeam"]);
const commonWorkRangeOptions = [
  { key: "1", label: { th: "1 วัน", en: "1 day" } },
  { key: "7", label: { th: "7 วันย้อนหลัง", en: "Last 7 days" } },
  { key: "30", label: { th: "30 วันย้อนหลัง", en: "Last 30 days" } },
  { key: "60", label: { th: "60 วันย้อนหลัง", en: "Last 60 days" } }
];
const workBoardRangeOptions = [
  ...commonWorkRangeOptions.filter(option => option.key !== "60"),
  { key: "all", label: { th: "ทั้งหมด", en: "All" } }
];
function commonWorkRangeLabel(key = commonWorkRange) {
  const option = workBoardRangeOptions.find(item => item.key === key) || workBoardRangeOptions[2];
  return option.label[currentLang] || option.label.th;
}
function commonWorkMatchesRange(job) {
  if (commonWorkRange === "all") return true;
  const days = Number(commonWorkRange) || 30;
  const date = normalizeJob(job).jobDate || job.date || job.createdAt?.slice(0, 10) || "";
  if (!date) return false;
  return date >= addDaysIso(-(days - 1)) && date <= todayIso();
}
function commonBoardStatusLabel(statusKey) {
  if (statusKey === "waiting_owner_or_admin_verification") return statusLabel("completed", statusKey);
  return statusLabel(statusKey);
}
function workBoardCopy() {
  const keyed = {
    myjobs: ["board.myJobs.title", "board.myJobs.subtitle"],
    technicianJobs: ["board.technician.title", "board.technician.subtitle"],
    housekeepingJobs: ["board.housekeeping.title", "board.housekeeping.subtitle"],
    juristicTeam: ["board.juristic.title", "board.juristic.subtitle"],
    residentWork: ["board.resident.title", "board.resident.subtitle"],
    commonWork: ["board.common.title", "board.common.subtitle"]
  }[currentView];
  if (keyed) return { title: t(keyed[0]), subtitle: t(keyed[1]) };
  if (currentView === "technicianJobs") return {
    title: currentLang === "th" ? "กระดานงานของช่าง" : "Technician Jobs Board",
    subtitle: currentLang === "th" ? "มุมมองแบบ Trello แบ่งช่องตามกรองสถานะของงานช่างอาคาร" : "Trello-style board grouped by status for technician jobs"
  };
  if (currentView === "housekeepingJobs") return {
    title: currentLang === "th" ? "กระดานงานของแม่บ้าน" : "Housekeeping Jobs Board",
    subtitle: currentLang === "th" ? "มุมมองแบบ Trello แบ่งช่องตามกรองสถานะของงานแม่บ้าน" : "Trello-style board grouped by status for housekeeping jobs"
  };
  if (currentView === "juristicTeam") return {
    title: currentLang === "th" ? "กระดานงานของทีมนิติ" : "Juristic Team Jobs Board",
    subtitle: currentLang === "th" ? "มุมมองแบบ Trello แบ่งช่องตามกรองสถานะของงานที่ทีมนิติดูแล" : "Trello-style board grouped by status for juristic team jobs"
  };
  if (currentView === "myjobs") return {
    title: currentLang === "th" ? "กระดานงานของฉัน" : "My Jobs Board",
    subtitle: currentLang === "th" ? "มุมมองแบบ Trello แบ่งช่องตามกรองสถานะของงานที่มอบหมายเข้าบัญชีนี้" : "Trello-style board grouped by status for jobs assigned to this account"
  };
  if (currentView === "residentWork") return {
    title: currentLang === "th" ? "กระดานงานของลูกบ้าน" : "Resident Jobs Board",
    subtitle: currentLang === "th" ? "มุมมองแบบ Trello แบ่งช่องตามกรองสถานะของงานลูกบ้าน" : "Trello-style board grouped by status for resident jobs"
  };
  return {
    title: currentLang === "th" ? "กระดานงานส่วนกลาง" : "Common Work Board",
    subtitle: currentLang === "th" ? "มุมมองแบบ Trello แบ่งช่องตามกรองสถานะของงานส่วนกลาง" : "Trello-style board grouped by common work status"
  };
}
function ensureCommonWorkBoardUi() {
  if ($("#commonWorkBoardPanel")) return;
  const toolbar = document.querySelector("#listView .list-toolbar");
  if (!toolbar) return;
  toolbar.insertAdjacentHTML("beforebegin", `
    <section class="panel common-work-board-panel hidden" id="commonWorkBoardPanel">
      <div class="panel-header">
        <div><h3>กระดานงานส่วนกลาง</h3><p>มุมมองแบบ Trello แบ่งช่องตามกรองสถานะของงานส่วนกลาง</p></div>
        <div class="common-board-controls">
          <div class="common-range-filter">
            ${workBoardRangeOptions.map(option => `<button type="button" data-common-board-range="${option.key}">${option.label[currentLang] || option.label.th}</button>`).join("")}
          </div>
          <div class="common-board-count" id="commonBoardCount">0 งาน</div>
        </div>
      </div>
      <div class="common-kanban-board" id="commonKanbanBoard"></div>
    </section>
  `);
}
function commonBoardCard(job) {
  job = normalizeJob(job);
  const c = categoryMeta[job.category] || { icon: "?", color: "#b56b28", soft: "#fff0e8" };
  const assignee = getUser(job.assignee);
  return `<article class="common-kanban-card" data-job-id="${job.id}">
    <div class="common-kanban-card-top"><span style="--tone:${c.color};--soft:${c.soft}">${c.icon} ${categoryLabel(job.category)}</span><small>${job.id}</small></div>
    <strong>${esc(getText(job.title))}</strong>
    <p>${esc(job.roomNo || getText(job.room) || "-")} ${job.building ? `· ตึก ${esc(job.building)}` : ""}</p>
    <div class="common-kanban-card-foot">
      <i class="priority ${job.priority}">${priorityLabel(job.priority)}</i>
      ${assignee ? `<span class="avatar">${initials(getUserName(assignee))}</span>` : `<em>ยังไม่มอบหมาย</em>`}
    </div>
  </article>`;
}
function renderCommonWorkBoard(boardJobs = []) {
  ensureCommonWorkBoardUi();
  const panel = $("#commonWorkBoardPanel");
  if (!panel) return;
  panel.classList.toggle("hidden", !workBoardViews.has(currentView));
  if (!workBoardViews.has(currentView)) return;
  const copy = workBoardCopy();
  panel.querySelector(".panel-header h3").textContent = copy.title;
  panel.querySelector(".panel-header p").textContent = copy.subtitle;
  $$("#commonWorkBoardPanel [data-common-board-range]").forEach(btn => btn.classList.toggle("active", btn.dataset.commonBoardRange === commonWorkRange));
  $("#commonBoardCount").textContent = `${boardJobs.length} ${currentLang === "th" ? "งาน" : "jobs"} · ${commonWorkRangeLabel()}`;
  const activeStatus = $("#statusFilter")?.value || "all";
  $("#commonKanbanBoard").innerHTML = commonWorkBoardStatuses.map(statusKey => {
    const items = boardJobs.filter(job => statusKey === "waiting_owner_or_admin_verification" ? job.subStatus === statusKey : normalizeStatus(job.status) === statusKey);
    return `<section class="common-kanban-column ${activeStatus === statusKey ? "active" : ""}">
      <button class="common-kanban-head" type="button" data-common-board-status="${statusKey}">
        <span>${commonBoardStatusLabel(statusKey)}</span><b>${items.length}</b>
      </button>
      <div class="common-kanban-cards">
        ${items.map(commonBoardCard).join("") || `<div class="common-kanban-empty">ยังไม่มีงานในสถานะนี้</div>`}
      </div>
    </section>`;
  }).join("");
}
function renderJobList() {
  const q = $("#jobSearch").value.trim().toLowerCase();
  const status = $("#statusFilter").value;
  const category = $("#categoryFilter").value;
  const sourceJobs = getViewJobs();
  const boardList = sourceJobs.filter(j =>
    !workBoardViews.has(currentView) || commonWorkMatchesRange(j)
  );
  renderCommonWorkBoard(boardList);
  const list = sourceJobs.filter(j =>
    (!q || `${getText(j.title)} ${getText(j.room)} ${j.id}`.toLowerCase().includes(q)) &&
    (category === "all" || j.category === category) &&
    (status === "all" || normalizeStatus(j.status) === status || j.subStatus === status)
  );
  $("#resultCount").textContent = list.length;
  const mode = getViewMode("jobs");
  $("#allJobsList").classList.toggle("card-view", mode === "grid");
  $("#allJobsList").innerHTML = list.map(j => jobRow(j, currentView === "pool")).join("") || emptyState(t("empty.noResults"));
  syncViewSwitches();
}
function navigate(view, statusFilter) {
  if (!hasSidebarAccess(view)) view = "dashboard";
  if (view === "residentWork" && !canViewResidentWork()) view = "dashboard";
  if (view === "team" && !canManageTeam()) view = "dashboard";
  if (view === "residents" && !canManageResidents()) view = "dashboard";
  if (view === "organization" && !canManageOrganization()) view = "dashboard";
  if (view === "backhouse" && !canManageAnnouncements()) view = "dashboard";
  if (view === "permissions" && !canManagePermissions()) view = "dashboard";
  if (view === "logs" && currentUser?.role !== "admin") view = "dashboard";
  currentView = view;
  $$(".nav-item").forEach(n => n.classList.toggle("active", n.dataset.view === view));
  $$(".mobile-nav-item").forEach(n => n.classList.toggle("active", n.dataset.view === view));
  $$(".view").forEach(v => v.classList.remove("active"));
  $("#pageTitle").textContent = t(`page.${view}.title`);
  $("#pageSubtitle").textContent = t(`page.${view}.subtitle`);
  if (view === "dashboard") $("#dashboardView").classList.add("active");
  else if (view === "finance") $("#financeView").classList.add("active");
  else if (view === "calendar") $("#calendarView").classList.add("active");
  else if (view === "chat") $("#chatView").classList.add("active");
  else if (isRoutineView(view)) $(`#${view}View`)?.classList.add("active");
  else if (view === "team") $("#teamView").classList.add("active");
  else if (view === "residents") $("#residentsView").classList.add("active");
  else if (view === "organization") $("#organizationView").classList.add("active");
  else if (view === "permissions") $("#permissionsView").classList.add("active");
  else if (view === "backhouse") $("#backhouseView").classList.add("active");
  else if (view === "staffLogs") $("#staffLogsView").classList.add("active");
  else if (view === "logs") $("#logsView").classList.add("active");
  else {
    $("#listView").classList.add("active");
    if (statusFilter) $("#statusFilter").value = statusFilter;
    renderJobList();
  }
  $("#sidebar").classList.remove("open");
}
function attachmentHtml(job) {
  return job.attachments?.length ? `<div class="job-photo-strip">${job.attachments.map(src => `<img src="${src}" alt="attachment">`).join("")}</div>` : "";
}
function assignmentControls(job) {
  if (!canAssign()) return "";
  const mainOptions = Object.keys(mainCategoryMeta).map(k => `<option value="${k}" ${job.mainCategory === k ? "selected" : ""}>${mainCategoryLabel(k)}</option>`).join("");
  return `<div class="action-box"><h3>${currentLang === "th" ? "คัดแยกและมอบหมายงาน" : "Categorize and assign"}</h3>
    <form class="assignment-form" data-assign-job="${job.id}">
      <div class="form-row"><label>${t("create.mainCategoryLabel")}<select name="mainCategory">${mainOptions}</select></label></div>
      <div class="form-row"><label>${t("create.categoryLabel")}<select name="category">${categoryOrder.map(k => `<option value="${k}" ${job.category === k ? "selected" : ""}>${categoryLabel(k)}</option>`).join("")}</select></label>
      <label>${t("create.assigneeLabel")}<select name="assignee">${assigneeCandidates().map(u => `<option value="${u.id}">${getUserName(u)} — ${departmentLabel(u.department) || categoryLabel(u.team)}</option>`).join("")}</select></label></div>
      <button class="primary-btn" type="submit">${currentLang === "th" ? "มอบหมายงาน" : "Assign job"}</button>
    </form></div>`;
}
function technicianControls(job) {
  job = normalizeJob(job);
  const canUpdate = canUpdateJob(currentUser, job);
  if (!canUpdate) return "";
  const pinBox = job.status === "completed" ? "" : `<div class="status-extra status-extra-completed hidden">
    <label class="team-check"><input type="checkbox" name="noPinAvailable" value="1"> ลูกบ้านไม่อยู่ / ไม่สามารถขอ PIN ได้</label>
    <label class="pin-field">กรอก PIN 4 หลัก<input name="closePin" maxlength="4" inputmode="numeric" placeholder="0000"></label>
    <label class="no-pin-field hidden">เหตุผลที่ไม่มี PIN<textarea name="noPinReason" rows="2"></textarea></label>
    <label>วันที่เสร็จงาน<input type="date" name="completedWorkDate"></label>
  </div>`;
  return `<div class="action-box"><h3>${t("modal.updateJob")}</h3>
    <form class="update-form" data-update-job="${job.id}">
      <div class="form-row"><label>${t("list.statusFilter")}<select name="status" data-status-select>${statuses.map(s => `<option value="${s}" ${job.status === s ? "selected" : ""}>${statusLabel(s)}</option>`).join("")}</select></label>
      <label class="status-extra status-extra-pending_inspection hidden">วันที่เข้าตรวจสอบ<input type="date" name="inspectionDate"></label></div>
      <div class="status-extra status-extra-inspected_waiting_repair hidden">
        <div class="form-row"><label>วันที่ตรวจสอบ / วันที่อัปเดต<input type="date" name="updateDate"></label></div>
        <label>สาเหตุ<textarea name="cause" rows="2"></textarea></label>
        <label>แนวทางแก้ไข<textarea name="solution" rows="2"></textarea></label>
      </div>
      <div class="status-extra status-extra-repaired_follow_up hidden">
        <label>วันที่ติดตาม/อัปเดตครั้งถัดไป<input type="date" name="nextUpdateDate"></label>
      </div>
      <div class="status-extra status-extra-temporary_waiting_parts hidden">
        <label>วันที่ติดตามอะไหล่/ผู้รับเหมาครั้งถัดไป<input type="date" name="nextUpdateDate"></label>
      </div>
      ${pinBox}
      <div class="status-extra status-photo-required hidden">
        <div class="photo-choice-wrap">
          <span class="photo-choice-title">แนบรูปภาพประกอบ ขั้นต่ำ 1 รูป สูงสุด 3 รูป</span>
          <div class="photo-choice">
            <label class="secondary-btn">📷 ถ่ายรูป<input type="file" name="statusAttachmentsCamera" accept="image/*" capture="environment" multiple hidden></label>
            <label class="secondary-btn">🖼 แนบรูป<input type="file" name="statusAttachmentsGallery" accept="image/*" multiple hidden></label>
          </div>
          <small>ระบบจะประทับชื่อผู้ใช้งานและเวลาแนบลงบนรูปอัตโนมัติ</small>
        </div>
      </div>
      <label>หมายเหตุ<textarea name="note" rows="2"></textarea></label>
      <button class="primary-btn" type="submit">${t("modal.saveStatus")}</button>
    </form></div>`;
}
function syncStatusFields(select) {
  if (!select) return;
  const form = select.closest("form");
  const status = select.value;
  form.querySelectorAll(".status-extra").forEach(el => {
    el.classList.add("hidden");
    el.querySelectorAll("input, textarea, select").forEach(input => input.disabled = true);
  });
  form.querySelectorAll(`.status-extra-${status}`).forEach(el => {
    el.classList.remove("hidden");
    el.querySelectorAll("input, textarea, select").forEach(input => input.disabled = false);
  });
  const photoBox = form.querySelector(".status-photo-required");
  if (photoBox) {
    const requiresAttachment = !["received", "rejected"].includes(status);
    photoBox.classList.toggle("hidden", !requiresAttachment);
    photoBox.querySelectorAll("input, textarea, select").forEach(input => input.disabled = !requiresAttachment);
  }
  const noPin = form.querySelector("[name='noPinAvailable']");
  const pinField = form.querySelector(".pin-field");
  const noPinField = form.querySelector(".no-pin-field");
  if (status === "completed") {
    pinField?.classList.toggle("hidden", !!noPin?.checked);
    noPinField?.classList.toggle("hidden", !noPin?.checked);
    pinField?.querySelectorAll("input, textarea, select").forEach(input => input.disabled = !!noPin?.checked);
    noPinField?.querySelectorAll("input, textarea, select").forEach(input => input.disabled = !noPin?.checked);
  }
}
function ensureAssignmentDueFields(job) {
  const form = document.querySelector(`[data-assign-job="${job.id}"]`);
  if (!form || form.querySelector("[name='dueDate']")) return;
  const submit = form.querySelector("[type='submit']");
  submit?.insertAdjacentHTML("beforebegin", `<div class="form-row assignment-due-row">
    <label>${currentLang === "th" ? "วันครบกำหนด" : "Due date"}<input type="date" name="dueDate" value="${job.dueDate || defaultDueDate(job)}"></label>
    <label>${currentLang === "th" ? "หมายเหตุ SLA" : "SLA note"}<input name="slaNote" value="${esc(job.slaNote || "")}" placeholder="${currentLang === "th" ? "เช่น ต้องติดตามภายใน 24 ชม." : "e.g. follow up within 24 hrs"}"></label>
  </div>`);
}
function jobEditableSnapshot(job) {
  return {
    title: getText(job.title),
    jobDate: job.jobDate || "",
    roomNo: job.roomNo || getText(job.room) || "",
    building: job.building || "",
    floor: job.floor || "",
    contactName: job.contactName || "",
    contactPhone: job.contactPhone || "",
    priority: job.priority || "normal",
    issueDescription: job.issueDescription || getText(job.description) || ""
  };
}
function googleFormEditControls(job) {
  if (!canEditGoogleFormJobDetails(job)) return "";
  const snapshot = jobEditableSnapshot(job);
  return `<div class="action-box google-edit-box">
    <div class="action-box-title">
      <div>
        <h3>${currentLang === "th" ? "แก้ไขรายละเอียดจาก Google Form" : "Edit Google Form details"}</h3>
        <p>${currentLang === "th" ? "สำหรับผู้ได้รับมอบอำนาจเท่านั้น ระบบจะเก็บค่าเดิมก่อนแก้ไขลง Timeline ของงานนี้ทุกครั้ง" : "Authorized users only. The system keeps the previous values in this job timeline every time."}</p>
      </div>
      <span class="audit-chip">${currentLang === "th" ? "เก็บ Log อัตโนมัติ" : "Auto audit log"}</span>
    </div>
    <form class="google-job-edit-form" data-edit-google-form-job="${job.id}">
      <div class="form-row">
        <label>${currentLang === "th" ? "หัวข้องาน" : "Job title"}<input name="title" value="${esc(snapshot.title)}"></label>
        <label>${currentLang === "th" ? "วันที่รับแจ้ง" : "Reported date"}<input type="date" name="jobDate" value="${esc(snapshot.jobDate)}"></label>
      </div>
      <div class="form-row">
        <label>${currentLang === "th" ? "เลขห้อง / พื้นที่" : "Room / Area"}<input name="roomNo" value="${esc(snapshot.roomNo)}"></label>
        <label>${currentLang === "th" ? "ความเร่งด่วน" : "Priority"}<select name="priority">
          ${["normal", "high", "urgent"].map(key => `<option value="${key}" ${snapshot.priority === key ? "selected" : ""}>${priorityLabel(key)}</option>`).join("")}
        </select></label>
      </div>
      <div class="form-row">
        <label>${currentLang === "th" ? "ตึก" : "Building"}<input name="building" value="${esc(snapshot.building)}"></label>
        <label>${currentLang === "th" ? "ชั้น" : "Floor"}<input name="floor" value="${esc(snapshot.floor)}"></label>
      </div>
      <div class="form-row">
        <label>${currentLang === "th" ? "ผู้ติดต่อ" : "Contact name"}<input name="contactName" value="${esc(snapshot.contactName)}"></label>
        <label>${currentLang === "th" ? "เบอร์ติดต่อ" : "Contact phone"}<input name="contactPhone" value="${esc(snapshot.contactPhone)}"></label>
      </div>
      <label>${currentLang === "th" ? "รายละเอียดเหตุที่เกิด" : "Issue details"}<textarea name="issueDescription" rows="3">${esc(snapshot.issueDescription)}</textarea></label>
      <button class="primary-btn" type="submit">${currentLang === "th" ? "บันทึกการแก้ไขรายละเอียด" : "Save detail changes"}</button>
    </form>
  </div>`;
}
function formatSnapshotChange(before, after) {
  const labels = {
    title: currentLang === "th" ? "หัวข้องาน" : "Title",
    jobDate: currentLang === "th" ? "วันที่รับแจ้ง" : "Reported date",
    roomNo: currentLang === "th" ? "เลขห้อง/พื้นที่" : "Room/Area",
    building: currentLang === "th" ? "ตึก" : "Building",
    floor: currentLang === "th" ? "ชั้น" : "Floor",
    contactName: currentLang === "th" ? "ผู้ติดต่อ" : "Contact",
    contactPhone: currentLang === "th" ? "เบอร์ติดต่อ" : "Phone",
    priority: currentLang === "th" ? "ความเร่งด่วน" : "Priority",
    issueDescription: currentLang === "th" ? "รายละเอียด" : "Details"
  };
  return Object.keys(labels).filter(key => String(before[key] || "") !== String(after[key] || "")).map(key => `${labels[key]}: "${before[key] || "-"}" → "${after[key] || "-"}"`).join(" | ");
}
function saveGoogleFormJobDetails(jobId, data) {
  const job = normalizeJob(jobs.find(item => item.id === jobId));
  if (!job || !canEditGoogleFormJobDetails(job)) return { ok: false, message: currentLang === "th" ? "ไม่มีสิทธิ์แก้ไขงานนี้" : "You do not have permission to edit this job" };
  const before = jobEditableSnapshot(job);
  if (!job.googleFormOriginal) job.googleFormOriginal = { ...before, capturedAt: new Date().toISOString() };
  const after = {
    title: data.title?.trim() || before.title,
    jobDate: data.jobDate || before.jobDate,
    roomNo: data.roomNo?.trim() || before.roomNo,
    building: data.building?.trim() || "",
    floor: data.floor?.trim() || "",
    contactName: data.contactName?.trim() || "",
    contactPhone: data.contactPhone?.trim() || "",
    priority: data.priority || before.priority,
    issueDescription: data.issueDescription?.trim() || ""
  };
  const diff = formatSnapshotChange(before, after);
  if (!diff) return { ok: false, message: currentLang === "th" ? "ยังไม่มีข้อมูลที่เปลี่ยนแปลง" : "No changes detected" };
  job.title = { th: after.title, en: after.title };
  job.jobDate = after.jobDate;
  job.date = after.jobDate;
  job.roomNo = after.roomNo;
  job.room = after.roomNo;
  job.building = after.building;
  job.floor = after.floor;
  job.contactName = after.contactName;
  job.contactPhone = after.contactPhone;
  job.priority = after.priority;
  job.issueDescription = after.issueDescription;
  job.description = after.issueDescription;
  addJobTimeline(job.id, {
    action: "google_form_detail_edited",
    message: currentLang === "th" ? `แก้ไขรายละเอียดงานจาก Google Form และเก็บค่าเดิมไว้: ${diff}` : `Edited Google Form job details and stored previous values: ${diff}`,
    data: { before, after, original: job.googleFormOriginal }
  });
  saveJobs();
  return { ok: true };
}
function openJob(id) {
  const job = normalizeJob(jobs.find(j => j.id === id));
  const assignee = getUser(job.assignee);
  const reporter = getUser(job.reporter);
  const assigner = getUser(job.assignedBy);
  const c = categoryMeta[job.category];
  const due = dueInfo(job);
  const exportHtml = `<div class="job-export-actions"><button class="secondary-btn" type="button" data-export-job-csv="${job.id}">Export CSV</button><button class="primary-btn" type="button" data-export-job-pdf="${job.id}">Export PDF</button></div>`;
  $("#modalTicket").textContent = job.id;
  $("#modalTitle").textContent = getText(job.title);
  const pinHtml = canSeeClosePin(currentUser, job)
    ? `<div class="pin-card"><span>PIN ปิดงาน</span><strong>${job.closePin}</strong><small>แสดงเฉพาะ Admin / ผู้มอบหมาย / เจ้าของห้อง / ผู้แจ้ง</small></div>`
    : `<div class="pin-card muted-pin"><span>PIN ปิดงาน</span><strong>••••</strong><small>ผู้ได้รับมอบหมายจะเห็นเฉพาะช่องกรอก PIN ตอนปิดงาน</small></div>`;
  const verifyHtml = canVerifyCompletion(currentUser, job)
    ? `<div class="action-box"><h3>ยืนยันปิดงาน</h3><p class="muted-line">งานนี้ช่างแจ้งเสร็จแล้วแต่ไม่มี PIN ต้องให้ผู้มีสิทธิ์ตรวจสอบและยืนยันก่อนนับเป็นเสร็จสิ้น</p><button class="primary-btn" data-verify-completion="${job.id}">ยืนยันปิดงานนี้</button></div>`
    : "";
  const timelineHtml = `<div class="timeline-box"><div class="section-heading"><div><h3>Timeline</h3><p>${currentLang === "th" ? "ประวัติการเปลี่ยนแปลงและการอัปเดตของงานนี้" : "History of changes and updates for this job"}</p></div></div>${(job.timeline || []).map(log => `<div class="timeline-item"><time>${new Date(log.at).toLocaleString(currentLang === "th" ? "th-TH" : "en-US")}</time><strong>${esc(log.message || log.action)}</strong><small>${esc(log.byName || "")}</small></div>`).join("") || "<p class='muted-line'>ยังไม่มี Timeline</p>"}</div>`;
  $("#jobModal .modal")?.classList.add("job-detail-modal");
  $("#modalBody").innerHTML = `
    ${exportHtml}
    <div class="detail-hero job-detail-hero">
      <div class="job-hero-main">
        <div>
          <span class="eyebrow">${currentLang === "th" ? "รายละเอียดงาน" : "Work order detail"}</span>
          <h3>${esc(getText(job.title))}</h3>
          <p>${esc(job.issueDescription || getText(job.description) || t("common.noDescription"))}</p>
        </div>
        <div class="job-hero-badges">
          <span class="status-pill status-${job.status}">${statusLabel(job.status, job.subStatus)}</span>
          ${job.raw ? `<span class="raw-badge">POOL</span>` : ""}
          ${job.source === "Google Form" ? `<span class="source-badge">Google Form</span>` : ""}
          ${job.hasScheduleConflict ? `<span class="raw-badge">เวลางานซ้อน</span>` : ""}
        </div>
      </div>
      <div class="job-quick-meta">
        <div><span>${currentLang === "th" ? "ผู้รับผิดชอบ" : "Assignee"}</span><strong>${assignee ? getUserName(assignee) : t("common.unassigned")}</strong></div>
        <div><span>${currentLang === "th" ? "ครบกำหนด" : "Due"}</span><strong>${job.dueDate ? `${formatDate(job.dueDate)} · ${due.label}` : "-"}</strong></div>
        <div><span>${currentLang === "th" ? "พื้นที่" : "Area"}</span><strong>${job.roomNo || getText(job.room)}</strong></div>
      </div>
      ${attachmentHtml(job)}
    </div>
    <div class="section-card">
    <div class="section-heading"><div><h3>${currentLang === "th" ? "ข้อมูลหลัก" : "Key information"}</h3><p>${currentLang === "th" ? "ข้อมูลที่ใช้ในการติดตาม มอบหมาย และติดต่อกลับ" : "Information used for tracking, assignment, and follow-up"}</p></div></div>
    <div class="detail-grid">
      <div class="detail-field"><span>วันที่งาน</span><strong>${formatDate(job.jobDate)}</strong></div>
      <div class="detail-field"><span>เวลา</span><strong>${job.startTime || "-"} ${job.endTime ? `- ${job.endTime}` : ""}</strong></div>
      <div class="detail-field"><span>เลขห้อง</span><strong>${job.roomNo || getText(job.room)}</strong></div>
      <div class="detail-field"><span>ตึก / ชั้น</span><strong>${job.building || "-"} / ${job.floor || "-"}</strong></div>
      <div class="detail-field"><span>ผู้ติดต่อ</span><strong>${job.contactName || "-"}</strong></div>
      <div class="detail-field"><span>เบอร์ติดต่อ</span><strong>${job.contactPhone ? `<a href="tel:${job.contactPhone}">${job.contactPhone}</a>` : "-"}</strong></div>
      <div class="detail-field"><span>${t("modal.category")}</span><strong>${c ? `${c.icon} ${categoryLabel(job.category)}` : categoryLabel(null)}</strong></div>
      <div class="detail-field"><span>${t("modal.reporter")}</span><strong>${reporter ? getUserName(reporter) : t("common.googleForms")}</strong></div>
      <div class="detail-field"><span>${t("modal.priority")}</span><strong>${priorityLabel(job.priority)}</strong></div>
      <div class="detail-field"><span>ผู้มอบหมาย</span><strong>${assigner ? getUserName(assigner) : job.assignedByName || "-"}</strong></div>
      <div class="detail-field"><span>${t("modal.assignee")}</span><strong>${assignee ? getUserName(assignee) : t("common.unassigned")}</strong></div>
      <div class="detail-field"><span>วันที่ติดตาม/อัปเดตถัดไป</span><strong>${job.nextUpdateDate ? formatDate(job.nextUpdateDate) : "-"}</strong></div>
      <div class="detail-field"><span>หมายเหตุ</span><strong>${job.note || "-"}</strong></div>
    </div>
    </div>
    ${googleFormEditControls(job)}
    ${pinHtml}
    ${assignmentControls(job)}
    ${technicianControls(job)}
    ${verifyHtml}
    ${timelineHtml}
  `;
  $("#modalBody .detail-grid")?.insertAdjacentHTML("beforeend", `
    <div class="detail-field"><span>${currentLang === "th" ? "วันครบกำหนด" : "Due date"}</span><strong>${job.dueDate ? `${formatDate(job.dueDate)} · ${due.label}` : "-"}</strong></div>
    <div class="detail-field"><span>${currentLang === "th" ? "หมายเหตุ SLA" : "SLA note"}</span><strong>${job.slaNote || "-"}</strong></div>
  `);
  $("#jobModal").classList.remove("hidden");
  ensureAssignmentDueFields(job);
  syncStatusFields($("#modalBody").querySelector("[data-status-select]"));
}
function populateControls() {
  ensureCreateMainCategoryControl();
  ensureCreateDueDateControl();
  const categoryOptions = categoryOrder.map(k => `<option value="${k}">${categoryLabel(k)}</option>`).join("");
  $("#categoryFilter").innerHTML = `<option value="all">${t("list.allCategories")}</option>${categoryOptions}`;
  if ($("#createMainCategory")) $("#createMainCategory").innerHTML = Object.keys(mainCategoryMeta).map(k => `<option value="${k}">${mainCategoryLabel(k)}</option>`).join("");
  $("#createCategory").innerHTML = categoryOptions;
  $("#prioritySelect").innerHTML = `<option value="normal">${priorityLabel("normal")}</option><option value="high">${priorityLabel("high")}</option><option value="urgent">${priorityLabel("urgent")}</option>`;
  $("#assigneeSelect").innerHTML = `<option value="">${currentLang === "th" ? "ยังไม่มอบหมาย" : "Unassigned"}</option>` + assigneeCandidates().map(u => `<option value="${u.id}">${getUserName(u)} — ${departmentLabel(u.department) || categoryLabel(u.team)}</option>`).join("");
  $("#userDepartmentSelect").innerHTML = departments.map(dept => `<option value="${dept.key}">${dept[currentLang]}</option>`).join("");
}
function ensureCreateMainCategoryControl() {
  if ($("#createMainCategory")) return;
  const categorySelect = $("#createCategory");
  const categoryLabelEl = categorySelect?.closest("label");
  if (!categoryLabelEl) return;
  categoryLabelEl.querySelector("span").textContent = currentLang === "th" ? "ประเภทรอง" : "Sub category";
  categoryLabelEl.insertAdjacentHTML("beforebegin", `<label><span>${t("create.mainCategoryLabel")}</span><select name="mainCategory" id="createMainCategory"></select></label>`);
}
function ensureCreateDueDateControl() {
  if ($("#createDueDate")) return;
  const assigneeSelect = $("#assigneeSelect");
  const row = assigneeSelect?.closest(".form-row");
  if (!row) return;
  row.insertAdjacentHTML("afterend", `<div class="form-row">
    <label>${currentLang === "th" ? "วันครบกำหนด" : "Due date"}<input name="dueDate" id="createDueDate" type="date"></label>
    <label>${currentLang === "th" ? "หมายเหตุ SLA" : "SLA note"}<input name="slaNote" id="createSlaNote" placeholder="${currentLang === "th" ? "เช่น ติดตามภายใน 24 ชม." : "e.g. follow up within 24 hrs"}"></label>
  </div>`);
}

function persistUserFields(user, extra = {}) {
  Object.assign(user, extra);
  const nameParts = splitFullName(user);
  user.name = fullName(user.firstName || nameParts.firstName, user.lastName || nameParts.lastName);
  user.enName = user.enName || user.name;
  teamOverrides[user.id] = {
    ...(teamOverrides[user.id] || {}),
    firstName: user.firstName,
    lastName: user.lastName,
    nickName: user.nickName,
    position: user.position,
    department: user.department,
    assignL1: !!user.assignL1,
    assignL2: !!user.assignL2,
    profileImage: user.profileImage,
    name: user.name,
    enName: user.enName,
    room: user.room,
    password: user.password,
    role: user.role,
    roleKey: user.roleKey,
    canAssign: !!user.canAssign
  };
  saveTeamOverrides();
}
function teamCardValue(userId, key) {
  return document.querySelector(`[${key}="${userId}"]`);
}
function fieldChange(label, before, after) {
  const oldValue = before === undefined || before === null || before === "" ? "-" : before;
  const newValue = after === undefined || after === null || after === "" ? "-" : after;
  return `${label}: ${oldValue} → ${newValue}`;
}
function booleanChange(label, before, after) {
  return `${label}: ${before ? "ON" : "OFF"} → ${after ? "ON" : "OFF"}`;
}
async function saveTeamMember(userId) {
  if (!canManageTeam()) return;
  const user = getUser(userId);
  if (!user || user.role === "admin") return;
  const parts = splitFullName(user);
  const next = {
    firstName: (teamCardValue(userId, "data-team-first")?.value || parts.firstName).trim(),
    lastName: (teamCardValue(userId, "data-team-last")?.value || parts.lastName).trim(),
    nickName: (teamCardValue(userId, "data-team-nick")?.value || "").trim(),
    position: (teamCardValue(userId, "data-team-position")?.value || "").trim(),
    department: teamCardValue(userId, "data-team-department")?.value || user.department,
    assignL1: !!teamCardValue(userId, "data-toggle-l1")?.checked,
    assignL2: !!teamCardValue(userId, "data-toggle-l2")?.checked
  };
  if (currentUser.role === "admin") {
    next.room = (teamCardValue(userId, "data-admin-user-id")?.value || user.room || "").trim().toUpperCase();
    next.password = (teamCardValue(userId, "data-admin-password")?.value || user.password || "").trim();
  }
  const photoInput = teamCardValue(userId, "data-team-photo");
  const changes = [];
  if ((user.firstName || parts.firstName) !== next.firstName) changes.push(fieldChange("firstName", user.firstName || parts.firstName, next.firstName));
  if ((user.lastName || parts.lastName) !== next.lastName) changes.push(fieldChange("lastName", user.lastName || parts.lastName, next.lastName));
  if ((user.nickName || "") !== next.nickName) changes.push(fieldChange("nickname", user.nickName, next.nickName));
  if ((user.position || "") !== next.position) changes.push(fieldChange("position", user.position, next.position));
  if ((user.department || "") !== next.department) changes.push(fieldChange("department", departmentLabel(user.department), departmentLabel(next.department)));
  if (!!user.assignL1 !== next.assignL1) changes.push(booleanChange("assignL1", user.assignL1, next.assignL1));
  if (!!user.assignL2 !== next.assignL2) changes.push(booleanChange("assignL2", user.assignL2, next.assignL2));
  if (currentUser.role === "admin" && (user.room || "") !== (next.room || "")) changes.push(fieldChange("loginId", user.room, next.room));
  if (currentUser.role === "admin" && (user.password || "") !== (next.password || "")) changes.push("password: changed");
  if (photoInput?.files?.[0]) {
    next.profileImage = await fileToDataUrl(photoInput.files[0]);
    changes.push("profileImage: changed");
  }
  if (!changes.length) return showToast(currentLang === "th" ? "ยังไม่มีข้อมูลที่เปลี่ยนแปลง" : "No changes to save");
  persistUserFields(user, next);
  addLog("TEAM_UPDATED", `${getUserName(user)} updated by ${getUserName(currentUser)} — ${changes.join("; ")}`);
  renderTeam();
  renderOrganization();
  renderProjectOrganization();
  showToast(currentLang === "th" ? "บันทึกข้อมูลพนักงานแล้ว" : "Team member saved");
}
function openUserModal() {
  $("#userForm").reset();
  $("#userForm").elements.id.value = "";
  $("#userModalTitle").textContent = currentLang === "th" ? "เพิ่มผู้ใช้งาน" : "Add user";
  $("#userModal").classList.remove("hidden");
}
function openPasswordModal(userId) {
  const user = getUser(userId);
  if (!user) return;
  $("#passwordForm").reset();
  $("#passwordForm").elements.userId.value = userId;
  $("#passwordForm").elements.password.value = user.password || "";
  $("#passwordModalTitle").textContent = currentLang === "th" ? `เปลี่ยนรหัสผ่านของ ${getUserName(user)}` : `Change password for ${getUserName(user)}`;
  $("#passwordModal").classList.remove("hidden");
}
function departmentToRoleKey(department) {
  if (department === "committee" || department === "juristic") return "role.assignmentOfficer";
  if (department === "technician" || department === "housekeeping" || department === "security_staff" || department === "gardener" || department === "contractor") return "role.technician";
  return "role.resident";
}
function requestOrDeleteUser(userId) {
  const user = getUser(userId);
  if (!user || user.role === "admin") return;
  if (currentUser.role === "admin") return deleteUserNow(userId);
  const requestId = `DEL-${Date.now()}`;
  jobs.unshift({
    id: requestId,
    source: "System",
    raw: false,
    title: { th: `คำขอลบผู้ใช้งาน ${getUserName(user)}`, en: `Delete user request: ${getUserName(user)}` },
    room: "Admin",
    areaType: "common",
    sharePublic: false,
    category: "building",
    priority: "high",
    status: "open",
    reporter: currentUser.id,
    date: "2026-06-26",
    time: new Date().toLocaleTimeString("th-TH", { hour: "2-digit", minute: "2-digit" }),
    description: { th: "Co-Admin ขอให้ Admin ตรวจสอบและยืนยันการลบผู้ใช้งาน", en: "Co-Admin requested Admin approval to delete a user." },
    deleteRequest: { userId, requestedBy: currentUser.id, status: "pending" },
    attachments: []
  });
  saveJobs();
  addLog("DELETE_USER_REQUESTED", `${getUserName(currentUser)} requested deletion of ${getUserName(user)}`);
  renderAll();
  showToast(currentLang === "th" ? "ส่งคำขอลบให้ Admin อนุมัติแล้ว" : "Delete request sent to Admin");
}
function deleteUserNow(userId, requestJobId = "") {
  const user = getUser(userId);
  if (!user || user.role === "admin") return;
  exportUserJobsCsv(user);
  if (!deletedUserIds.includes(userId)) deletedUserIds.push(userId);
  saveDeletedUsers();
  if (requestJobId) {
    const job = jobs.find(item => item.id === requestJobId);
    if (job?.deleteRequest) {
      job.deleteRequest.status = "approved";
      job.status = "completed";
    }
    saveJobs();
  }
  addLog("USER_DELETED", `${getUserName(user)} deleted by ${getUserName(currentUser)}; CSV exported`);
  renderAll();
  showToast(currentLang === "th" ? "ลบผู้ใช้และ Export CSV แล้ว" : "User deleted and CSV exported");
}

$("#loginForm").addEventListener("submit", e => {
  e.preventDefault();
  const room = $("#roomNumber").value.trim().toUpperCase();
  const password = $("#password").value;
  const user = activeUsers().find(u => u.room.toUpperCase() === room);
  if (user && user.password === password) showApp(user);
  else showToast(t("toast.loginFailed"));
});
$("#setupToggle").addEventListener("click", () => { setupMode = !setupMode; translateStaticDom(); });
$("#togglePassword").addEventListener("click", () => $("#password").type = $("#password").type === "password" ? "text" : "password");
$("#logoutBtn").addEventListener("click", () => { sessionStorage.removeItem("juristicUser"); location.reload(); });
$("#menuBtn").addEventListener("click", () => $("#sidebar").classList.toggle("open"));
$("#mobileCreateJobBtn").addEventListener("click", () => {
  if (!hasActionPermission("createJob")) return showToast(currentLang === "th" ? "บัญชีนี้ไม่มีสิทธิ์สร้างงาน" : "This account cannot create jobs");
  $("#createJobForm").elements.jobDate.value = new Date().toISOString().slice(0, 10);
  if ($("#createDueDate")) $("#createDueDate").value = addDaysIso(7);
  $("#createModal").classList.remove("hidden");
});
$("#languageToggle").addEventListener("click", () => setLanguage(currentLang === "th" ? "en" : "th"));
$("#authLanguageToggle").addEventListener("click", () => setLanguage(currentLang === "th" ? "en" : "th"));

document.addEventListener("click", e => {
  const announcementCard = e.target.closest("[data-open-announcement]");
  if (announcementCard) {
    openAnnouncement(Number(announcementCard.dataset.openAnnouncement), 0);
    return;
  }
  if (e.target.closest("[data-close-announcement]")) {
    $("#announcementModal")?.classList.add("hidden");
    revokeActiveAnnouncementDownloadUrl();
    return;
  }
  if (e.target.closest("#openBackhouseUploadBtn") && canManageAnnouncements()) {
    ensureBackhouseUploadModal();
    $("#backhouseUploadForm")?.reset();
    $("#backhouseUploadModal")?.classList.remove("hidden");
    return;
  }
  if (e.target.closest("[data-close-backhouse-upload]")) {
    $("#backhouseUploadModal")?.classList.add("hidden");
    return;
  }
  const announcementDownload = e.target.closest("[data-announcement-download]");
  if (announcementDownload) {
    if (announcementDownload.classList.contains("disabled") || announcementDownload.getAttribute("href") === "#") {
      e.preventDefault();
      downloadActiveAnnouncementPage();
    }
    return;
  }
  if (e.target.closest("[data-announcement-prev]")) {
    openAnnouncement(activeAnnouncementIndex, activeAnnouncementPage - 1);
    return;
  }
  if (e.target.closest("[data-announcement-next]")) {
    openAnnouncement(activeAnnouncementIndex, activeAnnouncementPage + 1);
    return;
  }
  const rangeButton = e.target.closest("[data-dashboard-range]");
  if (rangeButton) {
    dashboardRange = rangeButton.dataset.dashboardRange;
    localStorage.setItem("juristicDashboardRange", dashboardRange);
    renderAll();
    return;
  }
  if (e.target.closest("#financeExportBtn")) {
    exportFinanceReportCsv();
    return;
  }
  if (e.target.closest("#financeImportBtn")) {
    if (!canImportFinance()) return showToast(currentLang === "th" ? "บัญชีลูกบ้าน Import งบการเงินไม่ได้" : "Residents cannot import finance data");
    $("#financeImportInput")?.click();
    return;
  }
  if (e.target.closest("[data-calendar-prev]")) {
    shiftCalendarMonth(-1);
    return;
  }
  if (e.target.closest("[data-calendar-next]")) {
    shiftCalendarMonth(1);
    return;
  }
  if (e.target.closest("[data-calendar-today]")) {
    goToTodayCalendar();
    return;
  }
  if (e.target.closest("[data-calendar-pick-month]")) {
    calendarPickerMode = calendarPickerMode === "month" ? "" : "month";
    renderCalendar();
    return;
  }
  if (e.target.closest("[data-calendar-pick-year]")) {
    calendarPickerMode = calendarPickerMode === "year" ? "" : "year";
    renderCalendar();
    return;
  }
  const monthOption = e.target.closest("[data-calendar-month-option]");
  if (monthOption) {
    calendarPickerMode = "";
    setCalendarMonthYear(Number(monthOption.dataset.calendarMonthOption), selectedCalendarYear);
    renderCalendar();
    return;
  }
  const yearOption = e.target.closest("[data-calendar-year-option]");
  if (yearOption) {
    calendarPickerMode = "";
    setCalendarMonthYear(selectedCalendarMonth, Number(yearOption.dataset.calendarYearOption));
    renderCalendar();
    return;
  }
  const calendarDayButton = e.target.closest("[data-calendar-day]");
  if (calendarDayButton) {
    selectedCalendarDay = Number(calendarDayButton.dataset.calendarDay);
    localStorage.setItem("juristicSelectedCalendarDay", String(selectedCalendarDay));
    const firstEvent = calendarEventsForDay(selectedCalendarDay)[0];
    if (firstEvent) {
      selectedCalendarEventId = firstEvent.id;
      localStorage.setItem("juristicSelectedCalendarEvent", selectedCalendarEventId);
    }
    renderCalendar();
    return;
  }
  const calendarEventButton = e.target.closest("[data-calendar-event]");
  if (calendarEventButton?.dataset.calendarEvent) {
    selectedCalendarEventId = calendarEventButton.dataset.calendarEvent;
    localStorage.setItem("juristicSelectedCalendarEvent", selectedCalendarEventId);
    const item = selectedCalendarEvent();
    const eventDate = new Date(`${item.date}T00:00:00`);
    selectedCalendarMonth = eventDate.getMonth();
    selectedCalendarYear = eventDate.getFullYear();
    selectedCalendarDay = item.day;
    localStorage.setItem("juristicSelectedCalendarMonth", String(selectedCalendarMonth));
    localStorage.setItem("juristicSelectedCalendarYear", String(selectedCalendarYear));
    localStorage.setItem("juristicSelectedCalendarDay", String(selectedCalendarDay));
    renderCalendar();
    return;
  }
  if (e.target.closest("#connectGoogleCalendarBtn")) {
    googleCalendarConnected = true;
    localStorage.setItem("juristicGoogleCalendarConnected", "true");
    renderCalendar();
    showToast(currentLang === "th" ? "ทดสอบเชื่อมต่อ Google Calendar สำเร็จ" : "Google Calendar mock connected");
    return;
  }
  if (e.target.closest("#shareCalendarBtn")) {
    shareCalendarToPhone().catch(() => showToast(currentLang === "th" ? "แชร์ปฏิทินไม่สำเร็จ ลองดาวน์โหลดไฟล์ .ics อีกครั้ง" : "Calendar share failed"));
    return;
  }
  const backhouseMove = e.target.closest("[data-backhouse-move]");
  if (backhouseMove && canManageAnnouncements()) {
    const index = Number(backhouseMove.dataset.index);
    const target = backhouseMove.dataset.backhouseMove === "up" ? index - 1 : index + 1;
    if (announcements[target]) {
      [announcements[index], announcements[target]] = [announcements[target], announcements[index]];
      backhouseDirty = true;
      renderBackhouse();
      renderAnnouncements();
    }
    return;
  }
  const backhouseRemove = e.target.closest("[data-backhouse-remove]");
  if (backhouseRemove && canManageAnnouncements()) {
    announcements.splice(Number(backhouseRemove.dataset.backhouseRemove), 1);
    backhouseDirty = true;
    renderBackhouse();
    renderAnnouncements();
    return;
  }
  if (e.target.closest("#saveBackhouseBtn") && canManageAnnouncements()) {
    saveBackhouseAnnouncements();
    backhouseDirty = false;
    renderBackhouse();
    renderAnnouncements();
    showToast(currentLang === "th" ? "บันทึกประกาศและอัปเดตหน้า ภาพรวม แล้ว" : "Announcements saved and dashboard updated");
    return;
  }
  if (e.target.closest("#saveOrganizationBtn") && canManageOrganization()) {
    saveOrganizationDraft();
    renderOrganization();
    renderProjectOrganization();
    addLog("ORGANIZATION_CHART_SAVED", `Organization Chart saved by ${getUserName(currentUser)}`);
    showToast("บันทึก Organization Chart และอัปเดตหน้าภาพรวมแล้ว");
    return;
  }
  if (e.target.closest("#clearOrganizationBtn") && canManageOrganization()) {
    organizationDraftAssignments = {};
    organizationDirty = true;
    renderOrganization();
    showToast("ล้างโปรไฟล์ใน Organization Slots แล้ว กดบันทึกเพื่ออัปเดตหน้าภาพรวม");
    return;
  }
  const draftClearOrg = e.target.closest("[data-org-clear]");
  if (draftClearOrg && canManageOrganization()) {
    delete organizationDraftAssignments[draftClearOrg.dataset.orgClear];
    organizationDirty = true;
    renderOrganization();
    showToast("ล้าง Slot แล้ว กดบันทึกเพื่ออัปเดตหน้าภาพรวม");
    return;
  }
  const clearOrg = e.target.closest("[data-org-clear]");
  if (clearOrg && canManageOrganization()) {
    delete organizationAssignments[clearOrg.dataset.orgClear];
    saveOrganizationAssignments();
    renderOrganization();
    renderProjectOrganization();
    addLog("ORGANIZATION_SLOT_CLEARED", `${clearOrg.dataset.orgClear} cleared by ${getUserName(currentUser)}`);
    showToast("ล้าง Organization Slot แล้ว");
    return;
  }
  if (e.target.closest("#notificationBtn")) {
    renderNotifications();
    $("#notificationPanel").classList.toggle("hidden");
    return;
  }
  if (!e.target.closest("#notificationPanel")) $("#notificationPanel")?.classList.add("hidden");
  const commonBoardStatus = e.target.closest("[data-common-board-status]");
  if (commonBoardStatus) {
    $("#statusFilter").value = commonBoardStatus.dataset.commonBoardStatus;
    renderJobList();
    return;
  }
  const commonBoardRange = e.target.closest("[data-common-board-range]");
  if (commonBoardRange) {
    commonWorkRange = commonBoardRange.dataset.commonBoardRange;
    localStorage.setItem("juristicCommonWorkRange", commonWorkRange);
    renderJobList();
    return;
  }
  const navGroupToggle = e.target.closest(".nav-section-label[data-nav-group]");
  if (navGroupToggle) {
    const key = navGroupToggle.dataset.navGroup;
    collapsedSidebarGroups[key] = !collapsedSidebarGroups[key];
    localStorage.setItem("juristicCollapsedSidebarGroups", JSON.stringify(collapsedSidebarGroups));
    organizeSidebarGroups();
    return;
  }
  const routineOpen = e.target.closest("[data-routine-open]");
  if (routineOpen) {
    activeRoutineTaskId = activeRoutineTaskId === routineOpen.dataset.routineOpen ? "" : routineOpen.dataset.routineOpen;
    renderRoutineModule();
    return;
  }
  const routineStart = e.target.closest("[data-routine-start]");
  if (routineStart) {
    const task = routineTasks.find(item => item.id === routineStart.dataset.routineStart);
    if (task) {
      task.started_at = task.started_at || new Date().toISOString();
      setRoutineTaskStatus(task, "in_progress", "task_started", `${task.title} started by ${getUserName(currentUser)}`);
      activeRoutineTaskId = task.id;
      renderRoutineModule();
      showToast(currentLang === "th" ? "เริ่มงานแล้ว" : "Task started");
    }
    return;
  }
  const routineSubmit = e.target.closest("[data-routine-submit]");
  if (routineSubmit) {
    const task = routineTasks.find(item => item.id === routineSubmit.dataset.routineSubmit);
    if (task) {
      const error = validateRoutineSubmission(task);
      if (error) return showToast(error);
      task.submitted_at = new Date().toISOString();
      const end = new Date(`${task.task_date}T${task.end_time || "23:59"}:00`);
      task.is_late = new Date(task.submitted_at) > end;
      setRoutineTaskStatus(task, task.is_late ? "late_submitted" : "pending_verification", "task_submitted", `${task.title} submitted for verification`);
      renderRoutineModule();
      showToast(currentLang === "th" ? "ส่งงานเข้ารอตรวจรับแล้ว" : "Submitted for verification");
    }
    return;
  }
  const routineApprove = e.target.closest("[data-routine-approve]");
  if (routineApprove) {
    const task = routineTasks.find(item => item.id === routineApprove.dataset.routineApprove);
    if (task && canVerifyRoutineTask(currentUser, task) && confirm(currentLang === "th" ? "ยืนยันอนุมัติงานนี้?" : "Approve this task?")) {
      task.verified_at = new Date().toISOString();
      task.verified_by = currentUser.id;
      setRoutineTaskStatus(task, "completed", "task_approved", `${task.title} approved by ${getUserName(currentUser)}`);
      renderRoutineModule();
      showToast(currentLang === "th" ? "อนุมัติและปิดงานแล้ว" : "Approved and completed");
    }
    return;
  }
  const routineReject = e.target.closest("[data-routine-reject]");
  if (routineReject) {
    const task = routineTasks.find(item => item.id === routineReject.dataset.routineReject);
    if (task && canVerifyRoutineTask(currentUser, task)) {
      const reason = prompt(currentLang === "th" ? "กรอกเหตุผลการตีกลับ" : "Reject reason");
      if (!reason) return;
      task.reject_reason = reason;
      setRoutineTaskStatus(task, "rework", "task_rejected", reason);
      renderRoutineModule();
      showToast(currentLang === "th" ? "ตีกลับงานแล้ว" : "Task rejected");
    }
    return;
  }
  const routineRepair = e.target.closest("[data-routine-create-repair]");
  if (routineRepair) {
    const task = routineTasks.find(item => item.id === routineRepair.dataset.routineCreateRepair);
    if (task) {
      createJob({ title: `ต่อเนื่องจาก Daily PM: ${task.title}`, issueDescription: `พบความผิดปกติจาก Routine Task ${task.id}\nพื้นที่: ${task.area_name}\nหมายเหตุ: ${task.note || task.abnormal_note || "-"}`, roomNo: task.area_name, mainCategory: "common", category: task.department === "housekeeping" ? "cleaning" : task.department === "security" ? "security" : "building", priority: "high", jobDate: todayIso(), assignee: "", source: "Routine Daily PM", routineTaskId: task.id, initialAttachments: routineTaskAttachmentsFor(task.id, "image").map(item => item.stamped_file_url || item.file_url).slice(0, 3) });
      addRoutineLog(task.id, "repair_job_created_from_routine", task.status, task.status, `Repair job created from ${task.id}`);
      saveRoutineData();
      renderAll();
      showToast(currentLang === "th" ? "สร้างงานซ่อมต่อเนื่องแล้ว" : "Repair ticket created");
    }
    return;
  }
  const duplicateTemplate = e.target.closest("[data-routine-duplicate-template]");
  if (duplicateTemplate && canManageRoutineTemplates()) {
    const template = routineTemplates.find(item => item.id === duplicateTemplate.dataset.routineDuplicateTemplate);
    if (template) {
      const copy = JSON.parse(JSON.stringify(template));
      copy.id = `rtemp-copy-${Date.now()}`;
      copy.title = `${copy.title} (Copy)`;
      copy.created_at = new Date().toISOString();
      copy.updated_at = copy.created_at;
      routineTemplates.push(copy);
      saveRoutineData();
      addLog("ROUTINE_TEMPLATE_DUPLICATED", `${template.title} duplicated by ${getUserName(currentUser)}`);
      renderRoutineModule();
    }
    return;
  }
  const toggleTemplate = e.target.closest("[data-routine-toggle-template]");
  if (toggleTemplate && canManageRoutineTemplates()) {
    const template = routineTemplates.find(item => item.id === toggleTemplate.dataset.routineToggleTemplate);
    if (template) {
      template.is_active = !template.is_active;
      template.updated_at = new Date().toISOString();
      saveRoutineData();
      addRoutineLog("", "template_updated", "", "", `${template.title} ${template.is_active ? "enabled" : "disabled"}`);
      renderRoutineModule();
    }
    return;
  }
  if (e.target.closest("#exportRoutineCsvBtn")) {
    const rows = [["task_id", "date", "title", "department", "area", "assignee", "status", "started_at", "submitted_at", "verified_at", "is_late"]];
    routineTasks.filter(task => routineTaskVisibleToUser(task)).forEach(task => rows.push([task.id, task.task_date, task.title, task.department, task.area_name, getUserName(getUser(task.assignee_id)), task.status, task.started_at, task.submitted_at, task.verified_at, task.is_late ? "yes" : "no"]));
    downloadTextFile("routine-daily-pm-report.csv", rows.map(row => row.map(value => `"${String(value || "").replaceAll('"', '""')}"`).join(",")).join("\n"), "text/csv;charset=utf-8");
    showToast(currentLang === "th" ? "Export รายงาน Daily PM แล้ว" : "Routine report exported");
    return;
  }
  const viewButton = e.target.closest("[data-view-mode][data-view-scope]");
  if (viewButton) {
    const scope = viewButton.dataset.viewScope;
    setViewMode(scope, viewButton.dataset.viewMode);
    if (scope === "dashboard") renderDashboardJobs();
    else if (scope === "residents") renderResidents();
    else if (scope === "team") renderTeam();
    else renderJobList();
    return;
  }
  const chatScopeButton = e.target.closest("[data-chat-scope]");
  if (chatScopeButton) {
    chatScope = chatScopeButton.dataset.chatScope;
    activeChatRoomId = "";
    renderChat();
    return;
  }
  const chatRoomButton = e.target.closest("[data-chat-room]");
  if (chatRoomButton) {
    activeChatRoomId = chatRoomButton.dataset.chatRoom;
    renderChat();
    return;
  }
  const nav = e.target.closest("[data-view]"); if (nav) navigate(nav.dataset.view);
  const go = e.target.closest("[data-go]"); if (go) navigate(go.dataset.go);
  const row = e.target.closest("[data-job-id]"); if (row) { $("#notificationPanel")?.classList.add("hidden"); openJob(row.dataset.jobId); }
  const exportCsvButton = e.target.closest("[data-export-job-csv]");
  if (exportCsvButton) {
    exportJobCsv(exportCsvButton.dataset.exportJobCsv);
    return;
  }
  const exportPdfButton = e.target.closest("[data-export-job-pdf]");
  if (exportPdfButton) {
    printJobPdf(exportPdfButton.dataset.exportJobPdf);
    return;
  }
  if (e.target.closest("[data-close-modal]") || e.target === $("#jobModal")) $("#jobModal").classList.add("hidden");
  if (e.target.closest("[data-close-create]") || e.target === $("#createModal")) $("#createModal").classList.add("hidden");
  const card = e.target.closest("[data-status-filter]");
  if (card) navigate("pool", card.dataset.statusFilter === "waiting" ? "open" : card.dataset.statusFilter);
  const coAdminButton = e.target.closest("[data-toggle-coadmin]");
  if (coAdminButton && canGrantCoAdmin()) {
    const user = getUser(coAdminButton.dataset.toggleCoadmin);
    user.isCoAdmin = !user.isCoAdmin;
    teamOverrides[user.id] = { ...(teamOverrides[user.id] || {}), isCoAdmin: user.isCoAdmin };
    saveTeamOverrides();
    addLog(user.isCoAdmin ? "CO_ADMIN_GRANTED" : "CO_ADMIN_REVOKED", `${getUserName(user)} ${user.isCoAdmin ? "granted" : "revoked"} by ${getUserName(currentUser)}`);
    renderTeam();
    showToast(user.isCoAdmin ? (currentLang === "th" ? "มอบสิทธิ์ Co-Admin แล้ว" : "Co-Admin granted") : (currentLang === "th" ? "ถอดสิทธิ์ Co-Admin แล้ว" : "Co-Admin revoked"));
  }
  const deleteButton = e.target.closest("[data-delete-user]");
  if (deleteButton && canManageTeam()) {
    requestOrDeleteUser(deleteButton.dataset.deleteUser);
  }
  const saveTeamButton = e.target.closest("[data-save-team]");
  if (saveTeamButton && canManageTeam()) {
    saveTeamMember(saveTeamButton.dataset.saveTeam);
  }
  const savePermissionsButton = e.target.closest("[data-save-permissions]");
  if (savePermissionsButton && canManagePermissions()) {
    saveUserPermissions(savePermissionsButton.dataset.savePermissions);
  }
  const resetPermissionsButton = e.target.closest("[data-reset-permissions]");
  if (resetPermissionsButton && canManagePermissions()) {
    resetUserPermissions(resetPermissionsButton.dataset.resetPermissions);
  }
  if (e.target.closest("#previewBulkPermissionBtn") && canManagePermissions()) {
    const preview = $("#bulkPermissionPreview");
    if (preview) preview.innerHTML = bulkPermissionPreviewHtml();
  }
  if (e.target.closest("#applyBulkPermissionBtn") && canManagePermissions()) {
    applyBulkPermissions();
  }
  const confirmDelete = e.target.closest("[data-confirm-delete]");
  if (confirmDelete && currentUser.role === "admin") {
    deleteUserNow(confirmDelete.dataset.confirmDelete, confirmDelete.dataset.requestJob);
  }
  const verifyButton = e.target.closest("[data-verify-completion]");
  if (verifyButton && verifyCompletion(verifyButton.dataset.verifyCompletion)) {
    $("#jobModal").classList.add("hidden");
    renderAll();
    showToast("ยืนยันปิดงานเรียบร้อยแล้ว");
  }
  if (e.target.closest("#addUserBtn")) openUserModal();
  if (e.target.closest("#accountMenuBtn")) $("#accountMenu").classList.toggle("hidden");
  if (e.target.closest("#changeOwnPasswordBtn")) {
    openPasswordModal(currentUser.id);
    $("#accountMenu").classList.add("hidden");
  }
  if (e.target.closest("#exportEmployeesBtn") && currentUser.role === "admin") {
    exportEmployeesCsv();
    $("#accountMenu").classList.add("hidden");
    showToast(currentLang === "th" ? "Export รายชื่อพนักงานแล้ว" : "Employees exported");
  }
  if (e.target.closest("#exportEmployeesPageBtn") && canManageTeam()) {
    exportEmployeesCsv();
    showToast(currentLang === "th" ? "Export รายชื่อทีมงานแล้ว" : "Team exported");
  }
  if (e.target.closest("#importEmployeesPageBtn") && canManageTeam()) {
    $("#employeeCsvInput").click();
  }
  if (e.target.closest("#addResidentRoomBtn") && canManageResidents()) {
    if (residentRooms.length >= 882) return showToast("เพิ่มเลขห้องได้สูงสุด 882 ห้อง");
    const nextNo = String(residentRooms.length + 1).padStart(4, "0");
    const room = {
      id: `room-${Date.now()}`,
      roomNo1: `NEW-${nextNo}`,
      roomNo2: `NEW${nextNo}`,
      building: "",
      commonFeeStatus: "unpaid",
      owner: { firstName: "", lastName: "", nickName: "", phone: "", status: "owner", isCurrentResident: false },
      occupants: [],
      cars: []
    };
    residentRooms.unshift(room);
    saveResidentRooms();
    addLog("RESIDENT_ROOM_CREATED", `${room.roomNo1} created by ${getUserName(currentUser)}`);
    renderResidents();
    openResidentRoom(room.id);
  }
  const residentRow = e.target.closest("[data-open-resident]");
  if (residentRow && canManageResidents()) openResidentRoom(residentRow.dataset.openResident);
  if (e.target.closest("[data-close-resident]") || e.target === $("#residentModal")) $("#residentModal").classList.add("hidden");
  const saveResident = e.target.closest("[data-save-resident-room]");
  if (saveResident && canManageResidents()) {
    const room = residentRooms.find(item => item.id === saveResident.dataset.saveResidentRoom);
    if (room) {
      room.roomNo1 = document.querySelector(`[data-room-no1="${room.id}"]`).value.trim();
      room.roomNo2 = document.querySelector(`[data-room-no2="${room.id}"]`).value.trim();
      room.building = document.querySelector(`[data-room-building="${room.id}"]`).value.trim();
      room.commonFeeStatus = document.querySelector(`[data-room-payment="${room.id}"]`).value;
      room.owner = {
        firstName: document.querySelector(`[data-owner-first="${room.id}"]`).value.trim(),
        lastName: document.querySelector(`[data-owner-last="${room.id}"]`).value.trim(),
        nickName: document.querySelector(`[data-owner-nick="${room.id}"]`).value.trim(),
        phone: document.querySelector(`[data-owner-phone="${room.id}"]`).value.trim(),
        status: "owner",
        isCurrentResident: false,
        isPrimary: false
      };
      room.personOrder = $$("[data-person-card]").map(card => card.dataset.personCard);
      const currentKeys = $$(`[data-current-person="${room.id}"]:checked`).map(input => input.value);
      room.owner.isCurrentResident = currentKeys.includes("owner");
      (room.occupants || []).forEach((person, index) => {
        person.isCurrentResident = currentKeys.includes(`occupant-${index}`);
        person.isPrimary = false;
      });
      saveResidentRooms();
      addLog("RESIDENT_ROOM_UPDATED", `${room.roomNo1} updated by ${getUserName(currentUser)}`);
      renderResidents();
      $("#residentModal").classList.add("hidden");
      showToast("บันทึกข้อมูลลูกบ้านแล้ว");
    }
  }
  if (e.target.closest("#exportResidentsBtn") && (canManageResidents() || hasActionPermission("importExport"))) {
    exportResidentsCsv();
    showToast("Export ข้อมูลลูกบ้านแล้ว");
  }
  if (e.target.closest("#importResidentsBtn") && (canManageResidents() || hasActionPermission("importExport"))) {
    $("#residentCsvInput").click();
  }
  const addOccupant = e.target.closest("[data-add-occupant]");
  if (addOccupant && canManageResidents()) {
    const room = residentRooms.find(item => item.id === addOccupant.dataset.addOccupant);
    if (room) {
      room.occupants = room.occupants || [];
      room.occupants.push({
        firstName: $("#newOccFirst").value.trim(),
        lastName: $("#newOccLast").value.trim(),
        nickName: $("#newOccNick").value.trim(),
        phone: $("#newOccPhone").value.trim(),
        status: $("#newOccStatus").value,
        isCurrentResident: false
      });
      saveResidentRooms();
      addLog("RESIDENT_OCCUPANT_ADDED", `${room.roomNo1} occupant added by ${getUserName(currentUser)}`);
      renderResidents();
      openResidentRoom(room.id);
    }
  }
  const removeOccupant = e.target.closest("[data-remove-occupant]");
  if (removeOccupant && canManageResidents()) {
    const room = residentRooms.find(item => item.id === removeOccupant.dataset.removeOccupant);
    if (room) {
      room.occupants.splice(Number(removeOccupant.dataset.index), 1);
      saveResidentRooms();
      addLog("RESIDENT_OCCUPANT_REMOVED", `${room.roomNo1} occupant removed by ${getUserName(currentUser)}`);
      renderResidents();
      openResidentRoom(room.id);
    }
  }
  const addCar = e.target.closest("[data-add-car]");
  if (addCar && canManageResidents()) {
    const room = residentRooms.find(item => item.id === addCar.dataset.addCar);
    if (room) {
      room.cars = room.cars || [];
      room.cars.push({
        prefix: $("#newCarPrefix").value.trim(),
        number: $("#newCarNumber").value.trim(),
        province: $("#newCarProvince").value.trim(),
        brand: $("#newCarBrand").value.trim(),
        model: $("#newCarModel").value.trim(),
        color: $("#newCarColor").value.trim()
      });
      saveResidentRooms();
      addLog("RESIDENT_CAR_ADDED", `${room.roomNo1} car added by ${getUserName(currentUser)}`);
      renderResidents();
      openResidentRoom(room.id);
    }
  }
  const removeCar = e.target.closest("[data-remove-car]");
  if (removeCar && canManageResidents()) {
    const room = residentRooms.find(item => item.id === removeCar.dataset.removeCar);
    if (room) {
      room.cars.splice(Number(removeCar.dataset.index), 1);
      saveResidentRooms();
      addLog("RESIDENT_CAR_REMOVED", `${room.roomNo1} car removed by ${getUserName(currentUser)}`);
      renderResidents();
      openResidentRoom(room.id);
    }
  }
  const movePerson = e.target.closest("[data-move-person]");
  if (movePerson && canManageResidents()) {
    const card = movePerson.closest("[data-person-card]");
    const list = movePerson.closest(".resident-primary-list");
    if (card && list && movePerson.dataset.movePerson === "up" && card.previousElementSibling) list.insertBefore(card, card.previousElementSibling);
    if (card && list && movePerson.dataset.movePerson === "down" && card.nextElementSibling) list.insertBefore(card.nextElementSibling, card);
  }
  if (e.target.closest("[data-close-user]") || e.target === $("#userModal")) $("#userModal").classList.add("hidden");
  if (e.target.closest("[data-close-password]") || e.target === $("#passwordModal")) $("#passwordModal").classList.add("hidden");
});
document.addEventListener("dragstart", e => {
  const card = e.target.closest("[data-org-drag-user]");
  if (!card || !canManageOrganization()) return;
  e.dataTransfer.setData("text/plain", card.dataset.orgDragUser);
  e.dataTransfer.effectAllowed = "copy";
});
document.addEventListener("dragover", e => {
  const slot = e.target.closest("[data-org-slot]");
  if (!slot || !canManageOrganization()) return;
  e.preventDefault();
  slot.classList.add("drag-over");
});
document.addEventListener("dragleave", e => {
  const slot = e.target.closest("[data-org-slot]");
  if (slot) slot.classList.remove("drag-over");
});
document.addEventListener("drop", e => {
  const slot = e.target.closest("[data-org-slot]");
  if (!slot || !canManageOrganization()) return;
  e.preventDefault();
  slot.classList.remove("drag-over");
  const userId = e.dataTransfer.getData("text/plain");
  const user = getUser(userId);
  if (!user) return;
  const expectedDept = slot.dataset.acceptDept;
  if (inferDepartment(user) !== expectedDept) {
    return showToast(`ตำแหน่งนี้รับเฉพาะ ${departmentLabel(expectedDept)}`);
  }
  Object.keys(organizationDraftAssignments).forEach(slotId => {
    if (organizationDraftAssignments[slotId] === userId) delete organizationDraftAssignments[slotId];
  });
  organizationDraftAssignments[slot.dataset.orgSlot] = userId;
  organizationDirty = true;
  renderOrganization();
  showToast("จัดวางโปรไฟล์แล้ว กดบันทึกเพื่อแสดงในหน้าภาพรวม");
  return;
  showToast("บันทึกตำแหน่งใน Organization Chart แล้ว");
});
document.addEventListener("submit", async e => {
  const routineForm = e.target.closest("[data-routine-result-form]");
  if (routineForm) {
    e.preventDefault();
    const task = routineTasks.find(item => item.id === routineForm.dataset.routineResultForm);
    if (!task) return;
    const data = new FormData(routineForm);
    routineChecklistItems(task).forEach(item => {
      const raw = item.input_type === "checkbox" ? data.has(item.id) : String(data.get(item.id) || "").trim();
      const existing = routineTaskResults.find(row => row.task_id === task.id && row.checklist_item_id === item.id);
      if (existing) {
        existing.value = raw;
        existing.updated_at = new Date().toISOString();
      } else {
        routineTaskResults.push({ id: `rres-${Date.now()}-${Math.random().toString(16).slice(2)}`, task_id: task.id, checklist_item_id: item.id, value: raw, note: "", created_at: new Date().toISOString(), updated_at: new Date().toISOString() });
      }
    });
    task.note = String(data.get("note") || "").trim();
    const photos = [...(routineForm.elements.routinePhotos?.files || [])];
    const videos = [...(routineForm.elements.routineVideos?.files || [])];
    if (photos.length) await addRoutineAttachments(task, photos, "image");
    if (videos.length) await addRoutineAttachments(task, videos, "video");
    addRoutineLog(task.id, "checklist_updated", task.status, task.status, `${task.title} checklist updated`);
    saveRoutineData();
    renderRoutineModule();
    showToast(currentLang === "th" ? "บันทึก Checklist และหลักฐานแล้ว" : "Checklist and evidence saved");
    return;
  }
  const chatForm = e.target.closest("[data-chat-send]");
  if (chatForm) {
    e.preventDefault();
    const thread = chatThreads.find(item => item.id === chatForm.dataset.chatSend);
    const input = chatForm.querySelector("[name='message']");
    const text = input?.value?.trim();
    if (!thread || !text) return;
    thread.messages = thread.messages || [];
    thread.messages.push({
      id: `msg-${Date.now()}`,
      by: currentUser.id,
      at: new Date().toISOString(),
      text
    });
    thread.updatedAt = new Date().toISOString();
    activeChatRoomId = thread.id;
    saveChatThreads();
    renderChat();
    showToast(currentLang === "th" ? "ส่งข้อความแล้ว" : "Message sent");
    return;
  }
  const backhouseForm = e.target.closest("#backhouseUploadForm");
  if (backhouseForm) {
    e.preventDefault();
    if (!canManageOrganization()) return;
    const titleInput = backhouseForm.querySelector("[name='title']");
    const summaryInput = backhouseForm.querySelector("[name='summary']");
    const fileInput = backhouseForm.querySelector("[name='announcementFile']");
    const file = fileInput?.files?.[0];
    if (!file) return showToast(currentLang === "th" ? "กรุณาแนบเอกสารหรือรูปภาพ" : "Please attach a document or image");
    if (file.size > 12 * 1024 * 1024) return showToast(currentLang === "th" ? "ไฟล์ต้องมีขนาดไม่เกิน 12MB" : "File must be 12MB or less");
    const isPdf = file.type === "application/pdf" || file.name.toLowerCase().endsWith(".pdf");
    const src = await readFileAsDataUrl(file);
    announcements.unshift({
      id: `ann-${Date.now()}`,
      title: { th: titleInput.value.trim(), en: titleInput.value.trim() },
      type: isPdf ? "pdf" : "image",
      date: new Date().toISOString().slice(0, 10),
      audience: { th: "แสดงในหน้าภาพรวม", en: "Dashboard announcement" },
      summary: { th: summaryInput.value.trim(), en: summaryInput.value.trim() },
      pages: [src]
    });
    backhouseDirty = true;
    $("#backhouseUploadModal")?.classList.add("hidden");
    backhouseForm.reset();
    renderBackhouse();
    renderAnnouncements();
    showToast(currentLang === "th" ? "เพิ่มประกาศแล้ว กดบันทึกเพื่อยืนยัน" : "Announcement added. Save to confirm.");
    return;
  }
  const googleEditForm = e.target.closest("[data-edit-google-form-job]");
  if (googleEditForm) {
    e.preventDefault();
    const result = saveGoogleFormJobDetails(googleEditForm.dataset.editGoogleFormJob, Object.fromEntries(new FormData(googleEditForm)));
    if (!result.ok) return showToast(result.message);
    $("#jobModal").classList.add("hidden");
    renderAll();
    showToast(currentLang === "th" ? "บันทึกรายละเอียดและเก็บค่าเดิมลง Log งานแล้ว" : "Details saved and previous values were stored in the job log");
    return;
  }
  const assignForm = e.target.closest("[data-assign-job]");
  if (assignForm) {
    e.preventDefault();
    const job = normalizeJob(jobs.find(j => j.id === assignForm.dataset.assignJob));
    const data = Object.fromEntries(new FormData(assignForm));
    let extra = {};
    if (job.startTime) {
      const conflicts = checkScheduleConflict(data.assignee, job.jobDate, job.startTime, job.endTime, job.id);
      if (conflicts.length) {
        const list = conflicts.map(item => `${item.startTime}-${effectiveEndTime(item.startTime, item.endTime)} ${item.id} ${getText(item.title)}`).join("\n");
        if (!confirm(`ผู้ได้รับมอบหมายคนนี้มีงานอยู่ในช่วงเวลาดังกล่าว:\n\n${list}\n\nยืนยันที่จะมอบหมายงานซ้อนหรือไม่?`)) return;
        extra = { hasScheduleConflict: true, conflictConfirmedAt: new Date().toISOString(), conflictConfirmedBy: currentUser.id };
      }
    }
    assignJob(job.id, data.assignee, { category: data.category, mainCategory: data.mainCategory, dueDate: data.dueDate, slaNote: data.slaNote, ...extra });
    $("#jobModal").classList.add("hidden"); renderAll(); showToast(t("toast.assigned"));
  }
  const updateForm = e.target.closest("[data-update-job]");
  if (updateForm) {
    e.preventDefault();
    const job = normalizeJob(jobs.find(j => j.id === updateForm.dataset.updateJob));
    const data = Object.fromEntries(new FormData(updateForm));
    const files = getPhotoChoiceFiles(updateForm, "statusAttachments");
    if (files.length > 3) return showToast("แนบรูปได้สูงสุด 3 รูป");
    if (files.some(file => file.size > 5 * 1024 * 1024)) return showToast("รูปภาพต้องมีขนาดไม่เกิน 5MB ต่อไฟล์");
    const attachments = await Promise.all(files.map(fileToDataUrl));
    const result = updateJobStatus(job.id, {
      status: data.status,
      inspectionDate: data.inspectionDate,
      updateDate: data.updateDate,
      cause: data.cause?.trim(),
      solution: data.solution?.trim(),
      note: data.note?.trim(),
      closePin: data.closePin?.trim(),
      completedWorkDate: data.completedWorkDate,
      nextUpdateDate: data.nextUpdateDate,
      noPinAvailable: !!data.noPinAvailable,
      noPinReason: data.noPinReason?.trim(),
      attachments
    });
    if (result.errors?.length) return showToast(result.errors[0]);
    $("#jobModal").classList.add("hidden"); renderAll(); showToast(t("toast.statusUpdated"));
  }
});
function getPhotoChoiceFiles(form, baseName) {
  const elements = form.elements || {};
  return [
    ...(elements[`${baseName}Camera`]?.files || []),
    ...(elements[`${baseName}Gallery`]?.files || []),
    ...(elements[baseName]?.files || [])
  ];
}
function getSinglePhotoChoiceFile(form, baseName) {
  return getPhotoChoiceFiles(form, baseName)[0] || null;
}
function readFileAsDataUrl(file) {
  return new Promise(resolve => {
    const reader = new FileReader();
    reader.onload = () => resolve(reader.result);
    reader.readAsDataURL(file);
  });
}
function loadImage(src) {
  return new Promise((resolve, reject) => {
    const image = new Image();
    image.onload = () => resolve(image);
    image.onerror = reject;
    image.src = src;
  });
}
function imageStampText() {
  const userName = currentUser ? getUserName(currentUser) : "System";
  const stampTime = new Date().toLocaleString(currentLang === "th" ? "th-TH" : "en-US");
  return `${userName} · ${stampTime}`;
}
async function fileToDataUrl(file) {
  const dataUrl = await readFileAsDataUrl(file);
  if (!file?.type?.startsWith("image/")) return dataUrl;
  try {
    const image = await loadImage(dataUrl);
    const canvas = document.createElement("canvas");
    const maxSide = 1600;
    const scale = Math.min(1, maxSide / Math.max(image.naturalWidth || image.width, image.naturalHeight || image.height));
    canvas.width = Math.max(1, Math.round((image.naturalWidth || image.width) * scale));
    canvas.height = Math.max(1, Math.round((image.naturalHeight || image.height) * scale));
    const ctx = canvas.getContext("2d");
    ctx.drawImage(image, 0, 0, canvas.width, canvas.height);
    const stamp = imageStampText();
    const fontSize = Math.max(14, Math.round(canvas.width * 0.024));
    const padding = Math.max(10, Math.round(fontSize * 0.7));
    ctx.font = `700 ${fontSize}px "Anuphan", "IBM Plex Sans Thai", Arial, sans-serif`;
    const textWidth = ctx.measureText(stamp).width;
    const boxWidth = Math.min(canvas.width, textWidth + padding * 2);
    const boxHeight = Math.round(fontSize + padding * 1.5);
    ctx.fillStyle = "rgba(7, 28, 22, 0.68)";
    ctx.fillRect(0, canvas.height - boxHeight, boxWidth, boxHeight);
    ctx.fillStyle = "#ffffff";
    ctx.fillText(stamp, padding, canvas.height - padding);
    return canvas.toDataURL("image/jpeg", 0.88);
  } catch (error) {
    console.warn("Image stamp failed, using original file", error);
    return dataUrl;
  }
}
$("#userForm").addEventListener("submit", async e => {
  e.preventDefault();
  if (!canManageTeam()) return;
  const form = e.currentTarget;
  const data = Object.fromEntries(new FormData(form));
  const profileFile = getSinglePhotoChoiceFile(form, "profileImage");
  const id = data.id || `u${String(Date.now()).slice(-6)}`;
  const department = data.department || "juristic";
  const role = department === "committee" || department === "juristic" || department === "technician" || department === "housekeeping" || department === "security_staff" || department === "gardener" || department === "contractor" ? "staff" : "resident";
  const newUser = {
    id,
    firstName: data.firstName.trim(),
    lastName: data.lastName.trim(),
    name: fullName(data.firstName.trim(), data.lastName.trim()),
    enName: fullName(data.firstName.trim(), data.lastName.trim()),
    nickName: data.nickName.trim(),
    position: data.position.trim(),
    department,
    room: data.room.trim().toUpperCase(),
    role,
    roleKey: departmentToRoleKey(department),
    password: "1234",
    assignL1: !!data.assignL1,
    assignL2: !!data.assignL2,
    profileImage: profileFile ? await fileToDataUrl(profileFile) : ""
  };
  users.push(newUser);
  customUsers.push(newUser);
  saveCustomUsers();
  teamOverrides[newUser.id] = { ...newUser };
  saveTeamOverrides();
  addLog("USER_ADDED", `${getUserName(newUser)} added by ${getUserName(currentUser)}`);
  $("#userModal").classList.add("hidden");
  renderAll();
  showToast(currentLang === "th" ? "เพิ่มผู้ใช้งานแล้ว" : "User added");
});
$("#passwordForm").addEventListener("submit", e => {
  e.preventDefault();
  const data = Object.fromEntries(new FormData(e.currentTarget));
  const user = getUser(data.userId);
  if (!user) return;
  const canChange = currentUser.role === "admin" || currentUser.id === user.id;
  if (!canChange) return;
  user.password = data.password.trim();
  persistUserFields(user);
  addLog("PASSWORD_CHANGED", `${getUserName(user)} password changed by ${getUserName(currentUser)}`);
  $("#passwordModal").classList.add("hidden");
  renderTeam();
  showToast(currentLang === "th" ? "เปลี่ยนรหัสผ่านแล้ว" : "Password changed");
});
$("#createJobBtn").addEventListener("click", () => {
  if (!hasActionPermission("createJob")) return showToast(currentLang === "th" ? "บัญชีนี้ไม่มีสิทธิ์สร้างงาน" : "This account cannot create jobs");
  $("#createJobForm").elements.jobDate.value = new Date().toISOString().slice(0, 10);
  if ($("#createDueDate")) $("#createDueDate").value = addDaysIso(7);
  $("#createModal").classList.remove("hidden");
});
$("#createJobForm").addEventListener("submit", async e => {
  e.preventDefault();
  if (!hasActionPermission("createJob")) return showToast(currentLang === "th" ? "บัญชีนี้ไม่มีสิทธิ์สร้างงาน" : "This account cannot create jobs");
  const form = e.currentTarget;
  const data = Object.fromEntries(new FormData(form));
  if (data.endTime && !data.startTime) return showToast("ถ้ากรอกเวลาสิ้นสุด ต้องกรอกเวลาเริ่มงานด้วย");
  const files = getPhotoChoiceFiles(form, "initialAttachments");
  if (files.length > 3) return showToast("แนบรูปภาพได้สูงสุด 3 รูป");
  if (files.some(file => file.size > 5 * 1024 * 1024)) return showToast("รูปภาพต้องมีขนาดไม่เกิน 5MB ต่อไฟล์");
  let conflictExtra = {};
  if (data.assignee && data.startTime) {
    const conflicts = checkScheduleConflict(data.assignee, data.jobDate, data.startTime, data.endTime);
    if (conflicts.length) {
      const assignee = getUser(data.assignee);
      const list = conflicts.map(job => `${job.startTime}-${effectiveEndTime(job.startTime, job.endTime)} ${job.id} ${getText(job.title)}`).join("\n");
      const ok = confirm(`ผู้ได้รับมอบหมายคนนี้มีงานอยู่ในช่วงเวลาดังกล่าว:\n\n${list}\n\nยืนยันที่จะมอบหมายงานซ้อนหรือไม่?\n\nหากยืนยัน กรุณาโทรแจ้งผู้ได้รับมอบหมาย${assignee?.phone ? `ที่เบอร์ ${assignee.phone}` : ""} ก่อนดำเนินการ`);
      if (!ok) return;
      conflictExtra = { hasScheduleConflict: true, conflictConfirmedAt: new Date().toISOString(), conflictConfirmedBy: currentUser.id };
    }
  }
  const attachments = await Promise.all(files.map(fileToDataUrl));
  createJob({ ...data, initialAttachments: attachments, ...conflictExtra });
  form.reset();
  $("#createModal").classList.add("hidden");
  renderAll();
  showToast(t("toast.created"));
});
["jobSearch", "statusFilter", "categoryFilter"].forEach(id => $("#" + id).addEventListener(id === "jobSearch" ? "input" : "change", renderJobList));
$("#teamSearchInput").addEventListener("input", e => {
  teamSearch = e.target.value;
  renderTeam();
});
$("#teamDepartmentFilter").addEventListener("change", e => {
  teamDepartmentFilter = e.target.value;
  renderTeam();
});
$("#residentSearchInput").addEventListener("input", e => {
  residentSearch = e.target.value;
  renderResidents();
});
$("#residentBuildingFilter").addEventListener("change", e => {
  residentBuildingFilter = e.target.value;
  renderResidents();
});
document.addEventListener("input", e => {
  if (e.target?.id === "permissionSearchInput") {
    permissionSearch = e.target.value;
    renderPermissions();
  }
  if (e.target?.id === "chatSearchInput") {
    chatSearch = e.target.value;
    renderChat();
    const input = $("#chatSearchInput");
    input?.focus();
    input?.setSelectionRange(chatSearch.length, chatSearch.length);
  }
});
$("#residentCsvInput").addEventListener("change", e => {
  const file = e.target.files?.[0];
  if (file && (canManageResidents() || hasActionPermission("importExport"))) importResidentsCsv(file);
  e.target.value = "";
});
$("#employeeCsvInput").addEventListener("change", e => {
  const file = e.target.files?.[0];
  if (file && canManageTeam()) importEmployeesCsv(file);
  e.target.value = "";
});
document.addEventListener("change", async e => {
  if (e.target?.id === "permissionDepartmentFilter") {
    permissionDepartmentFilter = e.target.value;
    renderPermissions();
    return;
  }
  if (e.target?.id === "bulkPermissionGroup") {
    bulkPermissionGroup = e.target.value;
    renderPermissions();
    return;
  }
  if (e.target?.id === "bulkPermissionPreset") {
    bulkPermissionPreset = e.target.value;
    renderPermissions();
    return;
  }
  if (e.target?.id === "bulkPermissionMode") {
    bulkPermissionMode = e.target.value;
    renderPermissions();
    return;
  }
  if (e.target?.id === "chatRoomFilter") {
    chatRoomFilter = e.target.value;
    activeChatRoomId = "";
    renderChat();
    return;
  }
  if (e.target?.id === "routineDashboardDate") {
    routineDashboardDate = e.target.value || todayIso();
    localStorage.setItem("juristicRoutineDashboardDate", routineDashboardDate);
    renderRoutineModule();
    return;
  }
  if (e.target?.id === "routineDepartmentFilter") {
    routineDepartmentFilter = e.target.value;
    renderRoutineModule();
    return;
  }
  if (e.target?.id === "routineStatusFilter") {
    routineStatusFilter = e.target.value;
    renderRoutineModule();
    return;
  }
  const financeImport = e.target.closest("#financeImportInput");
  if (financeImport) {
    const file = financeImport.files?.[0];
    if (file && canImportFinance()) {
      addLog("FINANCE_IMPORTED", `${file.name} imported by ${getUserName(currentUser)}`);
      showToast(currentLang === "th" ? `Import งบการเงิน mock: ${file.name}` : `Finance mock imported: ${file.name}`);
    }
    financeImport.value = "";
  }
  const backhouseUpload = e.target.closest("#backhouseUploadInput");
  if (backhouseUpload) {
    const file = backhouseUpload.files?.[0];
    if (file && canManageAnnouncements()) {
      const isPdf = file.type === "application/pdf" || file.name.toLowerCase().endsWith(".pdf");
      const src = await fileToDataUrl(file);
      announcements.unshift({
        id: `ann-${Date.now()}`,
        title: { th: file.name.replace(/\.[^.]+$/, ""), en: file.name.replace(/\.[^.]+$/, "") },
        type: isPdf ? "pdf" : "image",
        date: new Date().toISOString().slice(0, 10),
        audience: { th: "แสดงในหน้าภาพรวม", en: "Dashboard announcement" },
        summary: { th: isPdf ? "ไฟล์ PDF ที่อัปโหลดจากประกาศ" : "รูปภาพที่อัปโหลดจากประกาศ", en: isPdf ? "PDF uploaded from Announcements" : "Image uploaded from Announcements" },
        pages: [src]
      });
      backhouseDirty = true;
      renderBackhouse();
      renderAnnouncements();
      showToast(currentLang === "th" ? "เพิ่มประกาศแล้ว กดบันทึกเพื่อยืนยัน" : "Announcement added. Save to confirm.");
    }
    backhouseUpload.value = "";
  }
  const statusSelect = e.target.closest("[data-status-select]");
  if (statusSelect) syncStatusFields(statusSelect);
  const noPin = e.target.closest("[name='noPinAvailable']");
  if (noPin) syncStatusFields(noPin.closest("form")?.querySelector("[data-status-select]"));
  const currentPerson = e.target.closest("[data-current-person]");
  if (currentPerson) currentPerson.closest(".resident-primary-card")?.classList.toggle("selected", currentPerson.checked);
});
$("#adminLogDateFilter").addEventListener("change", e => {
  adminLogDateFilter = e.target.value;
  renderLogs();
});
$("#adminLogRoleFilter").addEventListener("change", e => {
  adminLogRoleFilter = e.target.value;
  renderLogs();
});

applyTeamOverrides();
jobs = jobs.map(normalizeJob).filter(Boolean);
saveJobs();
translateStaticDom();
applyDeviceMode();
window.addEventListener("resize", () => { clearTimeout(applyDeviceMode.timer); applyDeviceMode.timer = setTimeout(applyDeviceMode, 120); });
const sessionUser = getUser(sessionStorage.getItem("juristicUser"));
if (sessionUser) showApp(sessionUser);
