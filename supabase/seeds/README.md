# Local Seed Modules

Add deterministic feature seed modules here and include them from `../seed.sql` only after domain approval. Seeds must use synthetic identities and must be safe to reset locally.
