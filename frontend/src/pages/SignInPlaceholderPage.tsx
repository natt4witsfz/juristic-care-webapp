import { PageHeader } from '../components/layout/PageHeader';

export function SignInPlaceholderPage() {
  return (
    <PageHeader
      description="Authentication is owned by a later approved sprint. This route reserves a stable navigation destination without implementing sign-in behavior."
      eyebrow="Reserved route"
      title="Sign in is not enabled yet."
    />
  );
}
